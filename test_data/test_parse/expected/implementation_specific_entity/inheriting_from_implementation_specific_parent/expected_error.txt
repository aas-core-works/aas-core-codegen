We do not know how to parse the AST node: ClassDef(name='Parent', bases=[], keywords=[], body=[Pass()], decorator_list=[Name(id='abstract', ctx=Load()), Name(id='implementation_specific', ctx=Load())])
Abstract classes can not be implementation-specific at the same time (otherwise we can not convert them to interfaces etc.)