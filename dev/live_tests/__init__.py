"""
Perform the live tests on SDKs.

That means, generate the code, compile it, if necessary, and run the generated tests.

These live tests are expected to replace :py:mod:`integration_tests` once complete.
"""
