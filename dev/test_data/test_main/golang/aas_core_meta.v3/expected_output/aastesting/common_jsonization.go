package aastesting

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

import (
	"fmt"
	"path"
	aasjsonization "github.com/aas-core-works/aas-core3.0-golang/jsonization"
	aastypes "github.com/aas-core-works/aas-core3.0-golang/types"
)

// Load a maximal example of [aastypes.IExtension] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMaximalExtension(
) (result aastypes.IExtension) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"Extension",
		"maximal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.ExtensionFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IExtension " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IExtension)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IExtension at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a minimal example of [aastypes.IExtension] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMinimalExtension(
) (result aastypes.IExtension) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"Extension",
		"minimal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.ExtensionFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IExtension " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IExtension)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IExtension at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a maximal example of [aastypes.IAdministrativeInformation] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMaximalAdministrativeInformation(
) (result aastypes.IAdministrativeInformation) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"AdministrativeInformation",
		"maximal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.AdministrativeInformationFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IAdministrativeInformation " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IAdministrativeInformation)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IAdministrativeInformation at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a minimal example of [aastypes.IAdministrativeInformation] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMinimalAdministrativeInformation(
) (result aastypes.IAdministrativeInformation) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"AdministrativeInformation",
		"minimal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.AdministrativeInformationFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IAdministrativeInformation " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IAdministrativeInformation)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IAdministrativeInformation at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a maximal example of [aastypes.IQualifier] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMaximalQualifier(
) (result aastypes.IQualifier) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"Qualifier",
		"maximal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.QualifierFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IQualifier " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IQualifier)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IQualifier at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a minimal example of [aastypes.IQualifier] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMinimalQualifier(
) (result aastypes.IQualifier) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"Qualifier",
		"minimal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.QualifierFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IQualifier " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IQualifier)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IQualifier at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a maximal example of [aastypes.IAssetAdministrationShell] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMaximalAssetAdministrationShell(
) (result aastypes.IAssetAdministrationShell) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"AssetAdministrationShell",
		"maximal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.AssetAdministrationShellFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IAssetAdministrationShell " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IAssetAdministrationShell)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IAssetAdministrationShell at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a minimal example of [aastypes.IAssetAdministrationShell] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMinimalAssetAdministrationShell(
) (result aastypes.IAssetAdministrationShell) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"AssetAdministrationShell",
		"minimal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.AssetAdministrationShellFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IAssetAdministrationShell " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IAssetAdministrationShell)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IAssetAdministrationShell at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a maximal example of [aastypes.IAssetInformation] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMaximalAssetInformation(
) (result aastypes.IAssetInformation) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"AssetInformation",
		"maximal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.AssetInformationFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IAssetInformation " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IAssetInformation)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IAssetInformation at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a minimal example of [aastypes.IAssetInformation] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMinimalAssetInformation(
) (result aastypes.IAssetInformation) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"AssetInformation",
		"minimal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.AssetInformationFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IAssetInformation " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IAssetInformation)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IAssetInformation at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a maximal example of [aastypes.IResource] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMaximalResource(
) (result aastypes.IResource) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"Resource",
		"maximal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.ResourceFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IResource " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IResource)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IResource at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a minimal example of [aastypes.IResource] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMinimalResource(
) (result aastypes.IResource) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"Resource",
		"minimal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.ResourceFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IResource " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IResource)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IResource at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a maximal example of [aastypes.ISpecificAssetID] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMaximalSpecificAssetID(
) (result aastypes.ISpecificAssetID) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"SpecificAssetId",
		"maximal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.SpecificAssetIDFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of ISpecificAssetID " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.ISpecificAssetID)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of ISpecificAssetID at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a minimal example of [aastypes.ISpecificAssetID] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMinimalSpecificAssetID(
) (result aastypes.ISpecificAssetID) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"SpecificAssetId",
		"minimal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.SpecificAssetIDFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of ISpecificAssetID " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.ISpecificAssetID)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of ISpecificAssetID at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a maximal example of [aastypes.ISubmodel] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMaximalSubmodel(
) (result aastypes.ISubmodel) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"Submodel",
		"maximal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.SubmodelFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of ISubmodel " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.ISubmodel)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of ISubmodel at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a minimal example of [aastypes.ISubmodel] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMinimalSubmodel(
) (result aastypes.ISubmodel) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"Submodel",
		"minimal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.SubmodelFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of ISubmodel " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.ISubmodel)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of ISubmodel at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a maximal example of [aastypes.IRelationshipElement] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMaximalRelationshipElement(
) (result aastypes.IRelationshipElement) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"RelationshipElement",
		"maximal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.RelationshipElementFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IRelationshipElement " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IRelationshipElement)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IRelationshipElement at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a minimal example of [aastypes.IRelationshipElement] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMinimalRelationshipElement(
) (result aastypes.IRelationshipElement) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"RelationshipElement",
		"minimal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.RelationshipElementFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IRelationshipElement " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IRelationshipElement)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IRelationshipElement at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a maximal example of [aastypes.ISubmodelElementList] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMaximalSubmodelElementList(
) (result aastypes.ISubmodelElementList) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"SubmodelElementList",
		"maximal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.SubmodelElementListFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of ISubmodelElementList " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.ISubmodelElementList)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of ISubmodelElementList at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a minimal example of [aastypes.ISubmodelElementList] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMinimalSubmodelElementList(
) (result aastypes.ISubmodelElementList) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"SubmodelElementList",
		"minimal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.SubmodelElementListFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of ISubmodelElementList " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.ISubmodelElementList)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of ISubmodelElementList at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a maximal example of [aastypes.ISubmodelElementCollection] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMaximalSubmodelElementCollection(
) (result aastypes.ISubmodelElementCollection) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"SubmodelElementCollection",
		"maximal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.SubmodelElementCollectionFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of ISubmodelElementCollection " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.ISubmodelElementCollection)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of ISubmodelElementCollection at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a minimal example of [aastypes.ISubmodelElementCollection] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMinimalSubmodelElementCollection(
) (result aastypes.ISubmodelElementCollection) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"SubmodelElementCollection",
		"minimal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.SubmodelElementCollectionFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of ISubmodelElementCollection " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.ISubmodelElementCollection)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of ISubmodelElementCollection at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a maximal example of [aastypes.IProperty] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMaximalProperty(
) (result aastypes.IProperty) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"Property",
		"maximal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.PropertyFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IProperty " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IProperty)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IProperty at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a minimal example of [aastypes.IProperty] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMinimalProperty(
) (result aastypes.IProperty) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"Property",
		"minimal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.PropertyFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IProperty " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IProperty)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IProperty at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a maximal example of [aastypes.IMultiLanguageProperty] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMaximalMultiLanguageProperty(
) (result aastypes.IMultiLanguageProperty) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"MultiLanguageProperty",
		"maximal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.MultiLanguagePropertyFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IMultiLanguageProperty " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IMultiLanguageProperty)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IMultiLanguageProperty at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a minimal example of [aastypes.IMultiLanguageProperty] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMinimalMultiLanguageProperty(
) (result aastypes.IMultiLanguageProperty) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"MultiLanguageProperty",
		"minimal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.MultiLanguagePropertyFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IMultiLanguageProperty " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IMultiLanguageProperty)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IMultiLanguageProperty at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a maximal example of [aastypes.IRange] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMaximalRange(
) (result aastypes.IRange) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"Range",
		"maximal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.RangeFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IRange " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IRange)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IRange at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a minimal example of [aastypes.IRange] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMinimalRange(
) (result aastypes.IRange) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"Range",
		"minimal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.RangeFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IRange " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IRange)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IRange at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a maximal example of [aastypes.IReferenceElement] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMaximalReferenceElement(
) (result aastypes.IReferenceElement) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"ReferenceElement",
		"maximal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.ReferenceElementFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IReferenceElement " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IReferenceElement)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IReferenceElement at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a minimal example of [aastypes.IReferenceElement] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMinimalReferenceElement(
) (result aastypes.IReferenceElement) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"ReferenceElement",
		"minimal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.ReferenceElementFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IReferenceElement " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IReferenceElement)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IReferenceElement at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a maximal example of [aastypes.IBlob] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMaximalBlob(
) (result aastypes.IBlob) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"Blob",
		"maximal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.BlobFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IBlob " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IBlob)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IBlob at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a minimal example of [aastypes.IBlob] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMinimalBlob(
) (result aastypes.IBlob) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"Blob",
		"minimal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.BlobFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IBlob " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IBlob)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IBlob at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a maximal example of [aastypes.IFile] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMaximalFile(
) (result aastypes.IFile) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"File",
		"maximal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.FileFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IFile " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IFile)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IFile at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a minimal example of [aastypes.IFile] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMinimalFile(
) (result aastypes.IFile) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"File",
		"minimal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.FileFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IFile " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IFile)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IFile at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a maximal example of [aastypes.IAnnotatedRelationshipElement] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMaximalAnnotatedRelationshipElement(
) (result aastypes.IAnnotatedRelationshipElement) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"AnnotatedRelationshipElement",
		"maximal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.AnnotatedRelationshipElementFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IAnnotatedRelationshipElement " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IAnnotatedRelationshipElement)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IAnnotatedRelationshipElement at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a minimal example of [aastypes.IAnnotatedRelationshipElement] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMinimalAnnotatedRelationshipElement(
) (result aastypes.IAnnotatedRelationshipElement) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"AnnotatedRelationshipElement",
		"minimal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.AnnotatedRelationshipElementFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IAnnotatedRelationshipElement " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IAnnotatedRelationshipElement)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IAnnotatedRelationshipElement at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a maximal example of [aastypes.IEntity] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMaximalEntity(
) (result aastypes.IEntity) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"Entity",
		"maximal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.EntityFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IEntity " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IEntity)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IEntity at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a minimal example of [aastypes.IEntity] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMinimalEntity(
) (result aastypes.IEntity) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"Entity",
		"minimal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.EntityFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IEntity " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IEntity)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IEntity at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a maximal example of [aastypes.IEventPayload] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMaximalEventPayload(
) (result aastypes.IEventPayload) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"EventPayload",
		"maximal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.EventPayloadFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IEventPayload " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IEventPayload)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IEventPayload at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a minimal example of [aastypes.IEventPayload] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMinimalEventPayload(
) (result aastypes.IEventPayload) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"EventPayload",
		"minimal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.EventPayloadFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IEventPayload " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IEventPayload)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IEventPayload at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a maximal example of [aastypes.IBasicEventElement] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMaximalBasicEventElement(
) (result aastypes.IBasicEventElement) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"BasicEventElement",
		"maximal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.BasicEventElementFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IBasicEventElement " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IBasicEventElement)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IBasicEventElement at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a minimal example of [aastypes.IBasicEventElement] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMinimalBasicEventElement(
) (result aastypes.IBasicEventElement) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"BasicEventElement",
		"minimal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.BasicEventElementFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IBasicEventElement " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IBasicEventElement)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IBasicEventElement at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a maximal example of [aastypes.IOperation] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMaximalOperation(
) (result aastypes.IOperation) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"Operation",
		"maximal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.OperationFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IOperation " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IOperation)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IOperation at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a minimal example of [aastypes.IOperation] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMinimalOperation(
) (result aastypes.IOperation) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"Operation",
		"minimal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.OperationFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IOperation " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IOperation)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IOperation at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a maximal example of [aastypes.IOperationVariable] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMaximalOperationVariable(
) (result aastypes.IOperationVariable) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"OperationVariable",
		"maximal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.OperationVariableFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IOperationVariable " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IOperationVariable)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IOperationVariable at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a minimal example of [aastypes.IOperationVariable] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMinimalOperationVariable(
) (result aastypes.IOperationVariable) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"OperationVariable",
		"minimal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.OperationVariableFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IOperationVariable " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IOperationVariable)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IOperationVariable at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a maximal example of [aastypes.ICapability] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMaximalCapability(
) (result aastypes.ICapability) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"Capability",
		"maximal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.CapabilityFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of ICapability " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.ICapability)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of ICapability at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a minimal example of [aastypes.ICapability] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMinimalCapability(
) (result aastypes.ICapability) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"Capability",
		"minimal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.CapabilityFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of ICapability " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.ICapability)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of ICapability at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a maximal example of [aastypes.IConceptDescription] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMaximalConceptDescription(
) (result aastypes.IConceptDescription) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"ConceptDescription",
		"maximal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.ConceptDescriptionFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IConceptDescription " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IConceptDescription)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IConceptDescription at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a minimal example of [aastypes.IConceptDescription] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMinimalConceptDescription(
) (result aastypes.IConceptDescription) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"ConceptDescription",
		"minimal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.ConceptDescriptionFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IConceptDescription " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IConceptDescription)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IConceptDescription at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a maximal example of [aastypes.IReference] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMaximalReference(
) (result aastypes.IReference) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"Reference",
		"maximal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.ReferenceFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IReference " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IReference)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IReference at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a minimal example of [aastypes.IReference] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMinimalReference(
) (result aastypes.IReference) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"Reference",
		"minimal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.ReferenceFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IReference " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IReference)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IReference at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a maximal example of [aastypes.IKey] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMaximalKey(
) (result aastypes.IKey) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"Key",
		"maximal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.KeyFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IKey " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IKey)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IKey at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a minimal example of [aastypes.IKey] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMinimalKey(
) (result aastypes.IKey) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"Key",
		"minimal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.KeyFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IKey " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IKey)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IKey at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a maximal example of [aastypes.ILangStringNameType] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMaximalLangStringNameType(
) (result aastypes.ILangStringNameType) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"LangStringNameType",
		"maximal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.LangStringNameTypeFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of ILangStringNameType " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.ILangStringNameType)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of ILangStringNameType at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a minimal example of [aastypes.ILangStringNameType] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMinimalLangStringNameType(
) (result aastypes.ILangStringNameType) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"LangStringNameType",
		"minimal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.LangStringNameTypeFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of ILangStringNameType " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.ILangStringNameType)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of ILangStringNameType at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a maximal example of [aastypes.ILangStringTextType] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMaximalLangStringTextType(
) (result aastypes.ILangStringTextType) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"LangStringTextType",
		"maximal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.LangStringTextTypeFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of ILangStringTextType " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.ILangStringTextType)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of ILangStringTextType at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a minimal example of [aastypes.ILangStringTextType] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMinimalLangStringTextType(
) (result aastypes.ILangStringTextType) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"LangStringTextType",
		"minimal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.LangStringTextTypeFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of ILangStringTextType " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.ILangStringTextType)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of ILangStringTextType at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a maximal example of [aastypes.IEnvironment] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMaximalEnvironment(
) (result aastypes.IEnvironment) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"Environment",
		"maximal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.EnvironmentFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IEnvironment " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IEnvironment)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IEnvironment at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a minimal example of [aastypes.IEnvironment] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMinimalEnvironment(
) (result aastypes.IEnvironment) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"Environment",
		"minimal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.EnvironmentFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IEnvironment " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IEnvironment)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IEnvironment at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a maximal example of [aastypes.IEmbeddedDataSpecification] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMaximalEmbeddedDataSpecification(
) (result aastypes.IEmbeddedDataSpecification) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"EmbeddedDataSpecification",
		"maximal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.EmbeddedDataSpecificationFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IEmbeddedDataSpecification " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IEmbeddedDataSpecification)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IEmbeddedDataSpecification at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a minimal example of [aastypes.IEmbeddedDataSpecification] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMinimalEmbeddedDataSpecification(
) (result aastypes.IEmbeddedDataSpecification) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"EmbeddedDataSpecification",
		"minimal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.EmbeddedDataSpecificationFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IEmbeddedDataSpecification " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IEmbeddedDataSpecification)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IEmbeddedDataSpecification at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a maximal example of [aastypes.ILevelType] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMaximalLevelType(
) (result aastypes.ILevelType) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"LevelType",
		"maximal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.LevelTypeFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of ILevelType " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.ILevelType)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of ILevelType at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a minimal example of [aastypes.ILevelType] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMinimalLevelType(
) (result aastypes.ILevelType) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"LevelType",
		"minimal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.LevelTypeFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of ILevelType " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.ILevelType)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of ILevelType at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a maximal example of [aastypes.IValueReferencePair] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMaximalValueReferencePair(
) (result aastypes.IValueReferencePair) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"ValueReferencePair",
		"maximal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.ValueReferencePairFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IValueReferencePair " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IValueReferencePair)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IValueReferencePair at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a minimal example of [aastypes.IValueReferencePair] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMinimalValueReferencePair(
) (result aastypes.IValueReferencePair) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"ValueReferencePair",
		"minimal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.ValueReferencePairFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IValueReferencePair " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IValueReferencePair)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IValueReferencePair at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a maximal example of [aastypes.IValueList] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMaximalValueList(
) (result aastypes.IValueList) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"ValueList",
		"maximal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.ValueListFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IValueList " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IValueList)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IValueList at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a minimal example of [aastypes.IValueList] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMinimalValueList(
) (result aastypes.IValueList) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"ValueList",
		"minimal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.ValueListFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IValueList " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IValueList)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IValueList at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a maximal example of [aastypes.ILangStringPreferredNameTypeIEC61360] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMaximalLangStringPreferredNameTypeIEC61360(
) (result aastypes.ILangStringPreferredNameTypeIEC61360) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"LangStringPreferredNameTypeIec61360",
		"maximal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.LangStringPreferredNameTypeIEC61360FromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of ILangStringPreferredNameTypeIEC61360 " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.ILangStringPreferredNameTypeIEC61360)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of ILangStringPreferredNameTypeIEC61360 at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a minimal example of [aastypes.ILangStringPreferredNameTypeIEC61360] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMinimalLangStringPreferredNameTypeIEC61360(
) (result aastypes.ILangStringPreferredNameTypeIEC61360) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"LangStringPreferredNameTypeIec61360",
		"minimal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.LangStringPreferredNameTypeIEC61360FromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of ILangStringPreferredNameTypeIEC61360 " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.ILangStringPreferredNameTypeIEC61360)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of ILangStringPreferredNameTypeIEC61360 at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a maximal example of [aastypes.ILangStringShortNameTypeIEC61360] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMaximalLangStringShortNameTypeIEC61360(
) (result aastypes.ILangStringShortNameTypeIEC61360) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"LangStringShortNameTypeIec61360",
		"maximal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.LangStringShortNameTypeIEC61360FromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of ILangStringShortNameTypeIEC61360 " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.ILangStringShortNameTypeIEC61360)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of ILangStringShortNameTypeIEC61360 at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a minimal example of [aastypes.ILangStringShortNameTypeIEC61360] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMinimalLangStringShortNameTypeIEC61360(
) (result aastypes.ILangStringShortNameTypeIEC61360) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"LangStringShortNameTypeIec61360",
		"minimal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.LangStringShortNameTypeIEC61360FromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of ILangStringShortNameTypeIEC61360 " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.ILangStringShortNameTypeIEC61360)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of ILangStringShortNameTypeIEC61360 at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a maximal example of [aastypes.ILangStringDefinitionTypeIEC61360] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMaximalLangStringDefinitionTypeIEC61360(
) (result aastypes.ILangStringDefinitionTypeIEC61360) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"LangStringDefinitionTypeIec61360",
		"maximal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.LangStringDefinitionTypeIEC61360FromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of ILangStringDefinitionTypeIEC61360 " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.ILangStringDefinitionTypeIEC61360)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of ILangStringDefinitionTypeIEC61360 at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a minimal example of [aastypes.ILangStringDefinitionTypeIEC61360] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMinimalLangStringDefinitionTypeIEC61360(
) (result aastypes.ILangStringDefinitionTypeIEC61360) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"LangStringDefinitionTypeIec61360",
		"minimal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.LangStringDefinitionTypeIEC61360FromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of ILangStringDefinitionTypeIEC61360 " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.ILangStringDefinitionTypeIEC61360)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of ILangStringDefinitionTypeIEC61360 at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a maximal example of [aastypes.IDataSpecificationIEC61360] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMaximalDataSpecificationIEC61360(
) (result aastypes.IDataSpecificationIEC61360) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"DataSpecificationIec61360",
		"maximal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.DataSpecificationIEC61360FromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IDataSpecificationIEC61360 " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IDataSpecificationIEC61360)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IDataSpecificationIEC61360 at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a minimal example of [aastypes.IDataSpecificationIEC61360] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMinimalDataSpecificationIEC61360(
) (result aastypes.IDataSpecificationIEC61360) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"DataSpecificationIec61360",
		"minimal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.DataSpecificationIEC61360FromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IDataSpecificationIEC61360 " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IDataSpecificationIEC61360)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IDataSpecificationIEC61360 at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.
