/**
 * Provide functions for loading generated examples of AAS instances.
 */

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

import * as path from "path";

import * as AasTypes from "../src/types";
import * as AasJsonization from "../src/jsonization";

import * as TestCommon from "./common";

/**
 * Load a maximal example of {@link types.Extension} from
 * the test data directory.
 */
export function loadMaximalExtension(
): AasTypes.Extension {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "Extension",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.extensionFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asExtension(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Extension in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.Extension} from
 * the test data directory.
 */
export function loadMinimalExtension(
): AasTypes.Extension {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "Extension",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.extensionFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asExtension(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Extension in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.AdministrativeInformation} from
 * the test data directory.
 */
export function loadMaximalAdministrativeInformation(
): AasTypes.AdministrativeInformation {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "AdministrativeInformation",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.administrativeInformationFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asAdministrativeInformation(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of AdministrativeInformation in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.AdministrativeInformation} from
 * the test data directory.
 */
export function loadMinimalAdministrativeInformation(
): AasTypes.AdministrativeInformation {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "AdministrativeInformation",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.administrativeInformationFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asAdministrativeInformation(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of AdministrativeInformation in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.Qualifier} from
 * the test data directory.
 */
export function loadMaximalQualifier(
): AasTypes.Qualifier {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "Qualifier",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.qualifierFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asQualifier(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Qualifier in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.Qualifier} from
 * the test data directory.
 */
export function loadMinimalQualifier(
): AasTypes.Qualifier {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "Qualifier",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.qualifierFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asQualifier(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Qualifier in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.AssetAdministrationShell} from
 * the test data directory.
 */
export function loadMaximalAssetAdministrationShell(
): AasTypes.AssetAdministrationShell {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "AssetAdministrationShell",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.assetAdministrationShellFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asAssetAdministrationShell(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of AssetAdministrationShell in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.AssetAdministrationShell} from
 * the test data directory.
 */
export function loadMinimalAssetAdministrationShell(
): AasTypes.AssetAdministrationShell {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "AssetAdministrationShell",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.assetAdministrationShellFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asAssetAdministrationShell(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of AssetAdministrationShell in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.AssetInformation} from
 * the test data directory.
 */
export function loadMaximalAssetInformation(
): AasTypes.AssetInformation {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "AssetInformation",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.assetInformationFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asAssetInformation(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of AssetInformation in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.AssetInformation} from
 * the test data directory.
 */
export function loadMinimalAssetInformation(
): AasTypes.AssetInformation {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "AssetInformation",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.assetInformationFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asAssetInformation(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of AssetInformation in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.Resource} from
 * the test data directory.
 */
export function loadMaximalResource(
): AasTypes.Resource {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "Resource",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.resourceFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asResource(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Resource in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.Resource} from
 * the test data directory.
 */
export function loadMinimalResource(
): AasTypes.Resource {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "Resource",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.resourceFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asResource(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Resource in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.SpecificAssetId} from
 * the test data directory.
 */
export function loadMaximalSpecificAssetId(
): AasTypes.SpecificAssetId {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "SpecificAssetId",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.specificAssetIdFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asSpecificAssetId(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of SpecificAssetId in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.SpecificAssetId} from
 * the test data directory.
 */
export function loadMinimalSpecificAssetId(
): AasTypes.SpecificAssetId {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "SpecificAssetId",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.specificAssetIdFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asSpecificAssetId(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of SpecificAssetId in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.Submodel} from
 * the test data directory.
 */
export function loadMaximalSubmodel(
): AasTypes.Submodel {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "Submodel",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.submodelFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asSubmodel(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Submodel in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.Submodel} from
 * the test data directory.
 */
export function loadMinimalSubmodel(
): AasTypes.Submodel {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "Submodel",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.submodelFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asSubmodel(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Submodel in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.RelationshipElement} from
 * the test data directory.
 */
export function loadMaximalRelationshipElement(
): AasTypes.RelationshipElement {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "RelationshipElement",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.relationshipElementFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asRelationshipElement(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of RelationshipElement in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.RelationshipElement} from
 * the test data directory.
 */
export function loadMinimalRelationshipElement(
): AasTypes.RelationshipElement {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "RelationshipElement",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.relationshipElementFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asRelationshipElement(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of RelationshipElement in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.SubmodelElementList} from
 * the test data directory.
 */
export function loadMaximalSubmodelElementList(
): AasTypes.SubmodelElementList {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "SubmodelElementList",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.submodelElementListFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asSubmodelElementList(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of SubmodelElementList in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.SubmodelElementList} from
 * the test data directory.
 */
export function loadMinimalSubmodelElementList(
): AasTypes.SubmodelElementList {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "SubmodelElementList",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.submodelElementListFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asSubmodelElementList(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of SubmodelElementList in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.SubmodelElementCollection} from
 * the test data directory.
 */
export function loadMaximalSubmodelElementCollection(
): AasTypes.SubmodelElementCollection {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "SubmodelElementCollection",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.submodelElementCollectionFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asSubmodelElementCollection(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of SubmodelElementCollection in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.SubmodelElementCollection} from
 * the test data directory.
 */
export function loadMinimalSubmodelElementCollection(
): AasTypes.SubmodelElementCollection {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "SubmodelElementCollection",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.submodelElementCollectionFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asSubmodelElementCollection(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of SubmodelElementCollection in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.Property} from
 * the test data directory.
 */
export function loadMaximalProperty(
): AasTypes.Property {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "Property",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.propertyFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asProperty(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Property in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.Property} from
 * the test data directory.
 */
export function loadMinimalProperty(
): AasTypes.Property {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "Property",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.propertyFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asProperty(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Property in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.MultiLanguageProperty} from
 * the test data directory.
 */
export function loadMaximalMultiLanguageProperty(
): AasTypes.MultiLanguageProperty {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "MultiLanguageProperty",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.multiLanguagePropertyFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asMultiLanguageProperty(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of MultiLanguageProperty in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.MultiLanguageProperty} from
 * the test data directory.
 */
export function loadMinimalMultiLanguageProperty(
): AasTypes.MultiLanguageProperty {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "MultiLanguageProperty",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.multiLanguagePropertyFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asMultiLanguageProperty(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of MultiLanguageProperty in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.Range} from
 * the test data directory.
 */
export function loadMaximalRange(
): AasTypes.Range {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "Range",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.rangeFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asRange(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Range in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.Range} from
 * the test data directory.
 */
export function loadMinimalRange(
): AasTypes.Range {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "Range",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.rangeFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asRange(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Range in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.ReferenceElement} from
 * the test data directory.
 */
export function loadMaximalReferenceElement(
): AasTypes.ReferenceElement {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "ReferenceElement",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.referenceElementFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asReferenceElement(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of ReferenceElement in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.ReferenceElement} from
 * the test data directory.
 */
export function loadMinimalReferenceElement(
): AasTypes.ReferenceElement {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "ReferenceElement",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.referenceElementFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asReferenceElement(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of ReferenceElement in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.Blob} from
 * the test data directory.
 */
export function loadMaximalBlob(
): AasTypes.Blob {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "Blob",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.blobFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asBlob(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Blob in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.Blob} from
 * the test data directory.
 */
export function loadMinimalBlob(
): AasTypes.Blob {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "Blob",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.blobFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asBlob(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Blob in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.File} from
 * the test data directory.
 */
export function loadMaximalFile(
): AasTypes.File {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "File",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.fileFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asFile(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of File in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.File} from
 * the test data directory.
 */
export function loadMinimalFile(
): AasTypes.File {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "File",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.fileFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asFile(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of File in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.AnnotatedRelationshipElement} from
 * the test data directory.
 */
export function loadMaximalAnnotatedRelationshipElement(
): AasTypes.AnnotatedRelationshipElement {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "AnnotatedRelationshipElement",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.annotatedRelationshipElementFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asAnnotatedRelationshipElement(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of AnnotatedRelationshipElement in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.AnnotatedRelationshipElement} from
 * the test data directory.
 */
export function loadMinimalAnnotatedRelationshipElement(
): AasTypes.AnnotatedRelationshipElement {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "AnnotatedRelationshipElement",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.annotatedRelationshipElementFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asAnnotatedRelationshipElement(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of AnnotatedRelationshipElement in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.Entity} from
 * the test data directory.
 */
export function loadMaximalEntity(
): AasTypes.Entity {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "Entity",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.entityFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asEntity(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Entity in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.Entity} from
 * the test data directory.
 */
export function loadMinimalEntity(
): AasTypes.Entity {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "Entity",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.entityFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asEntity(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Entity in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.EventPayload} from
 * the test data directory.
 */
export function loadMaximalEventPayload(
): AasTypes.EventPayload {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "EventPayload",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.eventPayloadFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asEventPayload(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of EventPayload in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.EventPayload} from
 * the test data directory.
 */
export function loadMinimalEventPayload(
): AasTypes.EventPayload {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "EventPayload",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.eventPayloadFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asEventPayload(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of EventPayload in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.BasicEventElement} from
 * the test data directory.
 */
export function loadMaximalBasicEventElement(
): AasTypes.BasicEventElement {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "BasicEventElement",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.basicEventElementFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asBasicEventElement(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of BasicEventElement in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.BasicEventElement} from
 * the test data directory.
 */
export function loadMinimalBasicEventElement(
): AasTypes.BasicEventElement {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "BasicEventElement",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.basicEventElementFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asBasicEventElement(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of BasicEventElement in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.Operation} from
 * the test data directory.
 */
export function loadMaximalOperation(
): AasTypes.Operation {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "Operation",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.operationFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asOperation(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Operation in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.Operation} from
 * the test data directory.
 */
export function loadMinimalOperation(
): AasTypes.Operation {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "Operation",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.operationFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asOperation(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Operation in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.OperationVariable} from
 * the test data directory.
 */
export function loadMaximalOperationVariable(
): AasTypes.OperationVariable {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "OperationVariable",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.operationVariableFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asOperationVariable(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of OperationVariable in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.OperationVariable} from
 * the test data directory.
 */
export function loadMinimalOperationVariable(
): AasTypes.OperationVariable {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "OperationVariable",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.operationVariableFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asOperationVariable(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of OperationVariable in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.Capability} from
 * the test data directory.
 */
export function loadMaximalCapability(
): AasTypes.Capability {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "Capability",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.capabilityFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asCapability(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Capability in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.Capability} from
 * the test data directory.
 */
export function loadMinimalCapability(
): AasTypes.Capability {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "Capability",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.capabilityFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asCapability(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Capability in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.ConceptDescription} from
 * the test data directory.
 */
export function loadMaximalConceptDescription(
): AasTypes.ConceptDescription {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "ConceptDescription",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.conceptDescriptionFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asConceptDescription(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of ConceptDescription in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.ConceptDescription} from
 * the test data directory.
 */
export function loadMinimalConceptDescription(
): AasTypes.ConceptDescription {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "ConceptDescription",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.conceptDescriptionFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asConceptDescription(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of ConceptDescription in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.Reference} from
 * the test data directory.
 */
export function loadMaximalReference(
): AasTypes.Reference {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "Reference",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.referenceFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asReference(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Reference in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.Reference} from
 * the test data directory.
 */
export function loadMinimalReference(
): AasTypes.Reference {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "Reference",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.referenceFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asReference(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Reference in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.Key} from
 * the test data directory.
 */
export function loadMaximalKey(
): AasTypes.Key {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "Key",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.keyFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asKey(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Key in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.Key} from
 * the test data directory.
 */
export function loadMinimalKey(
): AasTypes.Key {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "Key",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.keyFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asKey(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Key in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.LangStringNameType} from
 * the test data directory.
 */
export function loadMaximalLangStringNameType(
): AasTypes.LangStringNameType {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "LangStringNameType",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.langStringNameTypeFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asLangStringNameType(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of LangStringNameType in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.LangStringNameType} from
 * the test data directory.
 */
export function loadMinimalLangStringNameType(
): AasTypes.LangStringNameType {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "LangStringNameType",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.langStringNameTypeFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asLangStringNameType(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of LangStringNameType in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.LangStringTextType} from
 * the test data directory.
 */
export function loadMaximalLangStringTextType(
): AasTypes.LangStringTextType {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "LangStringTextType",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.langStringTextTypeFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asLangStringTextType(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of LangStringTextType in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.LangStringTextType} from
 * the test data directory.
 */
export function loadMinimalLangStringTextType(
): AasTypes.LangStringTextType {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "LangStringTextType",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.langStringTextTypeFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asLangStringTextType(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of LangStringTextType in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.Environment} from
 * the test data directory.
 */
export function loadMaximalEnvironment(
): AasTypes.Environment {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "Environment",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.environmentFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asEnvironment(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Environment in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.Environment} from
 * the test data directory.
 */
export function loadMinimalEnvironment(
): AasTypes.Environment {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "Environment",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.environmentFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asEnvironment(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Environment in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.EmbeddedDataSpecification} from
 * the test data directory.
 */
export function loadMaximalEmbeddedDataSpecification(
): AasTypes.EmbeddedDataSpecification {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "EmbeddedDataSpecification",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.embeddedDataSpecificationFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asEmbeddedDataSpecification(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of EmbeddedDataSpecification in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.EmbeddedDataSpecification} from
 * the test data directory.
 */
export function loadMinimalEmbeddedDataSpecification(
): AasTypes.EmbeddedDataSpecification {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "EmbeddedDataSpecification",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.embeddedDataSpecificationFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asEmbeddedDataSpecification(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of EmbeddedDataSpecification in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.LevelType} from
 * the test data directory.
 */
export function loadMaximalLevelType(
): AasTypes.LevelType {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "LevelType",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.levelTypeFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asLevelType(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of LevelType in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.LevelType} from
 * the test data directory.
 */
export function loadMinimalLevelType(
): AasTypes.LevelType {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "LevelType",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.levelTypeFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asLevelType(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of LevelType in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.ValueReferencePair} from
 * the test data directory.
 */
export function loadMaximalValueReferencePair(
): AasTypes.ValueReferencePair {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "ValueReferencePair",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.valueReferencePairFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asValueReferencePair(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of ValueReferencePair in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.ValueReferencePair} from
 * the test data directory.
 */
export function loadMinimalValueReferencePair(
): AasTypes.ValueReferencePair {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "ValueReferencePair",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.valueReferencePairFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asValueReferencePair(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of ValueReferencePair in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.ValueList} from
 * the test data directory.
 */
export function loadMaximalValueList(
): AasTypes.ValueList {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "ValueList",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.valueListFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asValueList(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of ValueList in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.ValueList} from
 * the test data directory.
 */
export function loadMinimalValueList(
): AasTypes.ValueList {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "ValueList",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.valueListFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asValueList(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of ValueList in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.LangStringPreferredNameTypeIec61360} from
 * the test data directory.
 */
export function loadMaximalLangStringPreferredNameTypeIec61360(
): AasTypes.LangStringPreferredNameTypeIec61360 {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "LangStringPreferredNameTypeIec61360",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.langStringPreferredNameTypeIec61360FromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asLangStringPreferredNameTypeIec61360(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of LangStringPreferredNameTypeIec61360 in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.LangStringPreferredNameTypeIec61360} from
 * the test data directory.
 */
export function loadMinimalLangStringPreferredNameTypeIec61360(
): AasTypes.LangStringPreferredNameTypeIec61360 {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "LangStringPreferredNameTypeIec61360",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.langStringPreferredNameTypeIec61360FromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asLangStringPreferredNameTypeIec61360(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of LangStringPreferredNameTypeIec61360 in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.LangStringShortNameTypeIec61360} from
 * the test data directory.
 */
export function loadMaximalLangStringShortNameTypeIec61360(
): AasTypes.LangStringShortNameTypeIec61360 {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "LangStringShortNameTypeIec61360",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.langStringShortNameTypeIec61360FromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asLangStringShortNameTypeIec61360(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of LangStringShortNameTypeIec61360 in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.LangStringShortNameTypeIec61360} from
 * the test data directory.
 */
export function loadMinimalLangStringShortNameTypeIec61360(
): AasTypes.LangStringShortNameTypeIec61360 {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "LangStringShortNameTypeIec61360",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.langStringShortNameTypeIec61360FromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asLangStringShortNameTypeIec61360(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of LangStringShortNameTypeIec61360 in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.LangStringDefinitionTypeIec61360} from
 * the test data directory.
 */
export function loadMaximalLangStringDefinitionTypeIec61360(
): AasTypes.LangStringDefinitionTypeIec61360 {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "LangStringDefinitionTypeIec61360",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.langStringDefinitionTypeIec61360FromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asLangStringDefinitionTypeIec61360(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of LangStringDefinitionTypeIec61360 in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.LangStringDefinitionTypeIec61360} from
 * the test data directory.
 */
export function loadMinimalLangStringDefinitionTypeIec61360(
): AasTypes.LangStringDefinitionTypeIec61360 {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "LangStringDefinitionTypeIec61360",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.langStringDefinitionTypeIec61360FromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asLangStringDefinitionTypeIec61360(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of LangStringDefinitionTypeIec61360 in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.DataSpecificationIec61360} from
 * the test data directory.
 */
export function loadMaximalDataSpecificationIec61360(
): AasTypes.DataSpecificationIec61360 {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "DataSpecificationIec61360",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.dataSpecificationIec61360FromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asDataSpecificationIec61360(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of DataSpecificationIec61360 in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.DataSpecificationIec61360} from
 * the test data directory.
 */
export function loadMinimalDataSpecificationIec61360(
): AasTypes.DataSpecificationIec61360 {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "DataSpecificationIec61360",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.dataSpecificationIec61360FromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asDataSpecificationIec61360(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of DataSpecificationIec61360 in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.
