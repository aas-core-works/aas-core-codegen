/**
 * Test XML de/serialization of concrete classes.
 */

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

import * as fs from "fs";
import * as path from "path";

import * as AasXmlization from "../src/xmlization";
import * as AasTypes from "../src/types";
import * as AasVerification from "../src/verification";

import * as TestCommon from "./common";

test("Extension XML round-trip OK", () => {
  const pths = Array.from(
    TestCommon.findFilesBySuffixRecursively(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Expected",
        "extension"
      ),
      ".xml"
    )
  );
  pths.sort();

  for (const pth of pths) {
    const text = fs.readFileSync(pth, "utf-8");

    const instanceOrError = AasXmlization.fromXmlString(text);
    expect(instanceOrError.error).toBeNull();
    const instance = instanceOrError.mustValue();

    const casted = AasTypes.asExtension(instance);
    if (casted === null) {
      throw new Error(
        `Expected instance of Extension in ${pth}, ` +
        `but got: ${typeof instance}`
      );
    }

    TestCommon.assertNoVerificationErrors(AasVerification.verify(casted), pth);

    const roundTripText = AasXmlization.toXmlString(casted);
    expect(roundTripText.length).toBeGreaterThan(0);
  }
});

test("Extension XML deserialization fail", () => {
  for (
    const causeDir of
    TestCommon.findImmediateSubdirectories(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Unexpected",
        "Unserializable"
      )
    )
  ) {
    const clsDir = path.join(
      causeDir,
      "extension"
    );
    if (!fs.existsSync(clsDir)) {
      continue;
    }

    const pths = Array.from(
      TestCommon.findFilesBySuffixRecursively(
        clsDir,
        ".xml"
      )
    );
    pths.sort();

    for (const pth of pths) {
      const text = fs.readFileSync(pth, "utf-8");
      const instanceOrError = AasXmlization.fromXmlString(text);
      expect(instanceOrError.error).not.toBeNull();
    }
  }
});

test("AdministrativeInformation XML round-trip OK", () => {
  const pths = Array.from(
    TestCommon.findFilesBySuffixRecursively(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Expected",
        "administrativeInformation"
      ),
      ".xml"
    )
  );
  pths.sort();

  for (const pth of pths) {
    const text = fs.readFileSync(pth, "utf-8");

    const instanceOrError = AasXmlization.fromXmlString(text);
    expect(instanceOrError.error).toBeNull();
    const instance = instanceOrError.mustValue();

    const casted = AasTypes.asAdministrativeInformation(instance);
    if (casted === null) {
      throw new Error(
        `Expected instance of AdministrativeInformation in ${pth}, ` +
        `but got: ${typeof instance}`
      );
    }

    TestCommon.assertNoVerificationErrors(AasVerification.verify(casted), pth);

    const roundTripText = AasXmlization.toXmlString(casted);
    expect(roundTripText.length).toBeGreaterThan(0);
  }
});

test("AdministrativeInformation XML deserialization fail", () => {
  for (
    const causeDir of
    TestCommon.findImmediateSubdirectories(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Unexpected",
        "Unserializable"
      )
    )
  ) {
    const clsDir = path.join(
      causeDir,
      "administrativeInformation"
    );
    if (!fs.existsSync(clsDir)) {
      continue;
    }

    const pths = Array.from(
      TestCommon.findFilesBySuffixRecursively(
        clsDir,
        ".xml"
      )
    );
    pths.sort();

    for (const pth of pths) {
      const text = fs.readFileSync(pth, "utf-8");
      const instanceOrError = AasXmlization.fromXmlString(text);
      expect(instanceOrError.error).not.toBeNull();
    }
  }
});

test("Qualifier XML round-trip OK", () => {
  const pths = Array.from(
    TestCommon.findFilesBySuffixRecursively(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Expected",
        "qualifier"
      ),
      ".xml"
    )
  );
  pths.sort();

  for (const pth of pths) {
    const text = fs.readFileSync(pth, "utf-8");

    const instanceOrError = AasXmlization.fromXmlString(text);
    expect(instanceOrError.error).toBeNull();
    const instance = instanceOrError.mustValue();

    const casted = AasTypes.asQualifier(instance);
    if (casted === null) {
      throw new Error(
        `Expected instance of Qualifier in ${pth}, ` +
        `but got: ${typeof instance}`
      );
    }

    TestCommon.assertNoVerificationErrors(AasVerification.verify(casted), pth);

    const roundTripText = AasXmlization.toXmlString(casted);
    expect(roundTripText.length).toBeGreaterThan(0);
  }
});

test("Qualifier XML deserialization fail", () => {
  for (
    const causeDir of
    TestCommon.findImmediateSubdirectories(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Unexpected",
        "Unserializable"
      )
    )
  ) {
    const clsDir = path.join(
      causeDir,
      "qualifier"
    );
    if (!fs.existsSync(clsDir)) {
      continue;
    }

    const pths = Array.from(
      TestCommon.findFilesBySuffixRecursively(
        clsDir,
        ".xml"
      )
    );
    pths.sort();

    for (const pth of pths) {
      const text = fs.readFileSync(pth, "utf-8");
      const instanceOrError = AasXmlization.fromXmlString(text);
      expect(instanceOrError.error).not.toBeNull();
    }
  }
});

test("AssetAdministrationShell XML round-trip OK", () => {
  const pths = Array.from(
    TestCommon.findFilesBySuffixRecursively(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Expected",
        "assetAdministrationShell"
      ),
      ".xml"
    )
  );
  pths.sort();

  for (const pth of pths) {
    const text = fs.readFileSync(pth, "utf-8");

    const instanceOrError = AasXmlization.fromXmlString(text);
    expect(instanceOrError.error).toBeNull();
    const instance = instanceOrError.mustValue();

    const casted = AasTypes.asAssetAdministrationShell(instance);
    if (casted === null) {
      throw new Error(
        `Expected instance of AssetAdministrationShell in ${pth}, ` +
        `but got: ${typeof instance}`
      );
    }

    TestCommon.assertNoVerificationErrors(AasVerification.verify(casted), pth);

    const roundTripText = AasXmlization.toXmlString(casted);
    expect(roundTripText.length).toBeGreaterThan(0);
  }
});

test("AssetAdministrationShell XML deserialization fail", () => {
  for (
    const causeDir of
    TestCommon.findImmediateSubdirectories(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Unexpected",
        "Unserializable"
      )
    )
  ) {
    const clsDir = path.join(
      causeDir,
      "assetAdministrationShell"
    );
    if (!fs.existsSync(clsDir)) {
      continue;
    }

    const pths = Array.from(
      TestCommon.findFilesBySuffixRecursively(
        clsDir,
        ".xml"
      )
    );
    pths.sort();

    for (const pth of pths) {
      const text = fs.readFileSync(pth, "utf-8");
      const instanceOrError = AasXmlization.fromXmlString(text);
      expect(instanceOrError.error).not.toBeNull();
    }
  }
});

test("AssetInformation XML round-trip OK", () => {
  const pths = Array.from(
    TestCommon.findFilesBySuffixRecursively(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Expected",
        "assetInformation"
      ),
      ".xml"
    )
  );
  pths.sort();

  for (const pth of pths) {
    const text = fs.readFileSync(pth, "utf-8");

    const instanceOrError = AasXmlization.fromXmlString(text);
    expect(instanceOrError.error).toBeNull();
    const instance = instanceOrError.mustValue();

    const casted = AasTypes.asAssetInformation(instance);
    if (casted === null) {
      throw new Error(
        `Expected instance of AssetInformation in ${pth}, ` +
        `but got: ${typeof instance}`
      );
    }

    TestCommon.assertNoVerificationErrors(AasVerification.verify(casted), pth);

    const roundTripText = AasXmlization.toXmlString(casted);
    expect(roundTripText.length).toBeGreaterThan(0);
  }
});

test("AssetInformation XML deserialization fail", () => {
  for (
    const causeDir of
    TestCommon.findImmediateSubdirectories(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Unexpected",
        "Unserializable"
      )
    )
  ) {
    const clsDir = path.join(
      causeDir,
      "assetInformation"
    );
    if (!fs.existsSync(clsDir)) {
      continue;
    }

    const pths = Array.from(
      TestCommon.findFilesBySuffixRecursively(
        clsDir,
        ".xml"
      )
    );
    pths.sort();

    for (const pth of pths) {
      const text = fs.readFileSync(pth, "utf-8");
      const instanceOrError = AasXmlization.fromXmlString(text);
      expect(instanceOrError.error).not.toBeNull();
    }
  }
});

test("Resource XML round-trip OK", () => {
  const pths = Array.from(
    TestCommon.findFilesBySuffixRecursively(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Expected",
        "resource"
      ),
      ".xml"
    )
  );
  pths.sort();

  for (const pth of pths) {
    const text = fs.readFileSync(pth, "utf-8");

    const instanceOrError = AasXmlization.fromXmlString(text);
    expect(instanceOrError.error).toBeNull();
    const instance = instanceOrError.mustValue();

    const casted = AasTypes.asResource(instance);
    if (casted === null) {
      throw new Error(
        `Expected instance of Resource in ${pth}, ` +
        `but got: ${typeof instance}`
      );
    }

    TestCommon.assertNoVerificationErrors(AasVerification.verify(casted), pth);

    const roundTripText = AasXmlization.toXmlString(casted);
    expect(roundTripText.length).toBeGreaterThan(0);
  }
});

test("Resource XML deserialization fail", () => {
  for (
    const causeDir of
    TestCommon.findImmediateSubdirectories(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Unexpected",
        "Unserializable"
      )
    )
  ) {
    const clsDir = path.join(
      causeDir,
      "resource"
    );
    if (!fs.existsSync(clsDir)) {
      continue;
    }

    const pths = Array.from(
      TestCommon.findFilesBySuffixRecursively(
        clsDir,
        ".xml"
      )
    );
    pths.sort();

    for (const pth of pths) {
      const text = fs.readFileSync(pth, "utf-8");
      const instanceOrError = AasXmlization.fromXmlString(text);
      expect(instanceOrError.error).not.toBeNull();
    }
  }
});

test("SpecificAssetId XML round-trip OK", () => {
  const pths = Array.from(
    TestCommon.findFilesBySuffixRecursively(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Expected",
        "specificAssetId"
      ),
      ".xml"
    )
  );
  pths.sort();

  for (const pth of pths) {
    const text = fs.readFileSync(pth, "utf-8");

    const instanceOrError = AasXmlization.fromXmlString(text);
    expect(instanceOrError.error).toBeNull();
    const instance = instanceOrError.mustValue();

    const casted = AasTypes.asSpecificAssetId(instance);
    if (casted === null) {
      throw new Error(
        `Expected instance of SpecificAssetId in ${pth}, ` +
        `but got: ${typeof instance}`
      );
    }

    TestCommon.assertNoVerificationErrors(AasVerification.verify(casted), pth);

    const roundTripText = AasXmlization.toXmlString(casted);
    expect(roundTripText.length).toBeGreaterThan(0);
  }
});

test("SpecificAssetId XML deserialization fail", () => {
  for (
    const causeDir of
    TestCommon.findImmediateSubdirectories(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Unexpected",
        "Unserializable"
      )
    )
  ) {
    const clsDir = path.join(
      causeDir,
      "specificAssetId"
    );
    if (!fs.existsSync(clsDir)) {
      continue;
    }

    const pths = Array.from(
      TestCommon.findFilesBySuffixRecursively(
        clsDir,
        ".xml"
      )
    );
    pths.sort();

    for (const pth of pths) {
      const text = fs.readFileSync(pth, "utf-8");
      const instanceOrError = AasXmlization.fromXmlString(text);
      expect(instanceOrError.error).not.toBeNull();
    }
  }
});

test("Submodel XML round-trip OK", () => {
  const pths = Array.from(
    TestCommon.findFilesBySuffixRecursively(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Expected",
        "submodel"
      ),
      ".xml"
    )
  );
  pths.sort();

  for (const pth of pths) {
    const text = fs.readFileSync(pth, "utf-8");

    const instanceOrError = AasXmlization.fromXmlString(text);
    expect(instanceOrError.error).toBeNull();
    const instance = instanceOrError.mustValue();

    const casted = AasTypes.asSubmodel(instance);
    if (casted === null) {
      throw new Error(
        `Expected instance of Submodel in ${pth}, ` +
        `but got: ${typeof instance}`
      );
    }

    TestCommon.assertNoVerificationErrors(AasVerification.verify(casted), pth);

    const roundTripText = AasXmlization.toXmlString(casted);
    expect(roundTripText.length).toBeGreaterThan(0);
  }
});

test("Submodel XML deserialization fail", () => {
  for (
    const causeDir of
    TestCommon.findImmediateSubdirectories(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Unexpected",
        "Unserializable"
      )
    )
  ) {
    const clsDir = path.join(
      causeDir,
      "submodel"
    );
    if (!fs.existsSync(clsDir)) {
      continue;
    }

    const pths = Array.from(
      TestCommon.findFilesBySuffixRecursively(
        clsDir,
        ".xml"
      )
    );
    pths.sort();

    for (const pth of pths) {
      const text = fs.readFileSync(pth, "utf-8");
      const instanceOrError = AasXmlization.fromXmlString(text);
      expect(instanceOrError.error).not.toBeNull();
    }
  }
});

test("RelationshipElement XML round-trip OK", () => {
  const pths = Array.from(
    TestCommon.findFilesBySuffixRecursively(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Expected",
        "relationshipElement"
      ),
      ".xml"
    )
  );
  pths.sort();

  for (const pth of pths) {
    const text = fs.readFileSync(pth, "utf-8");

    const instanceOrError = AasXmlization.fromXmlString(text);
    expect(instanceOrError.error).toBeNull();
    const instance = instanceOrError.mustValue();

    const casted = AasTypes.asRelationshipElement(instance);
    if (casted === null) {
      throw new Error(
        `Expected instance of RelationshipElement in ${pth}, ` +
        `but got: ${typeof instance}`
      );
    }

    TestCommon.assertNoVerificationErrors(AasVerification.verify(casted), pth);

    const roundTripText = AasXmlization.toXmlString(casted);
    expect(roundTripText.length).toBeGreaterThan(0);
  }
});

test("RelationshipElement XML deserialization fail", () => {
  for (
    const causeDir of
    TestCommon.findImmediateSubdirectories(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Unexpected",
        "Unserializable"
      )
    )
  ) {
    const clsDir = path.join(
      causeDir,
      "relationshipElement"
    );
    if (!fs.existsSync(clsDir)) {
      continue;
    }

    const pths = Array.from(
      TestCommon.findFilesBySuffixRecursively(
        clsDir,
        ".xml"
      )
    );
    pths.sort();

    for (const pth of pths) {
      const text = fs.readFileSync(pth, "utf-8");
      const instanceOrError = AasXmlization.fromXmlString(text);
      expect(instanceOrError.error).not.toBeNull();
    }
  }
});

test("SubmodelElementList XML round-trip OK", () => {
  const pths = Array.from(
    TestCommon.findFilesBySuffixRecursively(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Expected",
        "submodelElementList"
      ),
      ".xml"
    )
  );
  pths.sort();

  for (const pth of pths) {
    const text = fs.readFileSync(pth, "utf-8");

    const instanceOrError = AasXmlization.fromXmlString(text);
    expect(instanceOrError.error).toBeNull();
    const instance = instanceOrError.mustValue();

    const casted = AasTypes.asSubmodelElementList(instance);
    if (casted === null) {
      throw new Error(
        `Expected instance of SubmodelElementList in ${pth}, ` +
        `but got: ${typeof instance}`
      );
    }

    TestCommon.assertNoVerificationErrors(AasVerification.verify(casted), pth);

    const roundTripText = AasXmlization.toXmlString(casted);
    expect(roundTripText.length).toBeGreaterThan(0);
  }
});

test("SubmodelElementList XML deserialization fail", () => {
  for (
    const causeDir of
    TestCommon.findImmediateSubdirectories(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Unexpected",
        "Unserializable"
      )
    )
  ) {
    const clsDir = path.join(
      causeDir,
      "submodelElementList"
    );
    if (!fs.existsSync(clsDir)) {
      continue;
    }

    const pths = Array.from(
      TestCommon.findFilesBySuffixRecursively(
        clsDir,
        ".xml"
      )
    );
    pths.sort();

    for (const pth of pths) {
      const text = fs.readFileSync(pth, "utf-8");
      const instanceOrError = AasXmlization.fromXmlString(text);
      expect(instanceOrError.error).not.toBeNull();
    }
  }
});

test("SubmodelElementCollection XML round-trip OK", () => {
  const pths = Array.from(
    TestCommon.findFilesBySuffixRecursively(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Expected",
        "submodelElementCollection"
      ),
      ".xml"
    )
  );
  pths.sort();

  for (const pth of pths) {
    const text = fs.readFileSync(pth, "utf-8");

    const instanceOrError = AasXmlization.fromXmlString(text);
    expect(instanceOrError.error).toBeNull();
    const instance = instanceOrError.mustValue();

    const casted = AasTypes.asSubmodelElementCollection(instance);
    if (casted === null) {
      throw new Error(
        `Expected instance of SubmodelElementCollection in ${pth}, ` +
        `but got: ${typeof instance}`
      );
    }

    TestCommon.assertNoVerificationErrors(AasVerification.verify(casted), pth);

    const roundTripText = AasXmlization.toXmlString(casted);
    expect(roundTripText.length).toBeGreaterThan(0);
  }
});

test("SubmodelElementCollection XML deserialization fail", () => {
  for (
    const causeDir of
    TestCommon.findImmediateSubdirectories(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Unexpected",
        "Unserializable"
      )
    )
  ) {
    const clsDir = path.join(
      causeDir,
      "submodelElementCollection"
    );
    if (!fs.existsSync(clsDir)) {
      continue;
    }

    const pths = Array.from(
      TestCommon.findFilesBySuffixRecursively(
        clsDir,
        ".xml"
      )
    );
    pths.sort();

    for (const pth of pths) {
      const text = fs.readFileSync(pth, "utf-8");
      const instanceOrError = AasXmlization.fromXmlString(text);
      expect(instanceOrError.error).not.toBeNull();
    }
  }
});

test("Property XML round-trip OK", () => {
  const pths = Array.from(
    TestCommon.findFilesBySuffixRecursively(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Expected",
        "property"
      ),
      ".xml"
    )
  );
  pths.sort();

  for (const pth of pths) {
    const text = fs.readFileSync(pth, "utf-8");

    const instanceOrError = AasXmlization.fromXmlString(text);
    expect(instanceOrError.error).toBeNull();
    const instance = instanceOrError.mustValue();

    const casted = AasTypes.asProperty(instance);
    if (casted === null) {
      throw new Error(
        `Expected instance of Property in ${pth}, ` +
        `but got: ${typeof instance}`
      );
    }

    TestCommon.assertNoVerificationErrors(AasVerification.verify(casted), pth);

    const roundTripText = AasXmlization.toXmlString(casted);
    expect(roundTripText.length).toBeGreaterThan(0);
  }
});

test("Property XML deserialization fail", () => {
  for (
    const causeDir of
    TestCommon.findImmediateSubdirectories(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Unexpected",
        "Unserializable"
      )
    )
  ) {
    const clsDir = path.join(
      causeDir,
      "property"
    );
    if (!fs.existsSync(clsDir)) {
      continue;
    }

    const pths = Array.from(
      TestCommon.findFilesBySuffixRecursively(
        clsDir,
        ".xml"
      )
    );
    pths.sort();

    for (const pth of pths) {
      const text = fs.readFileSync(pth, "utf-8");
      const instanceOrError = AasXmlization.fromXmlString(text);
      expect(instanceOrError.error).not.toBeNull();
    }
  }
});

test("MultiLanguageProperty XML round-trip OK", () => {
  const pths = Array.from(
    TestCommon.findFilesBySuffixRecursively(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Expected",
        "multiLanguageProperty"
      ),
      ".xml"
    )
  );
  pths.sort();

  for (const pth of pths) {
    const text = fs.readFileSync(pth, "utf-8");

    const instanceOrError = AasXmlization.fromXmlString(text);
    expect(instanceOrError.error).toBeNull();
    const instance = instanceOrError.mustValue();

    const casted = AasTypes.asMultiLanguageProperty(instance);
    if (casted === null) {
      throw new Error(
        `Expected instance of MultiLanguageProperty in ${pth}, ` +
        `but got: ${typeof instance}`
      );
    }

    TestCommon.assertNoVerificationErrors(AasVerification.verify(casted), pth);

    const roundTripText = AasXmlization.toXmlString(casted);
    expect(roundTripText.length).toBeGreaterThan(0);
  }
});

test("MultiLanguageProperty XML deserialization fail", () => {
  for (
    const causeDir of
    TestCommon.findImmediateSubdirectories(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Unexpected",
        "Unserializable"
      )
    )
  ) {
    const clsDir = path.join(
      causeDir,
      "multiLanguageProperty"
    );
    if (!fs.existsSync(clsDir)) {
      continue;
    }

    const pths = Array.from(
      TestCommon.findFilesBySuffixRecursively(
        clsDir,
        ".xml"
      )
    );
    pths.sort();

    for (const pth of pths) {
      const text = fs.readFileSync(pth, "utf-8");
      const instanceOrError = AasXmlization.fromXmlString(text);
      expect(instanceOrError.error).not.toBeNull();
    }
  }
});

test("Range XML round-trip OK", () => {
  const pths = Array.from(
    TestCommon.findFilesBySuffixRecursively(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Expected",
        "range"
      ),
      ".xml"
    )
  );
  pths.sort();

  for (const pth of pths) {
    const text = fs.readFileSync(pth, "utf-8");

    const instanceOrError = AasXmlization.fromXmlString(text);
    expect(instanceOrError.error).toBeNull();
    const instance = instanceOrError.mustValue();

    const casted = AasTypes.asRange(instance);
    if (casted === null) {
      throw new Error(
        `Expected instance of Range in ${pth}, ` +
        `but got: ${typeof instance}`
      );
    }

    TestCommon.assertNoVerificationErrors(AasVerification.verify(casted), pth);

    const roundTripText = AasXmlization.toXmlString(casted);
    expect(roundTripText.length).toBeGreaterThan(0);
  }
});

test("Range XML deserialization fail", () => {
  for (
    const causeDir of
    TestCommon.findImmediateSubdirectories(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Unexpected",
        "Unserializable"
      )
    )
  ) {
    const clsDir = path.join(
      causeDir,
      "range"
    );
    if (!fs.existsSync(clsDir)) {
      continue;
    }

    const pths = Array.from(
      TestCommon.findFilesBySuffixRecursively(
        clsDir,
        ".xml"
      )
    );
    pths.sort();

    for (const pth of pths) {
      const text = fs.readFileSync(pth, "utf-8");
      const instanceOrError = AasXmlization.fromXmlString(text);
      expect(instanceOrError.error).not.toBeNull();
    }
  }
});

test("ReferenceElement XML round-trip OK", () => {
  const pths = Array.from(
    TestCommon.findFilesBySuffixRecursively(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Expected",
        "referenceElement"
      ),
      ".xml"
    )
  );
  pths.sort();

  for (const pth of pths) {
    const text = fs.readFileSync(pth, "utf-8");

    const instanceOrError = AasXmlization.fromXmlString(text);
    expect(instanceOrError.error).toBeNull();
    const instance = instanceOrError.mustValue();

    const casted = AasTypes.asReferenceElement(instance);
    if (casted === null) {
      throw new Error(
        `Expected instance of ReferenceElement in ${pth}, ` +
        `but got: ${typeof instance}`
      );
    }

    TestCommon.assertNoVerificationErrors(AasVerification.verify(casted), pth);

    const roundTripText = AasXmlization.toXmlString(casted);
    expect(roundTripText.length).toBeGreaterThan(0);
  }
});

test("ReferenceElement XML deserialization fail", () => {
  for (
    const causeDir of
    TestCommon.findImmediateSubdirectories(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Unexpected",
        "Unserializable"
      )
    )
  ) {
    const clsDir = path.join(
      causeDir,
      "referenceElement"
    );
    if (!fs.existsSync(clsDir)) {
      continue;
    }

    const pths = Array.from(
      TestCommon.findFilesBySuffixRecursively(
        clsDir,
        ".xml"
      )
    );
    pths.sort();

    for (const pth of pths) {
      const text = fs.readFileSync(pth, "utf-8");
      const instanceOrError = AasXmlization.fromXmlString(text);
      expect(instanceOrError.error).not.toBeNull();
    }
  }
});

test("Blob XML round-trip OK", () => {
  const pths = Array.from(
    TestCommon.findFilesBySuffixRecursively(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Expected",
        "blob"
      ),
      ".xml"
    )
  );
  pths.sort();

  for (const pth of pths) {
    const text = fs.readFileSync(pth, "utf-8");

    const instanceOrError = AasXmlization.fromXmlString(text);
    expect(instanceOrError.error).toBeNull();
    const instance = instanceOrError.mustValue();

    const casted = AasTypes.asBlob(instance);
    if (casted === null) {
      throw new Error(
        `Expected instance of Blob in ${pth}, ` +
        `but got: ${typeof instance}`
      );
    }

    TestCommon.assertNoVerificationErrors(AasVerification.verify(casted), pth);

    const roundTripText = AasXmlization.toXmlString(casted);
    expect(roundTripText.length).toBeGreaterThan(0);
  }
});

test("Blob XML deserialization fail", () => {
  for (
    const causeDir of
    TestCommon.findImmediateSubdirectories(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Unexpected",
        "Unserializable"
      )
    )
  ) {
    const clsDir = path.join(
      causeDir,
      "blob"
    );
    if (!fs.existsSync(clsDir)) {
      continue;
    }

    const pths = Array.from(
      TestCommon.findFilesBySuffixRecursively(
        clsDir,
        ".xml"
      )
    );
    pths.sort();

    for (const pth of pths) {
      const text = fs.readFileSync(pth, "utf-8");
      const instanceOrError = AasXmlization.fromXmlString(text);
      expect(instanceOrError.error).not.toBeNull();
    }
  }
});

test("File XML round-trip OK", () => {
  const pths = Array.from(
    TestCommon.findFilesBySuffixRecursively(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Expected",
        "file"
      ),
      ".xml"
    )
  );
  pths.sort();

  for (const pth of pths) {
    const text = fs.readFileSync(pth, "utf-8");

    const instanceOrError = AasXmlization.fromXmlString(text);
    expect(instanceOrError.error).toBeNull();
    const instance = instanceOrError.mustValue();

    const casted = AasTypes.asFile(instance);
    if (casted === null) {
      throw new Error(
        `Expected instance of File in ${pth}, ` +
        `but got: ${typeof instance}`
      );
    }

    TestCommon.assertNoVerificationErrors(AasVerification.verify(casted), pth);

    const roundTripText = AasXmlization.toXmlString(casted);
    expect(roundTripText.length).toBeGreaterThan(0);
  }
});

test("File XML deserialization fail", () => {
  for (
    const causeDir of
    TestCommon.findImmediateSubdirectories(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Unexpected",
        "Unserializable"
      )
    )
  ) {
    const clsDir = path.join(
      causeDir,
      "file"
    );
    if (!fs.existsSync(clsDir)) {
      continue;
    }

    const pths = Array.from(
      TestCommon.findFilesBySuffixRecursively(
        clsDir,
        ".xml"
      )
    );
    pths.sort();

    for (const pth of pths) {
      const text = fs.readFileSync(pth, "utf-8");
      const instanceOrError = AasXmlization.fromXmlString(text);
      expect(instanceOrError.error).not.toBeNull();
    }
  }
});

test("AnnotatedRelationshipElement XML round-trip OK", () => {
  const pths = Array.from(
    TestCommon.findFilesBySuffixRecursively(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Expected",
        "annotatedRelationshipElement"
      ),
      ".xml"
    )
  );
  pths.sort();

  for (const pth of pths) {
    const text = fs.readFileSync(pth, "utf-8");

    const instanceOrError = AasXmlization.fromXmlString(text);
    expect(instanceOrError.error).toBeNull();
    const instance = instanceOrError.mustValue();

    const casted = AasTypes.asAnnotatedRelationshipElement(instance);
    if (casted === null) {
      throw new Error(
        `Expected instance of AnnotatedRelationshipElement in ${pth}, ` +
        `but got: ${typeof instance}`
      );
    }

    TestCommon.assertNoVerificationErrors(AasVerification.verify(casted), pth);

    const roundTripText = AasXmlization.toXmlString(casted);
    expect(roundTripText.length).toBeGreaterThan(0);
  }
});

test("AnnotatedRelationshipElement XML deserialization fail", () => {
  for (
    const causeDir of
    TestCommon.findImmediateSubdirectories(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Unexpected",
        "Unserializable"
      )
    )
  ) {
    const clsDir = path.join(
      causeDir,
      "annotatedRelationshipElement"
    );
    if (!fs.existsSync(clsDir)) {
      continue;
    }

    const pths = Array.from(
      TestCommon.findFilesBySuffixRecursively(
        clsDir,
        ".xml"
      )
    );
    pths.sort();

    for (const pth of pths) {
      const text = fs.readFileSync(pth, "utf-8");
      const instanceOrError = AasXmlization.fromXmlString(text);
      expect(instanceOrError.error).not.toBeNull();
    }
  }
});

test("Entity XML round-trip OK", () => {
  const pths = Array.from(
    TestCommon.findFilesBySuffixRecursively(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Expected",
        "entity"
      ),
      ".xml"
    )
  );
  pths.sort();

  for (const pth of pths) {
    const text = fs.readFileSync(pth, "utf-8");

    const instanceOrError = AasXmlization.fromXmlString(text);
    expect(instanceOrError.error).toBeNull();
    const instance = instanceOrError.mustValue();

    const casted = AasTypes.asEntity(instance);
    if (casted === null) {
      throw new Error(
        `Expected instance of Entity in ${pth}, ` +
        `but got: ${typeof instance}`
      );
    }

    TestCommon.assertNoVerificationErrors(AasVerification.verify(casted), pth);

    const roundTripText = AasXmlization.toXmlString(casted);
    expect(roundTripText.length).toBeGreaterThan(0);
  }
});

test("Entity XML deserialization fail", () => {
  for (
    const causeDir of
    TestCommon.findImmediateSubdirectories(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Unexpected",
        "Unserializable"
      )
    )
  ) {
    const clsDir = path.join(
      causeDir,
      "entity"
    );
    if (!fs.existsSync(clsDir)) {
      continue;
    }

    const pths = Array.from(
      TestCommon.findFilesBySuffixRecursively(
        clsDir,
        ".xml"
      )
    );
    pths.sort();

    for (const pth of pths) {
      const text = fs.readFileSync(pth, "utf-8");
      const instanceOrError = AasXmlization.fromXmlString(text);
      expect(instanceOrError.error).not.toBeNull();
    }
  }
});

test("EventPayload XML round-trip OK", () => {
  const pths = Array.from(
    TestCommon.findFilesBySuffixRecursively(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Expected",
        "eventPayload"
      ),
      ".xml"
    )
  );
  pths.sort();

  for (const pth of pths) {
    const text = fs.readFileSync(pth, "utf-8");

    const instanceOrError = AasXmlization.fromXmlString(text);
    expect(instanceOrError.error).toBeNull();
    const instance = instanceOrError.mustValue();

    const casted = AasTypes.asEventPayload(instance);
    if (casted === null) {
      throw new Error(
        `Expected instance of EventPayload in ${pth}, ` +
        `but got: ${typeof instance}`
      );
    }

    TestCommon.assertNoVerificationErrors(AasVerification.verify(casted), pth);

    const roundTripText = AasXmlization.toXmlString(casted);
    expect(roundTripText.length).toBeGreaterThan(0);
  }
});

test("EventPayload XML deserialization fail", () => {
  for (
    const causeDir of
    TestCommon.findImmediateSubdirectories(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Unexpected",
        "Unserializable"
      )
    )
  ) {
    const clsDir = path.join(
      causeDir,
      "eventPayload"
    );
    if (!fs.existsSync(clsDir)) {
      continue;
    }

    const pths = Array.from(
      TestCommon.findFilesBySuffixRecursively(
        clsDir,
        ".xml"
      )
    );
    pths.sort();

    for (const pth of pths) {
      const text = fs.readFileSync(pth, "utf-8");
      const instanceOrError = AasXmlization.fromXmlString(text);
      expect(instanceOrError.error).not.toBeNull();
    }
  }
});

test("BasicEventElement XML round-trip OK", () => {
  const pths = Array.from(
    TestCommon.findFilesBySuffixRecursively(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Expected",
        "basicEventElement"
      ),
      ".xml"
    )
  );
  pths.sort();

  for (const pth of pths) {
    const text = fs.readFileSync(pth, "utf-8");

    const instanceOrError = AasXmlization.fromXmlString(text);
    expect(instanceOrError.error).toBeNull();
    const instance = instanceOrError.mustValue();

    const casted = AasTypes.asBasicEventElement(instance);
    if (casted === null) {
      throw new Error(
        `Expected instance of BasicEventElement in ${pth}, ` +
        `but got: ${typeof instance}`
      );
    }

    TestCommon.assertNoVerificationErrors(AasVerification.verify(casted), pth);

    const roundTripText = AasXmlization.toXmlString(casted);
    expect(roundTripText.length).toBeGreaterThan(0);
  }
});

test("BasicEventElement XML deserialization fail", () => {
  for (
    const causeDir of
    TestCommon.findImmediateSubdirectories(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Unexpected",
        "Unserializable"
      )
    )
  ) {
    const clsDir = path.join(
      causeDir,
      "basicEventElement"
    );
    if (!fs.existsSync(clsDir)) {
      continue;
    }

    const pths = Array.from(
      TestCommon.findFilesBySuffixRecursively(
        clsDir,
        ".xml"
      )
    );
    pths.sort();

    for (const pth of pths) {
      const text = fs.readFileSync(pth, "utf-8");
      const instanceOrError = AasXmlization.fromXmlString(text);
      expect(instanceOrError.error).not.toBeNull();
    }
  }
});

test("Operation XML round-trip OK", () => {
  const pths = Array.from(
    TestCommon.findFilesBySuffixRecursively(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Expected",
        "operation"
      ),
      ".xml"
    )
  );
  pths.sort();

  for (const pth of pths) {
    const text = fs.readFileSync(pth, "utf-8");

    const instanceOrError = AasXmlization.fromXmlString(text);
    expect(instanceOrError.error).toBeNull();
    const instance = instanceOrError.mustValue();

    const casted = AasTypes.asOperation(instance);
    if (casted === null) {
      throw new Error(
        `Expected instance of Operation in ${pth}, ` +
        `but got: ${typeof instance}`
      );
    }

    TestCommon.assertNoVerificationErrors(AasVerification.verify(casted), pth);

    const roundTripText = AasXmlization.toXmlString(casted);
    expect(roundTripText.length).toBeGreaterThan(0);
  }
});

test("Operation XML deserialization fail", () => {
  for (
    const causeDir of
    TestCommon.findImmediateSubdirectories(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Unexpected",
        "Unserializable"
      )
    )
  ) {
    const clsDir = path.join(
      causeDir,
      "operation"
    );
    if (!fs.existsSync(clsDir)) {
      continue;
    }

    const pths = Array.from(
      TestCommon.findFilesBySuffixRecursively(
        clsDir,
        ".xml"
      )
    );
    pths.sort();

    for (const pth of pths) {
      const text = fs.readFileSync(pth, "utf-8");
      const instanceOrError = AasXmlization.fromXmlString(text);
      expect(instanceOrError.error).not.toBeNull();
    }
  }
});

test("OperationVariable XML round-trip OK", () => {
  const pths = Array.from(
    TestCommon.findFilesBySuffixRecursively(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Expected",
        "operationVariable"
      ),
      ".xml"
    )
  );
  pths.sort();

  for (const pth of pths) {
    const text = fs.readFileSync(pth, "utf-8");

    const instanceOrError = AasXmlization.fromXmlString(text);
    expect(instanceOrError.error).toBeNull();
    const instance = instanceOrError.mustValue();

    const casted = AasTypes.asOperationVariable(instance);
    if (casted === null) {
      throw new Error(
        `Expected instance of OperationVariable in ${pth}, ` +
        `but got: ${typeof instance}`
      );
    }

    TestCommon.assertNoVerificationErrors(AasVerification.verify(casted), pth);

    const roundTripText = AasXmlization.toXmlString(casted);
    expect(roundTripText.length).toBeGreaterThan(0);
  }
});

test("OperationVariable XML deserialization fail", () => {
  for (
    const causeDir of
    TestCommon.findImmediateSubdirectories(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Unexpected",
        "Unserializable"
      )
    )
  ) {
    const clsDir = path.join(
      causeDir,
      "operationVariable"
    );
    if (!fs.existsSync(clsDir)) {
      continue;
    }

    const pths = Array.from(
      TestCommon.findFilesBySuffixRecursively(
        clsDir,
        ".xml"
      )
    );
    pths.sort();

    for (const pth of pths) {
      const text = fs.readFileSync(pth, "utf-8");
      const instanceOrError = AasXmlization.fromXmlString(text);
      expect(instanceOrError.error).not.toBeNull();
    }
  }
});

test("Capability XML round-trip OK", () => {
  const pths = Array.from(
    TestCommon.findFilesBySuffixRecursively(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Expected",
        "capability"
      ),
      ".xml"
    )
  );
  pths.sort();

  for (const pth of pths) {
    const text = fs.readFileSync(pth, "utf-8");

    const instanceOrError = AasXmlization.fromXmlString(text);
    expect(instanceOrError.error).toBeNull();
    const instance = instanceOrError.mustValue();

    const casted = AasTypes.asCapability(instance);
    if (casted === null) {
      throw new Error(
        `Expected instance of Capability in ${pth}, ` +
        `but got: ${typeof instance}`
      );
    }

    TestCommon.assertNoVerificationErrors(AasVerification.verify(casted), pth);

    const roundTripText = AasXmlization.toXmlString(casted);
    expect(roundTripText.length).toBeGreaterThan(0);
  }
});

test("Capability XML deserialization fail", () => {
  for (
    const causeDir of
    TestCommon.findImmediateSubdirectories(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Unexpected",
        "Unserializable"
      )
    )
  ) {
    const clsDir = path.join(
      causeDir,
      "capability"
    );
    if (!fs.existsSync(clsDir)) {
      continue;
    }

    const pths = Array.from(
      TestCommon.findFilesBySuffixRecursively(
        clsDir,
        ".xml"
      )
    );
    pths.sort();

    for (const pth of pths) {
      const text = fs.readFileSync(pth, "utf-8");
      const instanceOrError = AasXmlization.fromXmlString(text);
      expect(instanceOrError.error).not.toBeNull();
    }
  }
});

test("ConceptDescription XML round-trip OK", () => {
  const pths = Array.from(
    TestCommon.findFilesBySuffixRecursively(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Expected",
        "conceptDescription"
      ),
      ".xml"
    )
  );
  pths.sort();

  for (const pth of pths) {
    const text = fs.readFileSync(pth, "utf-8");

    const instanceOrError = AasXmlization.fromXmlString(text);
    expect(instanceOrError.error).toBeNull();
    const instance = instanceOrError.mustValue();

    const casted = AasTypes.asConceptDescription(instance);
    if (casted === null) {
      throw new Error(
        `Expected instance of ConceptDescription in ${pth}, ` +
        `but got: ${typeof instance}`
      );
    }

    TestCommon.assertNoVerificationErrors(AasVerification.verify(casted), pth);

    const roundTripText = AasXmlization.toXmlString(casted);
    expect(roundTripText.length).toBeGreaterThan(0);
  }
});

test("ConceptDescription XML deserialization fail", () => {
  for (
    const causeDir of
    TestCommon.findImmediateSubdirectories(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Unexpected",
        "Unserializable"
      )
    )
  ) {
    const clsDir = path.join(
      causeDir,
      "conceptDescription"
    );
    if (!fs.existsSync(clsDir)) {
      continue;
    }

    const pths = Array.from(
      TestCommon.findFilesBySuffixRecursively(
        clsDir,
        ".xml"
      )
    );
    pths.sort();

    for (const pth of pths) {
      const text = fs.readFileSync(pth, "utf-8");
      const instanceOrError = AasXmlization.fromXmlString(text);
      expect(instanceOrError.error).not.toBeNull();
    }
  }
});

test("Reference XML round-trip OK", () => {
  const pths = Array.from(
    TestCommon.findFilesBySuffixRecursively(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Expected",
        "reference"
      ),
      ".xml"
    )
  );
  pths.sort();

  for (const pth of pths) {
    const text = fs.readFileSync(pth, "utf-8");

    const instanceOrError = AasXmlization.fromXmlString(text);
    expect(instanceOrError.error).toBeNull();
    const instance = instanceOrError.mustValue();

    const casted = AasTypes.asReference(instance);
    if (casted === null) {
      throw new Error(
        `Expected instance of Reference in ${pth}, ` +
        `but got: ${typeof instance}`
      );
    }

    TestCommon.assertNoVerificationErrors(AasVerification.verify(casted), pth);

    const roundTripText = AasXmlization.toXmlString(casted);
    expect(roundTripText.length).toBeGreaterThan(0);
  }
});

test("Reference XML deserialization fail", () => {
  for (
    const causeDir of
    TestCommon.findImmediateSubdirectories(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Unexpected",
        "Unserializable"
      )
    )
  ) {
    const clsDir = path.join(
      causeDir,
      "reference"
    );
    if (!fs.existsSync(clsDir)) {
      continue;
    }

    const pths = Array.from(
      TestCommon.findFilesBySuffixRecursively(
        clsDir,
        ".xml"
      )
    );
    pths.sort();

    for (const pth of pths) {
      const text = fs.readFileSync(pth, "utf-8");
      const instanceOrError = AasXmlization.fromXmlString(text);
      expect(instanceOrError.error).not.toBeNull();
    }
  }
});

test("Key XML round-trip OK", () => {
  const pths = Array.from(
    TestCommon.findFilesBySuffixRecursively(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Expected",
        "key"
      ),
      ".xml"
    )
  );
  pths.sort();

  for (const pth of pths) {
    const text = fs.readFileSync(pth, "utf-8");

    const instanceOrError = AasXmlization.fromXmlString(text);
    expect(instanceOrError.error).toBeNull();
    const instance = instanceOrError.mustValue();

    const casted = AasTypes.asKey(instance);
    if (casted === null) {
      throw new Error(
        `Expected instance of Key in ${pth}, ` +
        `but got: ${typeof instance}`
      );
    }

    TestCommon.assertNoVerificationErrors(AasVerification.verify(casted), pth);

    const roundTripText = AasXmlization.toXmlString(casted);
    expect(roundTripText.length).toBeGreaterThan(0);
  }
});

test("Key XML deserialization fail", () => {
  for (
    const causeDir of
    TestCommon.findImmediateSubdirectories(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Unexpected",
        "Unserializable"
      )
    )
  ) {
    const clsDir = path.join(
      causeDir,
      "key"
    );
    if (!fs.existsSync(clsDir)) {
      continue;
    }

    const pths = Array.from(
      TestCommon.findFilesBySuffixRecursively(
        clsDir,
        ".xml"
      )
    );
    pths.sort();

    for (const pth of pths) {
      const text = fs.readFileSync(pth, "utf-8");
      const instanceOrError = AasXmlization.fromXmlString(text);
      expect(instanceOrError.error).not.toBeNull();
    }
  }
});

test("LangStringNameType XML round-trip OK", () => {
  const pths = Array.from(
    TestCommon.findFilesBySuffixRecursively(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Expected",
        "langStringNameType"
      ),
      ".xml"
    )
  );
  pths.sort();

  for (const pth of pths) {
    const text = fs.readFileSync(pth, "utf-8");

    const instanceOrError = AasXmlization.fromXmlString(text);
    expect(instanceOrError.error).toBeNull();
    const instance = instanceOrError.mustValue();

    const casted = AasTypes.asLangStringNameType(instance);
    if (casted === null) {
      throw new Error(
        `Expected instance of LangStringNameType in ${pth}, ` +
        `but got: ${typeof instance}`
      );
    }

    TestCommon.assertNoVerificationErrors(AasVerification.verify(casted), pth);

    const roundTripText = AasXmlization.toXmlString(casted);
    expect(roundTripText.length).toBeGreaterThan(0);
  }
});

test("LangStringNameType XML deserialization fail", () => {
  for (
    const causeDir of
    TestCommon.findImmediateSubdirectories(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Unexpected",
        "Unserializable"
      )
    )
  ) {
    const clsDir = path.join(
      causeDir,
      "langStringNameType"
    );
    if (!fs.existsSync(clsDir)) {
      continue;
    }

    const pths = Array.from(
      TestCommon.findFilesBySuffixRecursively(
        clsDir,
        ".xml"
      )
    );
    pths.sort();

    for (const pth of pths) {
      const text = fs.readFileSync(pth, "utf-8");
      const instanceOrError = AasXmlization.fromXmlString(text);
      expect(instanceOrError.error).not.toBeNull();
    }
  }
});

test("LangStringTextType XML round-trip OK", () => {
  const pths = Array.from(
    TestCommon.findFilesBySuffixRecursively(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Expected",
        "langStringTextType"
      ),
      ".xml"
    )
  );
  pths.sort();

  for (const pth of pths) {
    const text = fs.readFileSync(pth, "utf-8");

    const instanceOrError = AasXmlization.fromXmlString(text);
    expect(instanceOrError.error).toBeNull();
    const instance = instanceOrError.mustValue();

    const casted = AasTypes.asLangStringTextType(instance);
    if (casted === null) {
      throw new Error(
        `Expected instance of LangStringTextType in ${pth}, ` +
        `but got: ${typeof instance}`
      );
    }

    TestCommon.assertNoVerificationErrors(AasVerification.verify(casted), pth);

    const roundTripText = AasXmlization.toXmlString(casted);
    expect(roundTripText.length).toBeGreaterThan(0);
  }
});

test("LangStringTextType XML deserialization fail", () => {
  for (
    const causeDir of
    TestCommon.findImmediateSubdirectories(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Unexpected",
        "Unserializable"
      )
    )
  ) {
    const clsDir = path.join(
      causeDir,
      "langStringTextType"
    );
    if (!fs.existsSync(clsDir)) {
      continue;
    }

    const pths = Array.from(
      TestCommon.findFilesBySuffixRecursively(
        clsDir,
        ".xml"
      )
    );
    pths.sort();

    for (const pth of pths) {
      const text = fs.readFileSync(pth, "utf-8");
      const instanceOrError = AasXmlization.fromXmlString(text);
      expect(instanceOrError.error).not.toBeNull();
    }
  }
});

test("Environment XML round-trip OK", () => {
  const pths = Array.from(
    TestCommon.findFilesBySuffixRecursively(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Expected",
        "environment"
      ),
      ".xml"
    )
  );
  pths.sort();

  for (const pth of pths) {
    const text = fs.readFileSync(pth, "utf-8");

    const instanceOrError = AasXmlization.fromXmlString(text);
    expect(instanceOrError.error).toBeNull();
    const instance = instanceOrError.mustValue();

    const casted = AasTypes.asEnvironment(instance);
    if (casted === null) {
      throw new Error(
        `Expected instance of Environment in ${pth}, ` +
        `but got: ${typeof instance}`
      );
    }

    TestCommon.assertNoVerificationErrors(AasVerification.verify(casted), pth);

    const roundTripText = AasXmlization.toXmlString(casted);
    expect(roundTripText.length).toBeGreaterThan(0);
  }
});

test("Environment XML deserialization fail", () => {
  for (
    const causeDir of
    TestCommon.findImmediateSubdirectories(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Unexpected",
        "Unserializable"
      )
    )
  ) {
    const clsDir = path.join(
      causeDir,
      "environment"
    );
    if (!fs.existsSync(clsDir)) {
      continue;
    }

    const pths = Array.from(
      TestCommon.findFilesBySuffixRecursively(
        clsDir,
        ".xml"
      )
    );
    pths.sort();

    for (const pth of pths) {
      const text = fs.readFileSync(pth, "utf-8");
      const instanceOrError = AasXmlization.fromXmlString(text);
      expect(instanceOrError.error).not.toBeNull();
    }
  }
});

test("EmbeddedDataSpecification XML round-trip OK", () => {
  const pths = Array.from(
    TestCommon.findFilesBySuffixRecursively(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Expected",
        "embeddedDataSpecification"
      ),
      ".xml"
    )
  );
  pths.sort();

  for (const pth of pths) {
    const text = fs.readFileSync(pth, "utf-8");

    const instanceOrError = AasXmlization.fromXmlString(text);
    expect(instanceOrError.error).toBeNull();
    const instance = instanceOrError.mustValue();

    const casted = AasTypes.asEmbeddedDataSpecification(instance);
    if (casted === null) {
      throw new Error(
        `Expected instance of EmbeddedDataSpecification in ${pth}, ` +
        `but got: ${typeof instance}`
      );
    }

    TestCommon.assertNoVerificationErrors(AasVerification.verify(casted), pth);

    const roundTripText = AasXmlization.toXmlString(casted);
    expect(roundTripText.length).toBeGreaterThan(0);
  }
});

test("EmbeddedDataSpecification XML deserialization fail", () => {
  for (
    const causeDir of
    TestCommon.findImmediateSubdirectories(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Unexpected",
        "Unserializable"
      )
    )
  ) {
    const clsDir = path.join(
      causeDir,
      "embeddedDataSpecification"
    );
    if (!fs.existsSync(clsDir)) {
      continue;
    }

    const pths = Array.from(
      TestCommon.findFilesBySuffixRecursively(
        clsDir,
        ".xml"
      )
    );
    pths.sort();

    for (const pth of pths) {
      const text = fs.readFileSync(pth, "utf-8");
      const instanceOrError = AasXmlization.fromXmlString(text);
      expect(instanceOrError.error).not.toBeNull();
    }
  }
});

test("LevelType XML round-trip OK", () => {
  const pths = Array.from(
    TestCommon.findFilesBySuffixRecursively(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Expected",
        "levelType"
      ),
      ".xml"
    )
  );
  pths.sort();

  for (const pth of pths) {
    const text = fs.readFileSync(pth, "utf-8");

    const instanceOrError = AasXmlization.fromXmlString(text);
    expect(instanceOrError.error).toBeNull();
    const instance = instanceOrError.mustValue();

    const casted = AasTypes.asLevelType(instance);
    if (casted === null) {
      throw new Error(
        `Expected instance of LevelType in ${pth}, ` +
        `but got: ${typeof instance}`
      );
    }

    TestCommon.assertNoVerificationErrors(AasVerification.verify(casted), pth);

    const roundTripText = AasXmlization.toXmlString(casted);
    expect(roundTripText.length).toBeGreaterThan(0);
  }
});

test("LevelType XML deserialization fail", () => {
  for (
    const causeDir of
    TestCommon.findImmediateSubdirectories(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Unexpected",
        "Unserializable"
      )
    )
  ) {
    const clsDir = path.join(
      causeDir,
      "levelType"
    );
    if (!fs.existsSync(clsDir)) {
      continue;
    }

    const pths = Array.from(
      TestCommon.findFilesBySuffixRecursively(
        clsDir,
        ".xml"
      )
    );
    pths.sort();

    for (const pth of pths) {
      const text = fs.readFileSync(pth, "utf-8");
      const instanceOrError = AasXmlization.fromXmlString(text);
      expect(instanceOrError.error).not.toBeNull();
    }
  }
});

test("ValueReferencePair XML round-trip OK", () => {
  const pths = Array.from(
    TestCommon.findFilesBySuffixRecursively(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Expected",
        "valueReferencePair"
      ),
      ".xml"
    )
  );
  pths.sort();

  for (const pth of pths) {
    const text = fs.readFileSync(pth, "utf-8");

    const instanceOrError = AasXmlization.fromXmlString(text);
    expect(instanceOrError.error).toBeNull();
    const instance = instanceOrError.mustValue();

    const casted = AasTypes.asValueReferencePair(instance);
    if (casted === null) {
      throw new Error(
        `Expected instance of ValueReferencePair in ${pth}, ` +
        `but got: ${typeof instance}`
      );
    }

    TestCommon.assertNoVerificationErrors(AasVerification.verify(casted), pth);

    const roundTripText = AasXmlization.toXmlString(casted);
    expect(roundTripText.length).toBeGreaterThan(0);
  }
});

test("ValueReferencePair XML deserialization fail", () => {
  for (
    const causeDir of
    TestCommon.findImmediateSubdirectories(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Unexpected",
        "Unserializable"
      )
    )
  ) {
    const clsDir = path.join(
      causeDir,
      "valueReferencePair"
    );
    if (!fs.existsSync(clsDir)) {
      continue;
    }

    const pths = Array.from(
      TestCommon.findFilesBySuffixRecursively(
        clsDir,
        ".xml"
      )
    );
    pths.sort();

    for (const pth of pths) {
      const text = fs.readFileSync(pth, "utf-8");
      const instanceOrError = AasXmlization.fromXmlString(text);
      expect(instanceOrError.error).not.toBeNull();
    }
  }
});

test("ValueList XML round-trip OK", () => {
  const pths = Array.from(
    TestCommon.findFilesBySuffixRecursively(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Expected",
        "valueList"
      ),
      ".xml"
    )
  );
  pths.sort();

  for (const pth of pths) {
    const text = fs.readFileSync(pth, "utf-8");

    const instanceOrError = AasXmlization.fromXmlString(text);
    expect(instanceOrError.error).toBeNull();
    const instance = instanceOrError.mustValue();

    const casted = AasTypes.asValueList(instance);
    if (casted === null) {
      throw new Error(
        `Expected instance of ValueList in ${pth}, ` +
        `but got: ${typeof instance}`
      );
    }

    TestCommon.assertNoVerificationErrors(AasVerification.verify(casted), pth);

    const roundTripText = AasXmlization.toXmlString(casted);
    expect(roundTripText.length).toBeGreaterThan(0);
  }
});

test("ValueList XML deserialization fail", () => {
  for (
    const causeDir of
    TestCommon.findImmediateSubdirectories(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Unexpected",
        "Unserializable"
      )
    )
  ) {
    const clsDir = path.join(
      causeDir,
      "valueList"
    );
    if (!fs.existsSync(clsDir)) {
      continue;
    }

    const pths = Array.from(
      TestCommon.findFilesBySuffixRecursively(
        clsDir,
        ".xml"
      )
    );
    pths.sort();

    for (const pth of pths) {
      const text = fs.readFileSync(pth, "utf-8");
      const instanceOrError = AasXmlization.fromXmlString(text);
      expect(instanceOrError.error).not.toBeNull();
    }
  }
});

test("LangStringPreferredNameTypeIec61360 XML round-trip OK", () => {
  const pths = Array.from(
    TestCommon.findFilesBySuffixRecursively(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Expected",
        "langStringPreferredNameTypeIec61360"
      ),
      ".xml"
    )
  );
  pths.sort();

  for (const pth of pths) {
    const text = fs.readFileSync(pth, "utf-8");

    const instanceOrError = AasXmlization.fromXmlString(text);
    expect(instanceOrError.error).toBeNull();
    const instance = instanceOrError.mustValue();

    const casted = AasTypes.asLangStringPreferredNameTypeIec61360(instance);
    if (casted === null) {
      throw new Error(
        `Expected instance of LangStringPreferredNameTypeIec61360 in ${pth}, ` +
        `but got: ${typeof instance}`
      );
    }

    TestCommon.assertNoVerificationErrors(AasVerification.verify(casted), pth);

    const roundTripText = AasXmlization.toXmlString(casted);
    expect(roundTripText.length).toBeGreaterThan(0);
  }
});

test("LangStringPreferredNameTypeIec61360 XML deserialization fail", () => {
  for (
    const causeDir of
    TestCommon.findImmediateSubdirectories(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Unexpected",
        "Unserializable"
      )
    )
  ) {
    const clsDir = path.join(
      causeDir,
      "langStringPreferredNameTypeIec61360"
    );
    if (!fs.existsSync(clsDir)) {
      continue;
    }

    const pths = Array.from(
      TestCommon.findFilesBySuffixRecursively(
        clsDir,
        ".xml"
      )
    );
    pths.sort();

    for (const pth of pths) {
      const text = fs.readFileSync(pth, "utf-8");
      const instanceOrError = AasXmlization.fromXmlString(text);
      expect(instanceOrError.error).not.toBeNull();
    }
  }
});

test("LangStringShortNameTypeIec61360 XML round-trip OK", () => {
  const pths = Array.from(
    TestCommon.findFilesBySuffixRecursively(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Expected",
        "langStringShortNameTypeIec61360"
      ),
      ".xml"
    )
  );
  pths.sort();

  for (const pth of pths) {
    const text = fs.readFileSync(pth, "utf-8");

    const instanceOrError = AasXmlization.fromXmlString(text);
    expect(instanceOrError.error).toBeNull();
    const instance = instanceOrError.mustValue();

    const casted = AasTypes.asLangStringShortNameTypeIec61360(instance);
    if (casted === null) {
      throw new Error(
        `Expected instance of LangStringShortNameTypeIec61360 in ${pth}, ` +
        `but got: ${typeof instance}`
      );
    }

    TestCommon.assertNoVerificationErrors(AasVerification.verify(casted), pth);

    const roundTripText = AasXmlization.toXmlString(casted);
    expect(roundTripText.length).toBeGreaterThan(0);
  }
});

test("LangStringShortNameTypeIec61360 XML deserialization fail", () => {
  for (
    const causeDir of
    TestCommon.findImmediateSubdirectories(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Unexpected",
        "Unserializable"
      )
    )
  ) {
    const clsDir = path.join(
      causeDir,
      "langStringShortNameTypeIec61360"
    );
    if (!fs.existsSync(clsDir)) {
      continue;
    }

    const pths = Array.from(
      TestCommon.findFilesBySuffixRecursively(
        clsDir,
        ".xml"
      )
    );
    pths.sort();

    for (const pth of pths) {
      const text = fs.readFileSync(pth, "utf-8");
      const instanceOrError = AasXmlization.fromXmlString(text);
      expect(instanceOrError.error).not.toBeNull();
    }
  }
});

test("LangStringDefinitionTypeIec61360 XML round-trip OK", () => {
  const pths = Array.from(
    TestCommon.findFilesBySuffixRecursively(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Expected",
        "langStringDefinitionTypeIec61360"
      ),
      ".xml"
    )
  );
  pths.sort();

  for (const pth of pths) {
    const text = fs.readFileSync(pth, "utf-8");

    const instanceOrError = AasXmlization.fromXmlString(text);
    expect(instanceOrError.error).toBeNull();
    const instance = instanceOrError.mustValue();

    const casted = AasTypes.asLangStringDefinitionTypeIec61360(instance);
    if (casted === null) {
      throw new Error(
        `Expected instance of LangStringDefinitionTypeIec61360 in ${pth}, ` +
        `but got: ${typeof instance}`
      );
    }

    TestCommon.assertNoVerificationErrors(AasVerification.verify(casted), pth);

    const roundTripText = AasXmlization.toXmlString(casted);
    expect(roundTripText.length).toBeGreaterThan(0);
  }
});

test("LangStringDefinitionTypeIec61360 XML deserialization fail", () => {
  for (
    const causeDir of
    TestCommon.findImmediateSubdirectories(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Unexpected",
        "Unserializable"
      )
    )
  ) {
    const clsDir = path.join(
      causeDir,
      "langStringDefinitionTypeIec61360"
    );
    if (!fs.existsSync(clsDir)) {
      continue;
    }

    const pths = Array.from(
      TestCommon.findFilesBySuffixRecursively(
        clsDir,
        ".xml"
      )
    );
    pths.sort();

    for (const pth of pths) {
      const text = fs.readFileSync(pth, "utf-8");
      const instanceOrError = AasXmlization.fromXmlString(text);
      expect(instanceOrError.error).not.toBeNull();
    }
  }
});

test("DataSpecificationIec61360 XML round-trip OK", () => {
  const pths = Array.from(
    TestCommon.findFilesBySuffixRecursively(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Expected",
        "dataSpecificationIec61360"
      ),
      ".xml"
    )
  );
  pths.sort();

  for (const pth of pths) {
    const text = fs.readFileSync(pth, "utf-8");

    const instanceOrError = AasXmlization.fromXmlString(text);
    expect(instanceOrError.error).toBeNull();
    const instance = instanceOrError.mustValue();

    const casted = AasTypes.asDataSpecificationIec61360(instance);
    if (casted === null) {
      throw new Error(
        `Expected instance of DataSpecificationIec61360 in ${pth}, ` +
        `but got: ${typeof instance}`
      );
    }

    TestCommon.assertNoVerificationErrors(AasVerification.verify(casted), pth);

    const roundTripText = AasXmlization.toXmlString(casted);
    expect(roundTripText.length).toBeGreaterThan(0);
  }
});

test("DataSpecificationIec61360 XML deserialization fail", () => {
  for (
    const causeDir of
    TestCommon.findImmediateSubdirectories(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Unexpected",
        "Unserializable"
      )
    )
  ) {
    const clsDir = path.join(
      causeDir,
      "dataSpecificationIec61360"
    );
    if (!fs.existsSync(clsDir)) {
      continue;
    }

    const pths = Array.from(
      TestCommon.findFilesBySuffixRecursively(
        clsDir,
        ".xml"
      )
    );
    pths.sort();

    for (const pth of pths) {
      const text = fs.readFileSync(pth, "utf-8");
      const instanceOrError = AasXmlization.fromXmlString(text);
      expect(instanceOrError.error).not.toBeNull();
    }
  }
});

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.
