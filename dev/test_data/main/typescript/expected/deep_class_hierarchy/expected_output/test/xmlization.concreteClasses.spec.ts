/**
 * Test XML de/serialization of concrete classes.
 */

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

import * as fs from "fs";
import * as path from "path";

import * as AasXmlization from "../src/xmlization";
import * as AasTypes from "../src/types";
import * as AasVerification from "../src/verification";

import * as TestCommon from "./common";

test("Branch XML round-trip OK", () => {
  const pths = Array.from(
    TestCommon.findFilesBySuffixRecursively(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Expected",
        "branch"
      ),
      ".xml"
    )
  );
  pths.sort();

  for (const pth of pths) {
    const text = fs.readFileSync(pth, "utf-8");

    const instanceOrError = AasXmlization.fromXmlString(text);
    expect(instanceOrError.error).toBeNull();
    const instance = instanceOrError.mustValue();

    const casted = AasTypes.asBranch(instance);
    if (casted === null) {
      throw new Error(
        `Expected instance of Branch in ${pth}, ` +
        `but got: ${typeof instance}`
      );
    }

    TestCommon.assertNoVerificationErrors(AasVerification.verify(casted), pth);

    const roundTripText = AasXmlization.toXmlString(casted);
    expect(roundTripText.length).toBeGreaterThan(0);
  }
});

test("Branch XML deserialization fail", () => {
  for (
    const causeDir of
    TestCommon.findImmediateSubdirectories(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Unexpected",
        "Unserializable"
      )
    )
  ) {
    const clsDir = path.join(
      causeDir,
      "branch"
    );
    if (!fs.existsSync(clsDir)) {
      continue;
    }

    const pths = Array.from(
      TestCommon.findFilesBySuffixRecursively(
        clsDir,
        ".xml"
      )
    );
    pths.sort();

    for (const pth of pths) {
      const text = fs.readFileSync(pth, "utf-8");
      const instanceOrError = AasXmlization.fromXmlString(text);
      expect(instanceOrError.error).not.toBeNull();
    }
  }
});

test("Leaf XML round-trip OK", () => {
  const pths = Array.from(
    TestCommon.findFilesBySuffixRecursively(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Expected",
        "leaf"
      ),
      ".xml"
    )
  );
  pths.sort();

  for (const pth of pths) {
    const text = fs.readFileSync(pth, "utf-8");

    const instanceOrError = AasXmlization.fromXmlString(text);
    expect(instanceOrError.error).toBeNull();
    const instance = instanceOrError.mustValue();

    const casted = AasTypes.asLeaf(instance);
    if (casted === null) {
      throw new Error(
        `Expected instance of Leaf in ${pth}, ` +
        `but got: ${typeof instance}`
      );
    }

    TestCommon.assertNoVerificationErrors(AasVerification.verify(casted), pth);

    const roundTripText = AasXmlization.toXmlString(casted);
    expect(roundTripText.length).toBeGreaterThan(0);
  }
});

test("Leaf XML deserialization fail", () => {
  for (
    const causeDir of
    TestCommon.findImmediateSubdirectories(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Unexpected",
        "Unserializable"
      )
    )
  ) {
    const clsDir = path.join(
      causeDir,
      "leaf"
    );
    if (!fs.existsSync(clsDir)) {
      continue;
    }

    const pths = Array.from(
      TestCommon.findFilesBySuffixRecursively(
        clsDir,
        ".xml"
      )
    );
    pths.sort();

    for (const pth of pths) {
      const text = fs.readFileSync(pth, "utf-8");
      const instanceOrError = AasXmlization.fromXmlString(text);
      expect(instanceOrError.error).not.toBeNull();
    }
  }
});

test("Blossom XML round-trip OK", () => {
  const pths = Array.from(
    TestCommon.findFilesBySuffixRecursively(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Expected",
        "blossom"
      ),
      ".xml"
    )
  );
  pths.sort();

  for (const pth of pths) {
    const text = fs.readFileSync(pth, "utf-8");

    const instanceOrError = AasXmlization.fromXmlString(text);
    expect(instanceOrError.error).toBeNull();
    const instance = instanceOrError.mustValue();

    const casted = AasTypes.asBlossom(instance);
    if (casted === null) {
      throw new Error(
        `Expected instance of Blossom in ${pth}, ` +
        `but got: ${typeof instance}`
      );
    }

    TestCommon.assertNoVerificationErrors(AasVerification.verify(casted), pth);

    const roundTripText = AasXmlization.toXmlString(casted);
    expect(roundTripText.length).toBeGreaterThan(0);
  }
});

test("Blossom XML deserialization fail", () => {
  for (
    const causeDir of
    TestCommon.findImmediateSubdirectories(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Unexpected",
        "Unserializable"
      )
    )
  ) {
    const clsDir = path.join(
      causeDir,
      "blossom"
    );
    if (!fs.existsSync(clsDir)) {
      continue;
    }

    const pths = Array.from(
      TestCommon.findFilesBySuffixRecursively(
        clsDir,
        ".xml"
      )
    );
    pths.sort();

    for (const pth of pths) {
      const text = fs.readFileSync(pth, "utf-8");
      const instanceOrError = AasXmlization.fromXmlString(text);
      expect(instanceOrError.error).not.toBeNull();
    }
  }
});

test("Something XML round-trip OK", () => {
  const pths = Array.from(
    TestCommon.findFilesBySuffixRecursively(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Expected",
        "something"
      ),
      ".xml"
    )
  );
  pths.sort();

  for (const pth of pths) {
    const text = fs.readFileSync(pth, "utf-8");

    const instanceOrError = AasXmlization.fromXmlString(text);
    expect(instanceOrError.error).toBeNull();
    const instance = instanceOrError.mustValue();

    const casted = AasTypes.asSomething(instance);
    if (casted === null) {
      throw new Error(
        `Expected instance of Something in ${pth}, ` +
        `but got: ${typeof instance}`
      );
    }

    TestCommon.assertNoVerificationErrors(AasVerification.verify(casted), pth);

    const roundTripText = AasXmlization.toXmlString(casted);
    expect(roundTripText.length).toBeGreaterThan(0);
  }
});

test("Something XML deserialization fail", () => {
  for (
    const causeDir of
    TestCommon.findImmediateSubdirectories(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Unexpected",
        "Unserializable"
      )
    )
  ) {
    const clsDir = path.join(
      causeDir,
      "something"
    );
    if (!fs.existsSync(clsDir)) {
      continue;
    }

    const pths = Array.from(
      TestCommon.findFilesBySuffixRecursively(
        clsDir,
        ".xml"
      )
    );
    pths.sort();

    for (const pth of pths) {
      const text = fs.readFileSync(pth, "utf-8");
      const instanceOrError = AasXmlization.fromXmlString(text);
      expect(instanceOrError.error).not.toBeNull();
    }
  }
});

test("Container XML round-trip OK", () => {
  const pths = Array.from(
    TestCommon.findFilesBySuffixRecursively(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Expected",
        "container"
      ),
      ".xml"
    )
  );
  pths.sort();

  for (const pth of pths) {
    const text = fs.readFileSync(pth, "utf-8");

    const instanceOrError = AasXmlization.fromXmlString(text);
    expect(instanceOrError.error).toBeNull();
    const instance = instanceOrError.mustValue();

    const casted = AasTypes.asContainer(instance);
    if (casted === null) {
      throw new Error(
        `Expected instance of Container in ${pth}, ` +
        `but got: ${typeof instance}`
      );
    }

    TestCommon.assertNoVerificationErrors(AasVerification.verify(casted), pth);

    const roundTripText = AasXmlization.toXmlString(casted);
    expect(roundTripText.length).toBeGreaterThan(0);
  }
});

test("Container XML deserialization fail", () => {
  for (
    const causeDir of
    TestCommon.findImmediateSubdirectories(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Unexpected",
        "Unserializable"
      )
    )
  ) {
    const clsDir = path.join(
      causeDir,
      "container"
    );
    if (!fs.existsSync(clsDir)) {
      continue;
    }

    const pths = Array.from(
      TestCommon.findFilesBySuffixRecursively(
        clsDir,
        ".xml"
      )
    );
    pths.sort();

    for (const pth of pths) {
      const text = fs.readFileSync(pth, "utf-8");
      const instanceOrError = AasXmlization.fromXmlString(text);
      expect(instanceOrError.error).not.toBeNull();
    }
  }
});

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.
