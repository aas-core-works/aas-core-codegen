/**
 * Provide functions for loading generated examples of AAS instances.
 */

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

import * as path from "path";

import * as AasTypes from "../src/types";
import * as AasJsonization from "../src/jsonization";

import * as TestCommon from "./common";

/**
 * Load a maximal example of {@link types.Branch} from
 * the test data directory.
 */
export function loadMaximalBranch(
): AasTypes.Branch {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "Branch",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.branchFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asBranch(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Branch in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.Branch} from
 * the test data directory.
 */
export function loadMinimalBranch(
): AasTypes.Branch {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "Branch",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.branchFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asBranch(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Branch in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.Leaf} from
 * the test data directory.
 */
export function loadMaximalLeaf(
): AasTypes.Leaf {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "Leaf",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.leafFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asLeaf(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Leaf in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.Leaf} from
 * the test data directory.
 */
export function loadMinimalLeaf(
): AasTypes.Leaf {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "Leaf",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.leafFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asLeaf(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Leaf in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.Blossom} from
 * the test data directory.
 */
export function loadMaximalBlossom(
): AasTypes.Blossom {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "Blossom",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.blossomFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asBlossom(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Blossom in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.Blossom} from
 * the test data directory.
 */
export function loadMinimalBlossom(
): AasTypes.Blossom {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "Blossom",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.blossomFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asBlossom(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Blossom in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.Something} from
 * the test data directory.
 */
export function loadMaximalSomething(
): AasTypes.Something {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "Something",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.somethingFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asSomething(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Something in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.Something} from
 * the test data directory.
 */
export function loadMinimalSomething(
): AasTypes.Something {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "Something",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.somethingFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asSomething(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Something in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal example of {@link types.Container} from
 * the test data directory.
 */
export function loadMaximalContainer(
): AasTypes.Container {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "Container",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.containerFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asContainer(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Container in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.Container} from
 * the test data directory.
 */
export function loadMinimalContainer(
): AasTypes.Container {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "Container",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.containerFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asContainer(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Container in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.
