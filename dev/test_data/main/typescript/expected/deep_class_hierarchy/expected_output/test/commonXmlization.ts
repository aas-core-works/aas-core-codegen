/**
 * Provide functions for loading generated XML examples of AAS instances.
 */

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

import * as fs from "fs";
import * as path from "path";

import * as AasTypes from "../src/types";
import * as AasXmlization from "../src/xmlization";

import * as TestCommon from "./common";

/**
 * Load a maximal XML example of {@link types.Branch}
 * from the test data directory.
 */
export function loadMaximalBranch(
): AasTypes.Branch {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Xml",
    "Expected",
    "branch",
    "maximal.xml"
  );

  const text = fs.readFileSync(aPath, "utf-8");

  const instanceOrError = AasXmlization.fromXmlString(
    text
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asBranch(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Branch in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal XML example of {@link types.Branch}
 * from the test data directory.
 */
export function loadMinimalBranch(
): AasTypes.Branch {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Xml",
    "Expected",
    "branch",
    "minimal.xml"
  );

  const text = fs.readFileSync(aPath, "utf-8");

  const instanceOrError = AasXmlization.fromXmlString(
    text
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asBranch(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Branch in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal XML example of {@link types.Leaf}
 * from the test data directory.
 */
export function loadMaximalLeaf(
): AasTypes.Leaf {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Xml",
    "Expected",
    "leaf",
    "maximal.xml"
  );

  const text = fs.readFileSync(aPath, "utf-8");

  const instanceOrError = AasXmlization.fromXmlString(
    text
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asLeaf(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Leaf in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal XML example of {@link types.Leaf}
 * from the test data directory.
 */
export function loadMinimalLeaf(
): AasTypes.Leaf {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Xml",
    "Expected",
    "leaf",
    "minimal.xml"
  );

  const text = fs.readFileSync(aPath, "utf-8");

  const instanceOrError = AasXmlization.fromXmlString(
    text
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asLeaf(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Leaf in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal XML example of {@link types.Blossom}
 * from the test data directory.
 */
export function loadMaximalBlossom(
): AasTypes.Blossom {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Xml",
    "Expected",
    "blossom",
    "maximal.xml"
  );

  const text = fs.readFileSync(aPath, "utf-8");

  const instanceOrError = AasXmlization.fromXmlString(
    text
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asBlossom(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Blossom in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal XML example of {@link types.Blossom}
 * from the test data directory.
 */
export function loadMinimalBlossom(
): AasTypes.Blossom {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Xml",
    "Expected",
    "blossom",
    "minimal.xml"
  );

  const text = fs.readFileSync(aPath, "utf-8");

  const instanceOrError = AasXmlization.fromXmlString(
    text
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asBlossom(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Blossom in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal XML example of {@link types.Something}
 * from the test data directory.
 */
export function loadMaximalSomething(
): AasTypes.Something {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Xml",
    "Expected",
    "something",
    "maximal.xml"
  );

  const text = fs.readFileSync(aPath, "utf-8");

  const instanceOrError = AasXmlization.fromXmlString(
    text
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asSomething(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Something in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal XML example of {@link types.Something}
 * from the test data directory.
 */
export function loadMinimalSomething(
): AasTypes.Something {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Xml",
    "Expected",
    "something",
    "minimal.xml"
  );

  const text = fs.readFileSync(aPath, "utf-8");

  const instanceOrError = AasXmlization.fromXmlString(
    text
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asSomething(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Something in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a maximal XML example of {@link types.Container}
 * from the test data directory.
 */
export function loadMaximalContainer(
): AasTypes.Container {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Xml",
    "Expected",
    "container",
    "maximal.xml"
  );

  const text = fs.readFileSync(aPath, "utf-8");

  const instanceOrError = AasXmlization.fromXmlString(
    text
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asContainer(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Container in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal XML example of {@link types.Container}
 * from the test data directory.
 */
export function loadMinimalContainer(
): AasTypes.Container {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Xml",
    "Expected",
    "container",
    "minimal.xml"
  );

  const text = fs.readFileSync(aPath, "utf-8");

  const instanceOrError = AasXmlization.fromXmlString(
    text
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asContainer(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of Container in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.
