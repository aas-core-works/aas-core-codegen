// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

/**
 * Represent runtime model type of an instance.
 */
export enum ModelType {
  Branch = 0,
  Leaf = 1,
  Blossom = 2,
  Something = 3,
  Container = 4
}

/**
 * Iterate over the literals of {@link ModelType}.
 *
 * @remark
 * TypeScript does not provide an elegant way to iterate over the literals, so
 * this function helps you avoid common errors and pitfalls.
 *
 * @return iterator over the literals
 */
export function *overModelType (
): Iterable<ModelType> {
  // NOTE (mristin, 2022-12-03):
  // We yield numbers instead of literals to avoid name lookups on platforms
  // which do not provide JIT compilation of hot paths.
  yield <ModelType>0;  // Branch
  yield <ModelType>1;  // Leaf
  yield <ModelType>2;  // Blossom
  yield <ModelType>3;  // Something
  yield <ModelType>4;  // Container
}

/**
 * Represent the most general class of an AAS model.
 */
export abstract class Class {
  /**
   * Indicate the runtime model type of an instance.
   */
  abstract modelType(): ModelType;

  /**
   * Iterate over all the instances referenced from this one.
   */
  abstract descendOnce(): IterableIterator<Class>;

  /**
   * Iterate recursively over all the instances referenced from this one.
   */
  abstract descend(): IterableIterator<Class>;

  /**
   * Dispatch the `visitor` on this instance.
   *
   * @param visitor - to be dispatched
   */
  abstract accept(visitor: AbstractVisitor): void;

  /**
   * Dispatch the `visitor` on this instance with `context`.
   *
   * @param visitor - to be dispatched
   * @param context - of the visitation
   * @typeParam ContextT - type of the visitation context
   */
  abstract acceptWithContext<ContextT>(
    visitor: AbstractVisitorWithContext<ContextT>,
    context: ContextT
  ): void;

  /**
   * Dispatch the `transformer` on this instance.
   *
   * @param transformer - to be dispatched
   * @return this instance transformed
   * @typeParam T - type of the transformation result
   */
  abstract transform<T>(transformer: AbstractTransformer<T>): T;

  /**
   * Dispatch the `transformer` on this instance in `context`.
   *
   * @param transformer - to be dispatched
   * @param context - of the transformation
   * @return this instance transformed
   * @typeParam T - type of the transformation result
   */
  abstract transformWithContext<ContextT, T>(
    transformer: AbstractTransformerWithContext<ContextT, T>,
    context: ContextT
  ): T;
}

export interface INode extends Class {
  identifier: string;
}

export interface IBranch
  extends INode {
  description: string;
}

export class Branch
  extends Class
  implements IBranch {
  /**
   * Indicate the runtime model type of the instance.
   */
  modelType(): ModelType {
    // NOTE (mristin, 2022-12-03):
    // We yield numbers instead of literals to avoid name lookups on platforms
    // which do not provide JIT compilation of hot paths.
    return <ModelType>0;  // Branch
  }

  identifier: string;

  description: string;

  /**
   * Iterate over the instances referenced from this instance.
   *
   * We do not recurse into the referenced instances.
   *
   * @returns Iterator over the referenced instances
   */
  *descendOnce(): IterableIterator<Class> {
    // No descendable properties
  }

  /**
   * Iterate recursively over the instances referenced from this instance.
   *
   * @returns Iterator over the referenced instances
   */
  *descend(): IterableIterator<Class> {
    // No descendable properties
  }

  /**
   * Dispatch `visitor` on this instance.
   *
   * @param visitor - to visit this instance
   */
  accept(visitor: AbstractVisitor): void {
    visitor.visitBranch(this);
  }

  /**
   * Dispatch `visitor` with `context` on this instance.
   *
   * @param visitor - to visit this instance
   * @param context - to be passed along to the dispatched visitor method
   * @typeParam ContextT - type of the context
   */
  acceptWithContext<ContextT>(
    visitor: AbstractVisitorWithContext<ContextT>,
    context: ContextT
  ) {
    visitor.visitBranchWithContext(this, context);
  }

  /**
   * Dispatch the `transformer` on this instance.
   *
   * @param transformer - to transform this instance
   * @returns transformation of this instance
   * @paramType T - type of the transformation result
   */
  transform<T>(transformer: AbstractTransformer<T>): T {
    return transformer.transformBranch(this);
  }

  /**
   * Dispatch the `transformer` on this instance in `context`.
   *
   * @param transformer - to transform this instance
   * @param context - to be passed along to the `transformer`
   * @returns transformation of this instance
   * @paramType T - type of the transformation result
   * @paramType ContextT - type of the transformation context
   */
  transformWithContext<ContextT, T>(
    transformer: AbstractTransformerWithContext<ContextT, T>,
    context: ContextT
  ): T {
    return transformer.transformBranchWithContext(
      this, context
    );
  }

  constructor(
    identifier: string,
    description: string
  ) {
    super();
    this.identifier = identifier;
    this.description = description;
  }
}

export interface ILeaf
  extends IBranch {
  value: number;
}

export class Leaf
  extends Class
  implements ILeaf {
  /**
   * Indicate the runtime model type of the instance.
   */
  modelType(): ModelType {
    // NOTE (mristin, 2022-12-03):
    // We yield numbers instead of literals to avoid name lookups on platforms
    // which do not provide JIT compilation of hot paths.
    return <ModelType>1;  // Leaf
  }

  identifier: string;

  description: string;

  value: number;

  /**
   * Iterate over the instances referenced from this instance.
   *
   * We do not recurse into the referenced instances.
   *
   * @returns Iterator over the referenced instances
   */
  *descendOnce(): IterableIterator<Class> {
    // No descendable properties
  }

  /**
   * Iterate recursively over the instances referenced from this instance.
   *
   * @returns Iterator over the referenced instances
   */
  *descend(): IterableIterator<Class> {
    // No descendable properties
  }

  /**
   * Dispatch `visitor` on this instance.
   *
   * @param visitor - to visit this instance
   */
  accept(visitor: AbstractVisitor): void {
    visitor.visitLeaf(this);
  }

  /**
   * Dispatch `visitor` with `context` on this instance.
   *
   * @param visitor - to visit this instance
   * @param context - to be passed along to the dispatched visitor method
   * @typeParam ContextT - type of the context
   */
  acceptWithContext<ContextT>(
    visitor: AbstractVisitorWithContext<ContextT>,
    context: ContextT
  ) {
    visitor.visitLeafWithContext(this, context);
  }

  /**
   * Dispatch the `transformer` on this instance.
   *
   * @param transformer - to transform this instance
   * @returns transformation of this instance
   * @paramType T - type of the transformation result
   */
  transform<T>(transformer: AbstractTransformer<T>): T {
    return transformer.transformLeaf(this);
  }

  /**
   * Dispatch the `transformer` on this instance in `context`.
   *
   * @param transformer - to transform this instance
   * @param context - to be passed along to the `transformer`
   * @returns transformation of this instance
   * @paramType T - type of the transformation result
   * @paramType ContextT - type of the transformation context
   */
  transformWithContext<ContextT, T>(
    transformer: AbstractTransformerWithContext<ContextT, T>,
    context: ContextT
  ): T {
    return transformer.transformLeafWithContext(
      this, context
    );
  }

  constructor(
    identifier: string,
    description: string,
    value: number
  ) {
    super();
    this.identifier = identifier;
    this.description = description;
    this.value = value;
  }
}

export class Blossom
  extends Class
  implements ILeaf {
  /**
   * Indicate the runtime model type of the instance.
   */
  modelType(): ModelType {
    // NOTE (mristin, 2022-12-03):
    // We yield numbers instead of literals to avoid name lookups on platforms
    // which do not provide JIT compilation of hot paths.
    return <ModelType>2;  // Blossom
  }

  identifier: string;

  description: string;

  value: number;

  details: string;

  /**
   * Iterate over the instances referenced from this instance.
   *
   * We do not recurse into the referenced instances.
   *
   * @returns Iterator over the referenced instances
   */
  *descendOnce(): IterableIterator<Class> {
    // No descendable properties
  }

  /**
   * Iterate recursively over the instances referenced from this instance.
   *
   * @returns Iterator over the referenced instances
   */
  *descend(): IterableIterator<Class> {
    // No descendable properties
  }

  /**
   * Dispatch `visitor` on this instance.
   *
   * @param visitor - to visit this instance
   */
  accept(visitor: AbstractVisitor): void {
    visitor.visitBlossom(this);
  }

  /**
   * Dispatch `visitor` with `context` on this instance.
   *
   * @param visitor - to visit this instance
   * @param context - to be passed along to the dispatched visitor method
   * @typeParam ContextT - type of the context
   */
  acceptWithContext<ContextT>(
    visitor: AbstractVisitorWithContext<ContextT>,
    context: ContextT
  ) {
    visitor.visitBlossomWithContext(this, context);
  }

  /**
   * Dispatch the `transformer` on this instance.
   *
   * @param transformer - to transform this instance
   * @returns transformation of this instance
   * @paramType T - type of the transformation result
   */
  transform<T>(transformer: AbstractTransformer<T>): T {
    return transformer.transformBlossom(this);
  }

  /**
   * Dispatch the `transformer` on this instance in `context`.
   *
   * @param transformer - to transform this instance
   * @param context - to be passed along to the `transformer`
   * @returns transformation of this instance
   * @paramType T - type of the transformation result
   * @paramType ContextT - type of the transformation context
   */
  transformWithContext<ContextT, T>(
    transformer: AbstractTransformerWithContext<ContextT, T>,
    context: ContextT
  ): T {
    return transformer.transformBlossomWithContext(
      this, context
    );
  }

  constructor(
    identifier: string,
    description: string,
    value: number,
    details: string
  ) {
    super();
    this.identifier = identifier;
    this.description = description;
    this.value = value;
    this.details = details;
  }
}

export class Something extends Class {
  /**
   * Indicate the runtime model type of the instance.
   */
  modelType(): ModelType {
    // NOTE (mristin, 2022-12-03):
    // We yield numbers instead of literals to avoid name lookups on platforms
    // which do not provide JIT compilation of hot paths.
    return <ModelType>3;  // Something
  }

  someChoice: INode;

  somethingWithoutChoice: Branch;

  /**
   * Iterate over the instances referenced from this instance.
   *
   * We do not recurse into the referenced instances.
   *
   * @returns Iterator over the referenced instances
   */
  *descendOnce(): IterableIterator<Class> {
    yield this.someChoice;

    yield this.somethingWithoutChoice;
  }

  /**
   * Iterate recursively over the instances referenced from this instance.
   *
   * @returns Iterator over the referenced instances
   */
  *descend(): IterableIterator<Class> {
    yield this.someChoice;

    yield * this.someChoice.descend();

    yield this.somethingWithoutChoice;

    yield * this.somethingWithoutChoice.descend();
  }

  /**
   * Dispatch `visitor` on this instance.
   *
   * @param visitor - to visit this instance
   */
  accept(visitor: AbstractVisitor): void {
    visitor.visitSomething(this);
  }

  /**
   * Dispatch `visitor` with `context` on this instance.
   *
   * @param visitor - to visit this instance
   * @param context - to be passed along to the dispatched visitor method
   * @typeParam ContextT - type of the context
   */
  acceptWithContext<ContextT>(
    visitor: AbstractVisitorWithContext<ContextT>,
    context: ContextT
  ) {
    visitor.visitSomethingWithContext(this, context);
  }

  /**
   * Dispatch the `transformer` on this instance.
   *
   * @param transformer - to transform this instance
   * @returns transformation of this instance
   * @paramType T - type of the transformation result
   */
  transform<T>(transformer: AbstractTransformer<T>): T {
    return transformer.transformSomething(this);
  }

  /**
   * Dispatch the `transformer` on this instance in `context`.
   *
   * @param transformer - to transform this instance
   * @param context - to be passed along to the `transformer`
   * @returns transformation of this instance
   * @paramType T - type of the transformation result
   * @paramType ContextT - type of the transformation context
   */
  transformWithContext<ContextT, T>(
    transformer: AbstractTransformerWithContext<ContextT, T>,
    context: ContextT
  ): T {
    return transformer.transformSomethingWithContext(
      this, context
    );
  }

  constructor(
    someChoice: INode,
    somethingWithoutChoice: Branch
  ) {
    super();
    this.someChoice = someChoice;
    this.somethingWithoutChoice = somethingWithoutChoice;
  }
}

export class Container extends Class {
  /**
   * Indicate the runtime model type of the instance.
   */
  modelType(): ModelType {
    // NOTE (mristin, 2022-12-03):
    // We yield numbers instead of literals to avoid name lookups on platforms
    // which do not provide JIT compilation of hot paths.
    return <ModelType>4;  // Container
  }

  node: INode;

  something: Something;

  /**
   * Iterate over the instances referenced from this instance.
   *
   * We do not recurse into the referenced instances.
   *
   * @returns Iterator over the referenced instances
   */
  *descendOnce(): IterableIterator<Class> {
    yield this.node;

    yield this.something;
  }

  /**
   * Iterate recursively over the instances referenced from this instance.
   *
   * @returns Iterator over the referenced instances
   */
  *descend(): IterableIterator<Class> {
    yield this.node;

    yield * this.node.descend();

    yield this.something;

    yield * this.something.descend();
  }

  /**
   * Dispatch `visitor` on this instance.
   *
   * @param visitor - to visit this instance
   */
  accept(visitor: AbstractVisitor): void {
    visitor.visitContainer(this);
  }

  /**
   * Dispatch `visitor` with `context` on this instance.
   *
   * @param visitor - to visit this instance
   * @param context - to be passed along to the dispatched visitor method
   * @typeParam ContextT - type of the context
   */
  acceptWithContext<ContextT>(
    visitor: AbstractVisitorWithContext<ContextT>,
    context: ContextT
  ) {
    visitor.visitContainerWithContext(this, context);
  }

  /**
   * Dispatch the `transformer` on this instance.
   *
   * @param transformer - to transform this instance
   * @returns transformation of this instance
   * @paramType T - type of the transformation result
   */
  transform<T>(transformer: AbstractTransformer<T>): T {
    return transformer.transformContainer(this);
  }

  /**
   * Dispatch the `transformer` on this instance in `context`.
   *
   * @param transformer - to transform this instance
   * @param context - to be passed along to the `transformer`
   * @returns transformation of this instance
   * @paramType T - type of the transformation result
   * @paramType ContextT - type of the transformation context
   */
  transformWithContext<ContextT, T>(
    transformer: AbstractTransformerWithContext<ContextT, T>,
    context: ContextT
  ): T {
    return transformer.transformContainerWithContext(
      this, context
    );
  }

  constructor(
    node: INode,
    something: Something
  ) {
    super();
    this.node = node;
    this.something = something;
  }
}

/**
 * Visit the instances of the model.
 */
export abstract class AbstractVisitor {
  /**
   * Double-dispatch on `that`.
   */
  visit(that: Class): void {
    that.accept(this);
  }

  /**
   * Visit `that`.
   *
   * @param that - instance to be visited
   */
  abstract visitBranch(
    that: Branch
  ): void;

  /**
   * Visit `that`.
   *
   * @param that - instance to be visited
   */
  abstract visitLeaf(
    that: Leaf
  ): void;

  /**
   * Visit `that`.
   *
   * @param that - instance to be visited
   */
  abstract visitBlossom(
    that: Blossom
  ): void;

  /**
   * Visit `that`.
   *
   * @param that - instance to be visited
   */
  abstract visitSomething(
    that: Something
  ): void;

  /**
   * Visit `that`.
   *
   * @param that - instance to be visited
   */
  abstract visitContainer(
    that: Container
  ): void;
}

/**
 * Visit the instances of the model with context.
 *
 * @typeParam ContextT - type of the visitation context
 */
export abstract class AbstractVisitorWithContext<ContextT> {
  /**
   * Double-dispatch on `that` in `context`.
   *
   * @param that - instance to be visited
   * @param context - of the visitation
   */
  visitWithContext(
    that: Class,
    context: ContextT
  ): void {
    that.acceptWithContext(this, context);
  }

  /**
   * Visit `that` in `context`.
   *
   * @param that - instance to be visited
   * @param context - of the visitation
   */
  abstract visitBranchWithContext(
    that: Branch,
    context: ContextT
  ): void;

  /**
   * Visit `that` in `context`.
   *
   * @param that - instance to be visited
   * @param context - of the visitation
   */
  abstract visitLeafWithContext(
    that: Leaf,
    context: ContextT
  ): void;

  /**
   * Visit `that` in `context`.
   *
   * @param that - instance to be visited
   * @param context - of the visitation
   */
  abstract visitBlossomWithContext(
    that: Blossom,
    context: ContextT
  ): void;

  /**
   * Visit `that` in `context`.
   *
   * @param that - instance to be visited
   * @param context - of the visitation
   */
  abstract visitSomethingWithContext(
    that: Something,
    context: ContextT
  ): void;

  /**
   * Visit `that` in `context`.
   *
   * @param that - instance to be visited
   * @param context - of the visitation
   */
  abstract visitContainerWithContext(
    that: Container,
    context: ContextT
  ): void;
}

/**
 * Visit the instances of the model without action.
 *
 * @remarks
 * This visitor is not meant to be directly used. Instead, you usually
 * inherit from it, and implement only the relevant visit methods.
 */
export class PassThroughVisitor extends AbstractVisitor {
  /**
   * Visit `that`.
   *
   * @param that - instance to be visited
   */
  visitBranch(
    that: Branch
  ): void {
    for (const another of that.descendOnce()) {
      this.visit(another);
    }
  }

  /**
   * Visit `that`.
   *
   * @param that - instance to be visited
   */
  visitLeaf(
    that: Leaf
  ): void {
    for (const another of that.descendOnce()) {
      this.visit(another);
    }
  }

  /**
   * Visit `that`.
   *
   * @param that - instance to be visited
   */
  visitBlossom(
    that: Blossom
  ): void {
    for (const another of that.descendOnce()) {
      this.visit(another);
    }
  }

  /**
   * Visit `that`.
   *
   * @param that - instance to be visited
   */
  visitSomething(
    that: Something
  ): void {
    for (const another of that.descendOnce()) {
      this.visit(another);
    }
  }

  /**
   * Visit `that`.
   *
   * @param that - instance to be visited
   */
  visitContainer(
    that: Container
  ): void {
    for (const another of that.descendOnce()) {
      this.visit(another);
    }
  }
}

/**
 * Visit the instances of the model without action and in context.
 *
 * @remarks
 * This visitor is not meant to be directly used. Instead, you usually
 * inherit from it, and implement only the relevant visit methods.
 */
export class PassThroughVisitorWithContext<ContextT>
    extends AbstractVisitorWithContext<ContextT> {
  /**
   * Double-dispatch on `that` in `context`.
   */
  visitWithContext(
    that: Class,
    context: ContextT
  ): void {
    that.acceptWithContext(this, context);
  }

  /**
   * Visit `that` in `context`.
   *
   * @param that - instance to be visited
   * @param context - of the visitation
   */
  visitBranchWithContext(
    that: Branch,
    context: ContextT
  ): void {
    for (const another of that.descendOnce()) {
      this.visitWithContext(another, context);
    }
  }

  /**
   * Visit `that` in `context`.
   *
   * @param that - instance to be visited
   * @param context - of the visitation
   */
  visitLeafWithContext(
    that: Leaf,
    context: ContextT
  ): void {
    for (const another of that.descendOnce()) {
      this.visitWithContext(another, context);
    }
  }

  /**
   * Visit `that` in `context`.
   *
   * @param that - instance to be visited
   * @param context - of the visitation
   */
  visitBlossomWithContext(
    that: Blossom,
    context: ContextT
  ): void {
    for (const another of that.descendOnce()) {
      this.visitWithContext(another, context);
    }
  }

  /**
   * Visit `that` in `context`.
   *
   * @param that - instance to be visited
   * @param context - of the visitation
   */
  visitSomethingWithContext(
    that: Something,
    context: ContextT
  ): void {
    for (const another of that.descendOnce()) {
      this.visitWithContext(another, context);
    }
  }

  /**
   * Visit `that` in `context`.
   *
   * @param that - instance to be visited
   * @param context - of the visitation
   */
  visitContainerWithContext(
    that: Container,
    context: ContextT
  ): void {
    for (const another of that.descendOnce()) {
      this.visitWithContext(another, context);
    }
  }
}

/**
 * Transform the instance of the model.
 *
 * @typeParam T - type of the transformation result
 */
export abstract class AbstractTransformer<T> {
  /**
   * Double-dispatch on `that`.
   */
  transform(that: Class): T {
    return that.transform(this);
  }

  /**
   * Transform `that`.
   *
   * @param that - instance to be transformed
   * @returns transformed `that`
   */
  abstract transformBranch(
    that: Branch
  ): T;

  /**
   * Transform `that`.
   *
   * @param that - instance to be transformed
   * @returns transformed `that`
   */
  abstract transformLeaf(
    that: Leaf
  ): T;

  /**
   * Transform `that`.
   *
   * @param that - instance to be transformed
   * @returns transformed `that`
   */
  abstract transformBlossom(
    that: Blossom
  ): T;

  /**
   * Transform `that`.
   *
   * @param that - instance to be transformed
   * @returns transformed `that`
   */
  abstract transformSomething(
    that: Something
  ): T;

  /**
   * Transform `that`.
   *
   * @param that - instance to be transformed
   * @returns transformed `that`
   */
  abstract transformContainer(
    that: Container
  ): T;
}

/**
 * Transform the instances of the model in context.
 *
 * @typeParam ContextT - type of the transformation context
 * @typeParam T - type of the transformation result
 */
export abstract class AbstractTransformerWithContext<ContextT, T> {
  /**
   * Double-dispatch on `that` in `context`.
   *
   * @param that - instance to be transformed
   * @param context - of the transformation
   * @returns transformed `that`
   */
  transformWithContext(
    that: Class,
    context: ContextT
  ): T {
    return that.transformWithContext(this, context);
  }

  /**
   * Transform `that` in `context`.
   *
   * @param that - instance to be transformed
   * @param context - of the transformation
   * @returns transformed `that`
   */
  abstract transformBranchWithContext(
    that: Branch,
    context: ContextT
  ): T;

  /**
   * Transform `that` in `context`.
   *
   * @param that - instance to be transformed
   * @param context - of the transformation
   * @returns transformed `that`
   */
  abstract transformLeafWithContext(
    that: Leaf,
    context: ContextT
  ): T;

  /**
   * Transform `that` in `context`.
   *
   * @param that - instance to be transformed
   * @param context - of the transformation
   * @returns transformed `that`
   */
  abstract transformBlossomWithContext(
    that: Blossom,
    context: ContextT
  ): T;

  /**
   * Transform `that` in `context`.
   *
   * @param that - instance to be transformed
   * @param context - of the transformation
   * @returns transformed `that`
   */
  abstract transformSomethingWithContext(
    that: Something,
    context: ContextT
  ): T;

  /**
   * Transform `that` in `context`.
   *
   * @param that - instance to be transformed
   * @param context - of the transformation
   * @returns transformed `that`
   */
  abstract transformContainerWithContext(
    that: Container,
    context: ContextT
  ): T;
}

/**
 * Transform the instances of the model.
 *
 * @remarks
 * If you do not override the transformation methods, they simply
 * return {@link defaultResult}.
 *
 * @typeParam T - type of the transformation result
 */
export class TransformerWithDefault<T> extends AbstractTransformer<T> {
  /**
   * Default value which is returned if no override of the transformation
   */
  defaultResult: T

  /**
   * Initialize with the given `default` value.
   *
   * @param defaultResult - returned if no override of the transformation
   */
  constructor(defaultResult: T) {
    super();
    this.defaultResult = defaultResult;
  }

  /**
   * Transform `that`.
   *
   * @param that - instance to be transformed
   * @returns transformed `that`
   */
  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformBranch(
    that: Branch
  ): T {
    return this.defaultResult;
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */

  /**
   * Transform `that`.
   *
   * @param that - instance to be transformed
   * @returns transformed `that`
   */
  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformLeaf(
    that: Leaf
  ): T {
    return this.defaultResult;
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */

  /**
   * Transform `that`.
   *
   * @param that - instance to be transformed
   * @returns transformed `that`
   */
  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformBlossom(
    that: Blossom
  ): T {
    return this.defaultResult;
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */

  /**
   * Transform `that`.
   *
   * @param that - instance to be transformed
   * @returns transformed `that`
   */
  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformSomething(
    that: Something
  ): T {
    return this.defaultResult;
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */

  /**
   * Transform `that`.
   *
   * @param that - instance to be transformed
   * @returns transformed `that`
   */
  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformContainer(
    that: Container
  ): T {
    return this.defaultResult;
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */
}

/**
 * Transform the instances of the model in context.
 *
 * @remarks
 * If you do not override the transformation methods, they simply
 * return {@link defaultResult}.
 *
 * @typeParam ContextT - type of the visitation context
 * @typeParam T - type of the transformation result
 */
export class TransformerWithDefaultAndContext<ContextT, T>
    extends AbstractTransformerWithContext<ContextT, T> {
  /**
   * Default value which is returned if no override of the transformation
   */
  defaultResult: T

  /**
   * Initialize with the given `default` value.
   *
   * @param defaultResult - returned if no override of the transformation
   */
  constructor(defaultResult: T) {
    super();
    this.defaultResult = defaultResult;
  }

  /**
   * Transform `that` in `context`.
   *
   * @param that - instance to be transformed
   * @param context - of the visitation
   * @returns transformed `that`
   */
  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformBranchWithContext(
    that: Branch,
    context: ContextT
  ): T {
    return this.defaultResult;
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */

  /**
   * Transform `that` in `context`.
   *
   * @param that - instance to be transformed
   * @param context - of the visitation
   * @returns transformed `that`
   */
  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformLeafWithContext(
    that: Leaf,
    context: ContextT
  ): T {
    return this.defaultResult;
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */

  /**
   * Transform `that` in `context`.
   *
   * @param that - instance to be transformed
   * @param context - of the visitation
   * @returns transformed `that`
   */
  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformBlossomWithContext(
    that: Blossom,
    context: ContextT
  ): T {
    return this.defaultResult;
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */

  /**
   * Transform `that` in `context`.
   *
   * @param that - instance to be transformed
   * @param context - of the visitation
   * @returns transformed `that`
   */
  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformSomethingWithContext(
    that: Something,
    context: ContextT
  ): T {
    return this.defaultResult;
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */

  /**
   * Transform `that` in `context`.
   *
   * @param that - instance to be transformed
   * @param context - of the visitation
   * @returns transformed `that`
   */
  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformContainerWithContext(
    that: Container,
    context: ContextT
  ): T {
    return this.defaultResult;
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */
}

/**
 * Try to cast an instance of the model to {@link INode}.
 */
class AsNodeTransformer
    extends AbstractTransformer<INode | null> {
  transformBranch(
    that: Branch
  ): INode | null {
    return that as INode;
  }

  transformLeaf(
    that: Leaf
  ): INode | null {
    return that as INode;
  }

  transformBlossom(
    that: Blossom
  ): INode | null {
    return that as INode;
  }

  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformSomething(
    that: Something
  ): INode | null {
    return null;
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */

  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformContainer(
    that: Container
  ): INode | null {
    return null;
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */
}

const AS_NODE_TRANSFORMER =
  new AsNodeTransformer();

/**
 * Try to cast `that` instance to
 * the interface {@link INode}.
 *
 * @param that - instance to be casted
 * @returns - casted `that` if cast successful, or `null`
 */
export function asNode(
  that: Class
): INode | null {
  return AS_NODE_TRANSFORMER.transform(that);
}

/**
 * Check the type of `that` instance.
 *
 * @param that - instance to be type-checked
 * @returns `true` if the type check is successful
 */
export function isNode(
  that: Class
): that is INode {
  return asNode(that) !== null;
}

/**
 * Try to cast an instance of the model to {@link IBranch}.
 */
class AsBranchTransformer
    extends AbstractTransformer<IBranch | null> {
  transformBranch(
    that: Branch
  ): IBranch | null {
    return that as IBranch;
  }

  transformLeaf(
    that: Leaf
  ): IBranch | null {
    return that as IBranch;
  }

  transformBlossom(
    that: Blossom
  ): IBranch | null {
    return that as IBranch;
  }

  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformSomething(
    that: Something
  ): IBranch | null {
    return null;
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */

  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformContainer(
    that: Container
  ): IBranch | null {
    return null;
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */
}

const AS_BRANCH_TRANSFORMER =
  new AsBranchTransformer();

/**
 * Try to cast `that` instance to
 * the interface {@link IBranch}.
 *
 * @param that - instance to be casted
 * @returns - casted `that` if cast successful, or `null`
 */
export function asBranch(
  that: Class
): IBranch | null {
  return AS_BRANCH_TRANSFORMER.transform(that);
}

/**
 * Check the type of `that` instance.
 *
 * @param that - instance to be type-checked
 * @returns `true` if the type check is successful
 */
export function isBranch(
  that: Class
): that is IBranch {
  return asBranch(that) !== null;
}

/**
 * Try to cast an instance of the model to {@link ILeaf}.
 */
class AsLeafTransformer
    extends AbstractTransformer<ILeaf | null> {
  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformBranch(
    that: Branch
  ): ILeaf | null {
    return null;
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */

  transformLeaf(
    that: Leaf
  ): ILeaf | null {
    return that as ILeaf;
  }

  transformBlossom(
    that: Blossom
  ): ILeaf | null {
    return that as ILeaf;
  }

  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformSomething(
    that: Something
  ): ILeaf | null {
    return null;
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */

  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformContainer(
    that: Container
  ): ILeaf | null {
    return null;
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */
}

const AS_LEAF_TRANSFORMER =
  new AsLeafTransformer();

/**
 * Try to cast `that` instance to
 * the interface {@link ILeaf}.
 *
 * @param that - instance to be casted
 * @returns - casted `that` if cast successful, or `null`
 */
export function asLeaf(
  that: Class
): ILeaf | null {
  return AS_LEAF_TRANSFORMER.transform(that);
}

/**
 * Check the type of `that` instance.
 *
 * @param that - instance to be type-checked
 * @returns `true` if the type check is successful
 */
export function isLeaf(
  that: Class
): that is ILeaf {
  return asLeaf(that) !== null;
}

/**
 * Try to cast `that` instance to
 * the class {@link Blossom}.
 *
 * @param that - instance to be casted
 * @returns - casted `that` if cast successful, or `null`
 */
export function asBlossom(
  that: Class
): Blossom | null {
  return (that instanceof Blossom)
    ? <Blossom>that
    : null;
}

/**
 * Check the type of `that` instance.
 *
 * @param that - instance to be type-checked
 * @returns `true` if the type check is successful
 */
export function isBlossom(
  that: Class
): that is Blossom {
  return that instanceof Blossom;
}

/**
 * Try to cast `that` instance to
 * the class {@link Something}.
 *
 * @param that - instance to be casted
 * @returns - casted `that` if cast successful, or `null`
 */
export function asSomething(
  that: Class
): Something | null {
  return (that instanceof Something)
    ? <Something>that
    : null;
}

/**
 * Check the type of `that` instance.
 *
 * @param that - instance to be type-checked
 * @returns `true` if the type check is successful
 */
export function isSomething(
  that: Class
): that is Something {
  return that instanceof Something;
}

/**
 * Try to cast `that` instance to
 * the class {@link Container}.
 *
 * @param that - instance to be casted
 * @returns - casted `that` if cast successful, or `null`
 */
export function asContainer(
  that: Class
): Container | null {
  return (that instanceof Container)
    ? <Container>that
    : null;
}

/**
 * Check the type of `that` instance.
 *
 * @param that - instance to be type-checked
 * @returns `true` if the type check is successful
 */
export function isContainer(
  that: Class
): that is Container {
  return that instanceof Container;
}

class TypeMatcher extends AbstractTransformerWithContext<
  Readonly<Class>,
  boolean
> {
  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformBranchWithContext(
    that: Branch,
    other: Class
  ): boolean {
    return isBranch(other);
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */

  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformLeafWithContext(
    that: Leaf,
    other: Class
  ): boolean {
    return isLeaf(other);
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */

  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformBlossomWithContext(
    that: Blossom,
    other: Class
  ): boolean {
    return isBlossom(other);
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */

  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformSomethingWithContext(
    that: Something,
    other: Class
  ): boolean {
    return isSomething(other);
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */

  /* eslint-disable @typescript-eslint/no-unused-vars */
  transformContainerWithContext(
    that: Container,
    other: Class
  ): boolean {
    return isContainer(other);
  }
  /* eslint-enable @typescript-eslint/no-unused-vars */
}

const TYPE_MATCHER = new TypeMatcher();

/**
 * Check whether the type of `that` matches the type of `other` instance.
 *
 * @remarks
 * We check with `is*` function. Hence, if the class of `other` is a subclass of
 * the class of `that`, we confirm the match.
 *
 * @param that - standard instance
 * @param other - instance whose type is compared against `that`
 */
export function typesMatch<ClassT extends Class>(
  that: ClassT,
  other: Class
): other is ClassT {
  return TYPE_MATCHER.transformWithContext(that, other);
}

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.
