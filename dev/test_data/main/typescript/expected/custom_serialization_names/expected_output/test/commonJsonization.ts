/**
 * Provide functions for loading generated examples of AAS instances.
 */

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

import * as path from "path";

import * as AasTypes from "../src/types";
import * as AasJsonization from "../src/jsonization";

import * as TestCommon from "./common";

/**
 * Load a maximal example of {@link types.QueryCondition} from
 * the test data directory.
 */
export function loadMaximalQueryCondition(
): AasTypes.QueryCondition {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "QueryCondition",
    "maximal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.queryConditionFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asQueryCondition(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of QueryCondition in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

/**
 * Load a minimal example of {@link types.QueryCondition} from
 * the test data directory.
 */
export function loadMinimalQueryCondition(
): AasTypes.QueryCondition {
  const aPath = path.join(
    TestCommon.TEST_DATA_DIR,
    "Json",
    "Expected",
    "QueryCondition",
    "minimal.json"
  );

  const jsonable = TestCommon.readJsonFromFileSync(aPath);

  const instanceOrError = AasJsonization.queryConditionFromJsonable(
    jsonable
  );
  expect(instanceOrError.error).toBeNull();
  const instance = instanceOrError.mustValue();

  const casted = AasTypes.asQueryCondition(instance);
  if (casted === null) {
    throw new Error(
      `Expected instance of QueryCondition in ${aPath}, ` +
      `but got: ${typeof instance}`
    );
  }
  return casted;
}

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.
