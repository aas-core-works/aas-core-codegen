/**
 * Test XML de/serialization of concrete classes.
 */

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

import * as fs from "fs";
import * as path from "path";

import * as AasXmlization from "../src/xmlization";
import * as AasTypes from "../src/types";
import * as AasVerification from "../src/verification";

import * as TestCommon from "./common";

test("SomeItem XML round-trip OK", () => {
  const pths = Array.from(
    TestCommon.findFilesBySuffixRecursively(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Expected",
        "someItem"
      ),
      ".xml"
    )
  );
  pths.sort();

  for (const pth of pths) {
    const text = fs.readFileSync(pth, "utf-8");

    const instanceOrError = AasXmlization.fromXmlString(text);
    expect(instanceOrError.error).toBeNull();
    const instance = instanceOrError.mustValue();

    const casted = AasTypes.asSomeItem(instance);
    if (casted === null) {
      throw new Error(
        `Expected instance of SomeItem in ${pth}, ` +
        `but got: ${typeof instance}`
      );
    }

    TestCommon.assertNoVerificationErrors(AasVerification.verify(casted), pth);

    const roundTripText = AasXmlization.toXmlString(casted);
    expect(roundTripText.length).toBeGreaterThan(0);
  }
});

test("SomeItem XML deserialization fail", () => {
  for (
    const causeDir of
    TestCommon.findImmediateSubdirectories(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Unexpected",
        "Unserializable"
      )
    )
  ) {
    const clsDir = path.join(
      causeDir,
      "someItem"
    );
    if (!fs.existsSync(clsDir)) {
      continue;
    }

    const pths = Array.from(
      TestCommon.findFilesBySuffixRecursively(
        clsDir,
        ".xml"
      )
    );
    pths.sort();

    for (const pth of pths) {
      const text = fs.readFileSync(pth, "utf-8");
      const instanceOrError = AasXmlization.fromXmlString(text);
      expect(instanceOrError.error).not.toBeNull();
    }
  }
});

test("AnotherItem XML round-trip OK", () => {
  const pths = Array.from(
    TestCommon.findFilesBySuffixRecursively(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Expected",
        "anotherItem"
      ),
      ".xml"
    )
  );
  pths.sort();

  for (const pth of pths) {
    const text = fs.readFileSync(pth, "utf-8");

    const instanceOrError = AasXmlization.fromXmlString(text);
    expect(instanceOrError.error).toBeNull();
    const instance = instanceOrError.mustValue();

    const casted = AasTypes.asAnotherItem(instance);
    if (casted === null) {
      throw new Error(
        `Expected instance of AnotherItem in ${pth}, ` +
        `but got: ${typeof instance}`
      );
    }

    TestCommon.assertNoVerificationErrors(AasVerification.verify(casted), pth);

    const roundTripText = AasXmlization.toXmlString(casted);
    expect(roundTripText.length).toBeGreaterThan(0);
  }
});

test("AnotherItem XML deserialization fail", () => {
  for (
    const causeDir of
    TestCommon.findImmediateSubdirectories(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Unexpected",
        "Unserializable"
      )
    )
  ) {
    const clsDir = path.join(
      causeDir,
      "anotherItem"
    );
    if (!fs.existsSync(clsDir)) {
      continue;
    }

    const pths = Array.from(
      TestCommon.findFilesBySuffixRecursively(
        clsDir,
        ".xml"
      )
    );
    pths.sort();

    for (const pth of pths) {
      const text = fs.readFileSync(pth, "utf-8");
      const instanceOrError = AasXmlization.fromXmlString(text);
      expect(instanceOrError.error).not.toBeNull();
    }
  }
});

test("Simple XML round-trip OK", () => {
  const pths = Array.from(
    TestCommon.findFilesBySuffixRecursively(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Expected",
        "simple"
      ),
      ".xml"
    )
  );
  pths.sort();

  for (const pth of pths) {
    const text = fs.readFileSync(pth, "utf-8");

    const instanceOrError = AasXmlization.fromXmlString(text);
    expect(instanceOrError.error).toBeNull();
    const instance = instanceOrError.mustValue();

    const casted = AasTypes.asSimple(instance);
    if (casted === null) {
      throw new Error(
        `Expected instance of Simple in ${pth}, ` +
        `but got: ${typeof instance}`
      );
    }

    TestCommon.assertNoVerificationErrors(AasVerification.verify(casted), pth);

    const roundTripText = AasXmlization.toXmlString(casted);
    expect(roundTripText.length).toBeGreaterThan(0);
  }
});

test("Simple XML deserialization fail", () => {
  for (
    const causeDir of
    TestCommon.findImmediateSubdirectories(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Unexpected",
        "Unserializable"
      )
    )
  ) {
    const clsDir = path.join(
      causeDir,
      "simple"
    );
    if (!fs.existsSync(clsDir)) {
      continue;
    }

    const pths = Array.from(
      TestCommon.findFilesBySuffixRecursively(
        clsDir,
        ".xml"
      )
    );
    pths.sort();

    for (const pth of pths) {
      const text = fs.readFileSync(pth, "utf-8");
      const instanceOrError = AasXmlization.fromXmlString(text);
      expect(instanceOrError.error).not.toBeNull();
    }
  }
});

test("Something XML round-trip OK", () => {
  const pths = Array.from(
    TestCommon.findFilesBySuffixRecursively(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Expected",
        "something"
      ),
      ".xml"
    )
  );
  pths.sort();

  for (const pth of pths) {
    const text = fs.readFileSync(pth, "utf-8");

    const instanceOrError = AasXmlization.fromXmlString(text);
    expect(instanceOrError.error).toBeNull();
    const instance = instanceOrError.mustValue();

    const casted = AasTypes.asSomething(instance);
    if (casted === null) {
      throw new Error(
        `Expected instance of Something in ${pth}, ` +
        `but got: ${typeof instance}`
      );
    }

    TestCommon.assertNoVerificationErrors(AasVerification.verify(casted), pth);

    const roundTripText = AasXmlization.toXmlString(casted);
    expect(roundTripText.length).toBeGreaterThan(0);
  }
});

test("Something XML deserialization fail", () => {
  for (
    const causeDir of
    TestCommon.findImmediateSubdirectories(
      path.join(
        TestCommon.TEST_DATA_DIR,
        "Xml",
        "Unexpected",
        "Unserializable"
      )
    )
  ) {
    const clsDir = path.join(
      causeDir,
      "something"
    );
    if (!fs.existsSync(clsDir)) {
      continue;
    }

    const pths = Array.from(
      TestCommon.findFilesBySuffixRecursively(
        clsDir,
        ".xml"
      )
    );
    pths.sort();

    for (const pth of pths) {
      const text = fs.readFileSync(pth, "utf-8");
      const instanceOrError = AasXmlization.fromXmlString(text);
      expect(instanceOrError.error).not.toBeNull();
    }
  }
});

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.
