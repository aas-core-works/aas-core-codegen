#ifndef DUMMY_ENHANCING_GUARD_
#define DUMMY_ENHANCING_GUARD_

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

#include "dummy/common.hpp"
#include "dummy/stringification.hpp"
#include "dummy/types.hpp"

#pragma warning(push, 0)
#include <sstream>
#include <stdexcept>
#pragma warning(pop)

namespace dummy {

/**
 * \defgroup enhancing Enhance instances of the model with your custom enhancements.
 * @{
 */
namespace enhancing {

// region Forward declarations

template <typename E>
std::shared_ptr<
  types::IAbstractItem
> Wrap(
  const std::shared_ptr<
    types::IAbstractItem
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
);

template <typename E>
std::shared_ptr<
  types::ISomeItem
> Wrap(
  const std::shared_ptr<
    types::ISomeItem
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
);

template <typename E>
std::shared_ptr<
  types::IAnotherItem
> Wrap(
  const std::shared_ptr<
    types::IAnotherItem
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
);

template <typename E>
std::shared_ptr<
  types::ISimple
> Wrap(
  const std::shared_ptr<
    types::ISimple
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
);

template <typename E>
std::shared_ptr<
  types::ISomething
> Wrap(
  const std::shared_ptr<
    types::ISomething
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
);

// endregion Forward declarations

/// \cond HIDDEN
namespace impl {

template<typename E>
class IEnhanced {
 public:
  virtual const std::shared_ptr<E>& enhancement() const = 0;

  virtual std::shared_ptr<E>& mutable_enhancement() = 0;

  virtual void set_enhancement(
    std::shared_ptr<E> value
  ) = 0;

  virtual ~IEnhanced() = default;
};

template<class E>
class EnhancedSomeItem
    : virtual public types::ISomeItem,
    virtual public IEnhanced<E> {
 public:
  types::ModelType model_type() const override {
    return types::ModelType::kSomeItem;
  }

  const std::wstring& name() const override {
    return instance_->name();
  }

  std::wstring& mutable_name() override {
    return instance_->mutable_name();
  }

  void set_name(
    std::wstring value
  ) override {
    instance_->set_name(value);
  }

  const std::shared_ptr<E>& enhancement() const {
    return enhancement_;
  }

  std::shared_ptr<E>& mutable_enhancement() {
    return enhancement_;
  }

  void set_enhancement(
    std::shared_ptr<E> value
  ) {
    enhancement_ = std::move(value);
  }

  EnhancedSomeItem(
    std::shared_ptr<types::ISomeItem> instance,
    std::shared_ptr<E> enhancement
  ) :
    instance_(instance),
    enhancement_(enhancement) {
    // Intentionally empty.
  }

  virtual ~EnhancedSomeItem() = default;

 private:
  std::shared_ptr<types::ISomeItem> instance_;
  std::shared_ptr<E> enhancement_;
};

template<class E>
class EnhancedAnotherItem
    : virtual public types::IAnotherItem,
    virtual public IEnhanced<E> {
 public:
  types::ModelType model_type() const override {
    return types::ModelType::kAnotherItem;
  }

  int64_t serial_number() const override {
    return instance_->serial_number();
  }

  int64_t& mutable_serial_number() override {
    return instance_->mutable_serial_number();
  }

  void set_serial_number(
    int64_t value
  ) override {
    instance_->set_serial_number(value);
  }

  const std::shared_ptr<E>& enhancement() const {
    return enhancement_;
  }

  std::shared_ptr<E>& mutable_enhancement() {
    return enhancement_;
  }

  void set_enhancement(
    std::shared_ptr<E> value
  ) {
    enhancement_ = std::move(value);
  }

  EnhancedAnotherItem(
    std::shared_ptr<types::IAnotherItem> instance,
    std::shared_ptr<E> enhancement
  ) :
    instance_(instance),
    enhancement_(enhancement) {
    // Intentionally empty.
  }

  virtual ~EnhancedAnotherItem() = default;

 private:
  std::shared_ptr<types::IAnotherItem> instance_;
  std::shared_ptr<E> enhancement_;
};

template<class E>
class EnhancedSimple
    : virtual public types::ISimple,
    virtual public IEnhanced<E> {
 public:
  types::ModelType model_type() const override {
    return types::ModelType::kSimple;
  }

  const std::wstring& name() const override {
    return instance_->name();
  }

  std::wstring& mutable_name() override {
    return instance_->mutable_name();
  }

  void set_name(
    std::wstring value
  ) override {
    instance_->set_name(value);
  }

  const std::shared_ptr<E>& enhancement() const {
    return enhancement_;
  }

  std::shared_ptr<E>& mutable_enhancement() {
    return enhancement_;
  }

  void set_enhancement(
    std::shared_ptr<E> value
  ) {
    enhancement_ = std::move(value);
  }

  EnhancedSimple(
    std::shared_ptr<types::ISimple> instance,
    std::shared_ptr<E> enhancement
  ) :
    instance_(instance),
    enhancement_(enhancement) {
    // Intentionally empty.
  }

  virtual ~EnhancedSimple() = default;

 private:
  std::shared_ptr<types::ISimple> instance_;
  std::shared_ptr<E> enhancement_;
};

template<class E>
class EnhancedSomething
    : virtual public types::ISomething,
    virtual public IEnhanced<E> {
 public:
  types::ModelType model_type() const override {
    return types::ModelType::kSomething;
  }

  const std::vector<
    std::shared_ptr<types::IAbstractItem>
  >& some_items() const override {
    return instance_->some_items();
  }

  std::vector<
    std::shared_ptr<types::IAbstractItem>
  >& mutable_some_items() override {
    return instance_->mutable_some_items();
  }

  void set_some_items(
    std::vector<
      std::shared_ptr<types::IAbstractItem>
    > value
  ) override {
    instance_->set_some_items(value);
  }

  const std::vector<
    std::shared_ptr<types::ISimple>
  >& some_simples() const override {
    return instance_->some_simples();
  }

  std::vector<
    std::shared_ptr<types::ISimple>
  >& mutable_some_simples() override {
    return instance_->mutable_some_simples();
  }

  void set_some_simples(
    std::vector<
      std::shared_ptr<types::ISimple>
    > value
  ) override {
    instance_->set_some_simples(value);
  }

  const std::shared_ptr<E>& enhancement() const {
    return enhancement_;
  }

  std::shared_ptr<E>& mutable_enhancement() {
    return enhancement_;
  }

  void set_enhancement(
    std::shared_ptr<E> value
  ) {
    enhancement_ = std::move(value);
  }

  EnhancedSomething(
    std::shared_ptr<types::ISomething> instance,
    std::shared_ptr<E> enhancement
  ) :
    instance_(instance),
    enhancement_(enhancement) {
    // Intentionally empty.
  }

  virtual ~EnhancedSomething() = default;

 private:
  std::shared_ptr<types::ISomething> instance_;
  std::shared_ptr<E> enhancement_;
};

/**
 * Wrap \p that with an enhanced instance.
 *
 * \param that instance to be wrapped and enhanced
 * \param factory to produce an enhancement based on an instance
 * \return Enhanced instance, or `that` if no enhancement produced
 *
 * \tparam E type of the enhancement
 */
template<typename E>
std::shared_ptr<types::ISomeItem> WrapSomeItem(
  const std::shared_ptr<types::ISomeItem>& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  // We assume that we already checked whether `that` has been enhanced
  // in the caller.

  // No properties to be recursively enhanced.

  std::shared_ptr<E> enh(
    factory(that)
  );
  return (enh == nullptr)
    ? that
    : std::shared_ptr<types::ISomeItem>(
      new EnhancedSomeItem<E>(
        that,
        enh
      )
    );
}

/**
 * Wrap \p that with an enhanced instance.
 *
 * \param that instance to be wrapped and enhanced
 * \param factory to produce an enhancement based on an instance
 * \return Enhanced instance, or `that` if no enhancement produced
 *
 * \tparam E type of the enhancement
 */
template<typename E>
std::shared_ptr<types::IAnotherItem> WrapAnotherItem(
  const std::shared_ptr<types::IAnotherItem>& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  // We assume that we already checked whether `that` has been enhanced
  // in the caller.

  // No properties to be recursively enhanced.

  std::shared_ptr<E> enh(
    factory(that)
  );
  return (enh == nullptr)
    ? that
    : std::shared_ptr<types::IAnotherItem>(
      new EnhancedAnotherItem<E>(
        that,
        enh
      )
    );
}

/**
 * Wrap \p that with an enhanced instance.
 *
 * \param that instance to be wrapped and enhanced
 * \param factory to produce an enhancement based on an instance
 * \return Enhanced instance, or `that` if no enhancement produced
 *
 * \tparam E type of the enhancement
 */
template<typename E>
std::shared_ptr<types::ISimple> WrapSimple(
  const std::shared_ptr<types::ISimple>& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  // We assume that we already checked whether `that` has been enhanced
  // in the caller.

  // No properties to be recursively enhanced.

  std::shared_ptr<E> enh(
    factory(that)
  );
  return (enh == nullptr)
    ? that
    : std::shared_ptr<types::ISimple>(
      new EnhancedSimple<E>(
        that,
        enh
      )
    );
}

/**
 * Wrap \p that with an enhanced instance.
 *
 * \param that instance to be wrapped and enhanced
 * \param factory to produce an enhancement based on an instance
 * \return Enhanced instance, or `that` if no enhancement produced
 *
 * \tparam E type of the enhancement
 */
template<typename E>
std::shared_ptr<types::ISomething> WrapSomething(
  const std::shared_ptr<types::ISomething>& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  // We assume that we already checked whether `that` has been enhanced
  // in the caller.

  {
    const std::vector<
      std::shared_ptr<types::IAbstractItem>
    >& value(
      that->some_items()
    );
    const std::size_t size = value.size();

    std::vector<
      std::shared_ptr<types::IAbstractItem>
    > wrapped;
    wrapped.reserve(size);

    for (
      const std::shared_ptr<types::IAbstractItem>& item
      : value
    ) {
      wrapped.emplace_back(
        Wrap<E>(
          item,
          factory
        )
      );
    }

    that->set_some_items(
      std::move(wrapped)
    );
  }

  {
    const std::vector<
      std::shared_ptr<types::ISimple>
    >& value(
      that->some_simples()
    );
    const std::size_t size = value.size();

    std::vector<
      std::shared_ptr<types::ISimple>
    > wrapped;
    wrapped.reserve(size);

    for (
      const std::shared_ptr<types::ISimple>& item
      : value
    ) {
      wrapped.emplace_back(
        Wrap<E>(
          item,
          factory
        )
      );
    }

    that->set_some_simples(
      std::move(wrapped)
    );
  }

  std::shared_ptr<E> enh(
    factory(that)
  );
  return (enh == nullptr)
    ? that
    : std::shared_ptr<types::ISomething>(
      new EnhancedSomething<E>(
        that,
        enh
      )
    );
}

/**
 * Assert that the \p that instance has not been already enhanced.
 *
 * \param that instance to be checked
 * \tparam E type of the enhancement
 * \tparam T interface type of \p that instance
 * \throw std::logic_error if \p that already enhanced
 */
template<
  typename E,
  typename T,
  typename std::enable_if<
    std::is_base_of<types::IClass, T>::value
  >::type* = nullptr
>
void AssertNotEnhanced(
  const std::shared_ptr<T>& that
) {
  std::shared_ptr<impl::IEnhanced<E> > enhanced(
    std::dynamic_pointer_cast<
      impl::IEnhanced<E>
    >(that)
  );
  if (enhanced != nullptr) {
    throw std::logic_error(
      common::Concat(
        "An instance of ",
        stringification::to_string(that->model_type()),
        " has been already wrapped."
      )
    );
  }
}

}  // namespace impl
/// \endcond

/**
 * Wrap \p that instance recursively with the enhancement produced by the \p factory.
 *
 * The factory decides itself whether it will produce an enhancement for
 * \p that instance, or not. Even if no enhancement has been produced for \p that
 * instance, we will still continue to enhance the instances referenced
 * by \p that instance recursively.
 *
 * \param that instance to wrap
 * \param factory to selectively produce an enhancement
 * \return enhanced \p that instance
 * \throw std::logic_error if \p that instance has been already wrapped.
 * \tparam E type of the enhancement
 */
///@{

template <typename E>
std::shared_ptr<
  types::IClass
> Wrap(
  const std::shared_ptr<
    types::IClass
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  impl::AssertNotEnhanced<
    E,
    types::IClass
  >(that);

  switch (that->model_type()) {
    case types::ModelType::kSomeItem:
      return impl::WrapSomeItem<E>(
        std::dynamic_pointer_cast<
          types::ISomeItem
        >(that),
        factory
      );
      break;
    case types::ModelType::kAnotherItem:
      return impl::WrapAnotherItem<E>(
        std::dynamic_pointer_cast<
          types::IAnotherItem
        >(that),
        factory
      );
      break;
    case types::ModelType::kSimple:
      return impl::WrapSimple<E>(
        std::dynamic_pointer_cast<
          types::ISimple
        >(that),
        factory
      );
      break;
    case types::ModelType::kSomething:
      return impl::WrapSomething<E>(
        std::dynamic_pointer_cast<
          types::ISomething
        >(that),
        factory
      );
      break;
    default:
      throw std::invalid_argument(
        common::Concat(
          "Unexpected model type: ",
          std::to_string(
            static_cast<std::uint32_t>(
              that->model_type()
            )
          )
        )
      );
      break;
  }
}

template <typename E>
std::shared_ptr<
  types::IAbstractItem
> Wrap(
  const std::shared_ptr<
    types::IAbstractItem
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  impl::AssertNotEnhanced<
    E,
    types::IAbstractItem
  >(that);

  switch (that->model_type()) {
    case types::ModelType::kAnotherItem:
      return impl::WrapAnotherItem<E>(
        std::dynamic_pointer_cast<
          types::IAnotherItem
        >(that),
        factory
      );
      break;
    case types::ModelType::kSomeItem:
      return impl::WrapSomeItem<E>(
        std::dynamic_pointer_cast<
          types::ISomeItem
        >(that),
        factory
      );
      break;
    default:
      throw std::invalid_argument(
        common::Concat(
          "Unexpected model type: ",
          std::to_string(
            static_cast<std::uint32_t>(
              that->model_type()
            )
          )
        )
      );
      break;
  }
}

template <typename E>
std::shared_ptr<
  types::ISomeItem
> Wrap(
  const std::shared_ptr<
    types::ISomeItem
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  impl::AssertNotEnhanced<
    E,
    types::ISomeItem
  >(that);

  switch (that->model_type()) {
    case types::ModelType::kSomeItem:
      return impl::WrapSomeItem<E>(
        that,
        factory
      );
      break;
    default:
      throw std::invalid_argument(
        common::Concat(
          "Unexpected model type: ",
          std::to_string(
            static_cast<std::uint32_t>(
              that->model_type()
            )
          )
        )
      );
      break;
  }
}

template <typename E>
std::shared_ptr<
  types::IAnotherItem
> Wrap(
  const std::shared_ptr<
    types::IAnotherItem
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  impl::AssertNotEnhanced<
    E,
    types::IAnotherItem
  >(that);

  switch (that->model_type()) {
    case types::ModelType::kAnotherItem:
      return impl::WrapAnotherItem<E>(
        that,
        factory
      );
      break;
    default:
      throw std::invalid_argument(
        common::Concat(
          "Unexpected model type: ",
          std::to_string(
            static_cast<std::uint32_t>(
              that->model_type()
            )
          )
        )
      );
      break;
  }
}

template <typename E>
std::shared_ptr<
  types::ISimple
> Wrap(
  const std::shared_ptr<
    types::ISimple
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  impl::AssertNotEnhanced<
    E,
    types::ISimple
  >(that);

  switch (that->model_type()) {
    case types::ModelType::kSimple:
      return impl::WrapSimple<E>(
        that,
        factory
      );
      break;
    default:
      throw std::invalid_argument(
        common::Concat(
          "Unexpected model type: ",
          std::to_string(
            static_cast<std::uint32_t>(
              that->model_type()
            )
          )
        )
      );
      break;
  }
}

template <typename E>
std::shared_ptr<
  types::ISomething
> Wrap(
  const std::shared_ptr<
    types::ISomething
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  impl::AssertNotEnhanced<
    E,
    types::ISomething
  >(that);

  switch (that->model_type()) {
    case types::ModelType::kSomething:
      return impl::WrapSomething<E>(
        that,
        factory
      );
      break;
    default:
      throw std::invalid_argument(
        common::Concat(
          "Unexpected model type: ",
          std::to_string(
            static_cast<std::uint32_t>(
              that->model_type()
            )
          )
        )
      );
      break;
  }
}

///@}}

/**
 * Try to unwrap the enhancement from \p that instance.
 *
 * \param that instance possibly wrapped with an enhancement
 * \return the enhancement, or `nullptr` if \p that instance has not been wrapped
 * \tparam E type of the enhancement
 */
template <typename E>
std::shared_ptr<E> Unwrap(
  const std::shared_ptr<types::IClass>& that
) {
  const std::shared_ptr<impl::IEnhanced<E> >& maybe_enhanced(
    std::dynamic_pointer_cast<impl::IEnhanced<E> >(that)
  );

  if (!maybe_enhanced) {
    return nullptr;
  }

  return maybe_enhanced->enhancement();
}

/**
 * Unwrap the enhancement from \p that instance.
 *
 * \remark \p that instance must have been wrapped before.
 *
 * \param that instance expected to be wrapped with an enhancement
 * \return the enhancement
 * \throw std::invalid_argument if \p that instance has not been wrapped
 * \tparam E type of the enhancement
 */
template <typename E>
std::shared_ptr<E> MustUnwrap(
  const std::shared_ptr<types::IClass>& that
) {
  std::shared_ptr<E> enhancement(
    Unwrap<E>(that)
  );
  if (!enhancement) {
    throw std::invalid_argument(
      common::Concat(
        "Expected an instance of ",
        stringification::to_string(that->model_type()),
        " to have been already wrapped with an enhancement, "
        "but it has been not."
      )
    );
  }
  return enhancement;
}

}  // namespace enhancing
/**@}*/

}  // namespace dummy

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

#endif  // DUMMY_ENHANCING_GUARD_
