#ifndef DUMMY_TYPES_GUARD_
#define DUMMY_TYPES_GUARD_

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

#include "dummy/common.hpp"

#pragma warning(push, 0)
#include <cstddef>
#include <cstdint>
#include <memory>
#include <string>
#include <vector>
#pragma warning(pop)

namespace dummy {

/**
 * \defgroup types Define data structures corresponding to the meta-model.
 * @{
 */
namespace types {

// region Enumerations

/**
 * Enumerate the model types for faster type switches.
 *
 * For example, switch statements can be implemented as jump tables.
 */
enum class ModelType : std::uint32_t {
  kBranch = 0,
  kLeaf = 1,
  kBlossom = 2,
  kSomething = 3,
  kContainer = 4
};

// endregion Enumerations

// region Forward declaration of interfaces

// endregion Forward declaration of interfaces

class INode;

class IBranch;

class ILeaf;

class IBlossom;

class ISomething;

class IContainer;

// region Class interfaces

/**
 * Model the most general instance of the model.
 */
class IClass {
 public:
  /**
   * Indicate the runtime model type.
   */
  virtual ModelType model_type() const = 0;
  virtual ~IClass() = default;
};

class INode
    : virtual public IClass {
 public:
  virtual const std::wstring& identifier() const = 0;

  virtual std::wstring& mutable_identifier() = 0;

  virtual void set_identifier(
    std::wstring value
  ) = 0;

  virtual ~INode() = default;
};

class IBranch
    : virtual public INode {
 public:
  virtual const std::wstring& description() const = 0;

  virtual std::wstring& mutable_description() = 0;

  virtual void set_description(
    std::wstring value
  ) = 0;

  virtual ~IBranch() = default;
};

class ILeaf
    : virtual public IBranch {
 public:
  virtual int64_t value() const = 0;

  virtual int64_t& mutable_value() = 0;

  virtual void set_value(
    int64_t value
  ) = 0;

  virtual ~ILeaf() = default;
};

class IBlossom
    : virtual public ILeaf {
 public:
  virtual const std::wstring& details() const = 0;

  virtual std::wstring& mutable_details() = 0;

  virtual void set_details(
    std::wstring value
  ) = 0;

  virtual ~IBlossom() = default;
};

class ISomething
    : virtual public IClass {
 public:
  virtual const std::shared_ptr<INode>& some_choice() const = 0;

  virtual std::shared_ptr<INode>& mutable_some_choice() = 0;

  virtual void set_some_choice(
    std::shared_ptr<INode> value
  ) = 0;

  virtual const std::shared_ptr<IBranch>& something_without_choice() const = 0;

  virtual std::shared_ptr<IBranch>& mutable_something_without_choice() = 0;

  virtual void set_something_without_choice(
    std::shared_ptr<IBranch> value
  ) = 0;

  virtual ~ISomething() = default;
};

class IContainer
    : virtual public IClass {
 public:
  virtual const std::shared_ptr<INode>& node() const = 0;

  virtual std::shared_ptr<INode>& mutable_node() = 0;

  virtual void set_node(
    std::shared_ptr<INode> value
  ) = 0;

  virtual const std::shared_ptr<ISomething>& something() const = 0;

  virtual std::shared_ptr<ISomething>& mutable_something() = 0;

  virtual void set_something(
    std::shared_ptr<ISomething> value
  ) = 0;

  virtual ~IContainer() = default;
};

// endregion

// region Definitions of concrete classes

class Branch
    : public IBranch {
 public:
  Branch(
    std::wstring identifier,
    std::wstring description
  );

  ModelType model_type() const override;

  // region Get and set identifier_

  const std::wstring& identifier() const override;

  std::wstring& mutable_identifier() override;

  void set_identifier(
    std::wstring value
  ) override;

  // endregion

  // region Get and set description_

  const std::wstring& description() const override;

  std::wstring& mutable_description() override;

  void set_description(
    std::wstring value
  ) override;

  // endregion

  ~Branch() override = default;

 private:
  std::wstring identifier_;

  std::wstring description_;
};

class Leaf
    : public ILeaf {
 public:
  Leaf(
    std::wstring identifier,
    std::wstring description,
    int64_t value
  );

  ModelType model_type() const override;

  // region Get and set identifier_

  const std::wstring& identifier() const override;

  std::wstring& mutable_identifier() override;

  void set_identifier(
    std::wstring value
  ) override;

  // endregion

  // region Get and set description_

  const std::wstring& description() const override;

  std::wstring& mutable_description() override;

  void set_description(
    std::wstring value
  ) override;

  // endregion

  // region Get and set value_

  int64_t value() const override;

  int64_t& mutable_value() override;

  void set_value(
    int64_t value
  ) override;

  // endregion

  ~Leaf() override = default;

 private:
  std::wstring identifier_;

  std::wstring description_;

  int64_t value_;
};

class Blossom
    : public IBlossom {
 public:
  Blossom(
    std::wstring identifier,
    std::wstring description,
    int64_t value,
    std::wstring details
  );

  ModelType model_type() const override;

  // region Get and set identifier_

  const std::wstring& identifier() const override;

  std::wstring& mutable_identifier() override;

  void set_identifier(
    std::wstring value
  ) override;

  // endregion

  // region Get and set description_

  const std::wstring& description() const override;

  std::wstring& mutable_description() override;

  void set_description(
    std::wstring value
  ) override;

  // endregion

  // region Get and set value_

  int64_t value() const override;

  int64_t& mutable_value() override;

  void set_value(
    int64_t value
  ) override;

  // endregion

  // region Get and set details_

  const std::wstring& details() const override;

  std::wstring& mutable_details() override;

  void set_details(
    std::wstring value
  ) override;

  // endregion

  ~Blossom() override = default;

 private:
  std::wstring identifier_;

  std::wstring description_;

  int64_t value_;

  std::wstring details_;
};

class Something
    : public ISomething {
 public:
  Something(
    std::shared_ptr<INode> some_choice,
    std::shared_ptr<IBranch> something_without_choice
  );

  ModelType model_type() const override;

  // region Get and set some_choice_

  const std::shared_ptr<INode>& some_choice() const override;

  std::shared_ptr<INode>& mutable_some_choice() override;

  void set_some_choice(
    std::shared_ptr<INode> value
  ) override;

  // endregion

  // region Get and set something_without_choice_

  const std::shared_ptr<IBranch>& something_without_choice() const override;

  std::shared_ptr<IBranch>& mutable_something_without_choice() override;

  void set_something_without_choice(
    std::shared_ptr<IBranch> value
  ) override;

  // endregion

  ~Something() override = default;

 private:
  std::shared_ptr<INode> some_choice_;

  std::shared_ptr<IBranch> something_without_choice_;
};

class Container
    : public IContainer {
 public:
  Container(
    std::shared_ptr<INode> node,
    std::shared_ptr<ISomething> something
  );

  ModelType model_type() const override;

  // region Get and set node_

  const std::shared_ptr<INode>& node() const override;

  std::shared_ptr<INode>& mutable_node() override;

  void set_node(
    std::shared_ptr<INode> value
  ) override;

  // endregion

  // region Get and set something_

  const std::shared_ptr<ISomething>& something() const override;

  std::shared_ptr<ISomething>& mutable_something() override;

  void set_something(
    std::shared_ptr<ISomething> value
  ) override;

  // endregion

  ~Container() override = default;

 private:
  std::shared_ptr<INode> node_;

  std::shared_ptr<ISomething> something_;
};

// endregion

// region Is-a functions

/**
 * \brief Check whether \p that instance is of runtime type
 * \ref INode.
 *
 * We use `IClass::model_type` to determine the runtime type, which is
 * a bit faster than native C++'s RTTI.
 *
 * \param that instance to check for runtime type
 * \return `true` if \p that instance is indeed
 * an instance of \ref INode
 */
bool IsNode(
  const IClass& that
);

/**
 * \brief Check whether \p that instance is of runtime type
 * \ref IBranch.
 *
 * We use `IClass::model_type` to determine the runtime type, which is
 * a bit faster than native C++'s RTTI.
 *
 * \param that instance to check for runtime type
 * \return `true` if \p that instance is indeed
 * an instance of \ref IBranch
 */
bool IsBranch(
  const IClass& that
);

/**
 * \brief Check whether \p that instance is of runtime type
 * \ref ILeaf.
 *
 * We use `IClass::model_type` to determine the runtime type, which is
 * a bit faster than native C++'s RTTI.
 *
 * \param that instance to check for runtime type
 * \return `true` if \p that instance is indeed
 * an instance of \ref ILeaf
 */
bool IsLeaf(
  const IClass& that
);

/**
 * \brief Check whether \p that instance is of runtime type
 * \ref IBlossom.
 *
 * We use `IClass::model_type` to determine the runtime type, which is
 * a bit faster than native C++'s RTTI.
 *
 * \param that instance to check for runtime type
 * \return `true` if \p that instance is indeed
 * an instance of \ref IBlossom
 */
bool IsBlossom(
  const IClass& that
);

/**
 * \brief Check whether \p that instance is of runtime type
 * \ref ISomething.
 *
 * We use `IClass::model_type` to determine the runtime type, which is
 * a bit faster than native C++'s RTTI.
 *
 * \param that instance to check for runtime type
 * \return `true` if \p that instance is indeed
 * an instance of \ref ISomething
 */
bool IsSomething(
  const IClass& that
);

/**
 * \brief Check whether \p that instance is of runtime type
 * \ref IContainer.
 *
 * We use `IClass::model_type` to determine the runtime type, which is
 * a bit faster than native C++'s RTTI.
 *
 * \param that instance to check for runtime type
 * \return `true` if \p that instance is indeed
 * an instance of \ref IContainer
 */
bool IsContainer(
  const IClass& that
);

// endregion Is-a functions

}  // namespace types
/**@}*/

}  // namespace dummy

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

#endif  // DUMMY_TYPES_GUARD_
