#ifndef DUMMY_ENHANCING_GUARD_
#define DUMMY_ENHANCING_GUARD_

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

#include "dummy/common.hpp"
#include "dummy/stringification.hpp"
#include "dummy/types.hpp"

#pragma warning(push, 0)
#include <sstream>
#include <stdexcept>
#pragma warning(pop)

namespace dummy {

/**
 * \defgroup enhancing Enhance instances of the model with your custom enhancements.
 * @{
 */
namespace enhancing {

// region Forward declarations

template <typename E>
std::shared_ptr<
  types::INode
> Wrap(
  const std::shared_ptr<
    types::INode
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
);

template <typename E>
std::shared_ptr<
  types::IBranch
> Wrap(
  const std::shared_ptr<
    types::IBranch
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
);

template <typename E>
std::shared_ptr<
  types::ILeaf
> Wrap(
  const std::shared_ptr<
    types::ILeaf
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
);

template <typename E>
std::shared_ptr<
  types::IBlossom
> Wrap(
  const std::shared_ptr<
    types::IBlossom
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
);

template <typename E>
std::shared_ptr<
  types::ISomething
> Wrap(
  const std::shared_ptr<
    types::ISomething
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
);

template <typename E>
std::shared_ptr<
  types::IContainer
> Wrap(
  const std::shared_ptr<
    types::IContainer
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
);

// endregion Forward declarations

/// \cond HIDDEN
namespace impl {

template<typename E>
class IEnhanced {
 public:
  virtual const std::shared_ptr<E>& enhancement() const = 0;

  virtual std::shared_ptr<E>& mutable_enhancement() = 0;

  virtual void set_enhancement(
    std::shared_ptr<E> value
  ) = 0;

  virtual ~IEnhanced() = default;
};

template<class E>
class EnhancedBranch
    : virtual public types::IBranch,
    virtual public IEnhanced<E> {
 public:
  types::ModelType model_type() const override {
    return types::ModelType::kBranch;
  }

  const std::wstring& identifier() const override {
    return instance_->identifier();
  }

  std::wstring& mutable_identifier() override {
    return instance_->mutable_identifier();
  }

  void set_identifier(
    std::wstring value
  ) override {
    instance_->set_identifier(value);
  }

  const std::wstring& description() const override {
    return instance_->description();
  }

  std::wstring& mutable_description() override {
    return instance_->mutable_description();
  }

  void set_description(
    std::wstring value
  ) override {
    instance_->set_description(value);
  }

  const std::shared_ptr<E>& enhancement() const {
    return enhancement_;
  }

  std::shared_ptr<E>& mutable_enhancement() {
    return enhancement_;
  }

  void set_enhancement(
    std::shared_ptr<E> value
  ) {
    enhancement_ = std::move(value);
  }

  EnhancedBranch(
    std::shared_ptr<types::IBranch> instance,
    std::shared_ptr<E> enhancement
  ) :
    instance_(instance),
    enhancement_(enhancement) {
    // Intentionally empty.
  }

  virtual ~EnhancedBranch() = default;

 private:
  std::shared_ptr<types::IBranch> instance_;
  std::shared_ptr<E> enhancement_;
};

template<class E>
class EnhancedLeaf
    : virtual public types::ILeaf,
    virtual public IEnhanced<E> {
 public:
  types::ModelType model_type() const override {
    return types::ModelType::kLeaf;
  }

  const std::wstring& identifier() const override {
    return instance_->identifier();
  }

  std::wstring& mutable_identifier() override {
    return instance_->mutable_identifier();
  }

  void set_identifier(
    std::wstring value
  ) override {
    instance_->set_identifier(value);
  }

  const std::wstring& description() const override {
    return instance_->description();
  }

  std::wstring& mutable_description() override {
    return instance_->mutable_description();
  }

  void set_description(
    std::wstring value
  ) override {
    instance_->set_description(value);
  }

  int64_t value() const override {
    return instance_->value();
  }

  int64_t& mutable_value() override {
    return instance_->mutable_value();
  }

  void set_value(
    int64_t value
  ) override {
    instance_->set_value(value);
  }

  const std::shared_ptr<E>& enhancement() const {
    return enhancement_;
  }

  std::shared_ptr<E>& mutable_enhancement() {
    return enhancement_;
  }

  void set_enhancement(
    std::shared_ptr<E> value
  ) {
    enhancement_ = std::move(value);
  }

  EnhancedLeaf(
    std::shared_ptr<types::ILeaf> instance,
    std::shared_ptr<E> enhancement
  ) :
    instance_(instance),
    enhancement_(enhancement) {
    // Intentionally empty.
  }

  virtual ~EnhancedLeaf() = default;

 private:
  std::shared_ptr<types::ILeaf> instance_;
  std::shared_ptr<E> enhancement_;
};

template<class E>
class EnhancedBlossom
    : virtual public types::IBlossom,
    virtual public IEnhanced<E> {
 public:
  types::ModelType model_type() const override {
    return types::ModelType::kBlossom;
  }

  const std::wstring& identifier() const override {
    return instance_->identifier();
  }

  std::wstring& mutable_identifier() override {
    return instance_->mutable_identifier();
  }

  void set_identifier(
    std::wstring value
  ) override {
    instance_->set_identifier(value);
  }

  const std::wstring& description() const override {
    return instance_->description();
  }

  std::wstring& mutable_description() override {
    return instance_->mutable_description();
  }

  void set_description(
    std::wstring value
  ) override {
    instance_->set_description(value);
  }

  int64_t value() const override {
    return instance_->value();
  }

  int64_t& mutable_value() override {
    return instance_->mutable_value();
  }

  void set_value(
    int64_t value
  ) override {
    instance_->set_value(value);
  }

  const std::wstring& details() const override {
    return instance_->details();
  }

  std::wstring& mutable_details() override {
    return instance_->mutable_details();
  }

  void set_details(
    std::wstring value
  ) override {
    instance_->set_details(value);
  }

  const std::shared_ptr<E>& enhancement() const {
    return enhancement_;
  }

  std::shared_ptr<E>& mutable_enhancement() {
    return enhancement_;
  }

  void set_enhancement(
    std::shared_ptr<E> value
  ) {
    enhancement_ = std::move(value);
  }

  EnhancedBlossom(
    std::shared_ptr<types::IBlossom> instance,
    std::shared_ptr<E> enhancement
  ) :
    instance_(instance),
    enhancement_(enhancement) {
    // Intentionally empty.
  }

  virtual ~EnhancedBlossom() = default;

 private:
  std::shared_ptr<types::IBlossom> instance_;
  std::shared_ptr<E> enhancement_;
};

template<class E>
class EnhancedSomething
    : virtual public types::ISomething,
    virtual public IEnhanced<E> {
 public:
  types::ModelType model_type() const override {
    return types::ModelType::kSomething;
  }

  const std::shared_ptr<types::INode>& some_choice() const override {
    return instance_->some_choice();
  }

  std::shared_ptr<types::INode>& mutable_some_choice() override {
    return instance_->mutable_some_choice();
  }

  void set_some_choice(
    std::shared_ptr<types::INode> value
  ) override {
    instance_->set_some_choice(value);
  }

  const std::shared_ptr<types::IBranch>& something_without_choice() const override {
    return instance_->something_without_choice();
  }

  std::shared_ptr<types::IBranch>& mutable_something_without_choice() override {
    return instance_->mutable_something_without_choice();
  }

  void set_something_without_choice(
    std::shared_ptr<types::IBranch> value
  ) override {
    instance_->set_something_without_choice(value);
  }

  const std::shared_ptr<E>& enhancement() const {
    return enhancement_;
  }

  std::shared_ptr<E>& mutable_enhancement() {
    return enhancement_;
  }

  void set_enhancement(
    std::shared_ptr<E> value
  ) {
    enhancement_ = std::move(value);
  }

  EnhancedSomething(
    std::shared_ptr<types::ISomething> instance,
    std::shared_ptr<E> enhancement
  ) :
    instance_(instance),
    enhancement_(enhancement) {
    // Intentionally empty.
  }

  virtual ~EnhancedSomething() = default;

 private:
  std::shared_ptr<types::ISomething> instance_;
  std::shared_ptr<E> enhancement_;
};

template<class E>
class EnhancedContainer
    : virtual public types::IContainer,
    virtual public IEnhanced<E> {
 public:
  types::ModelType model_type() const override {
    return types::ModelType::kContainer;
  }

  const std::shared_ptr<types::INode>& node() const override {
    return instance_->node();
  }

  std::shared_ptr<types::INode>& mutable_node() override {
    return instance_->mutable_node();
  }

  void set_node(
    std::shared_ptr<types::INode> value
  ) override {
    instance_->set_node(value);
  }

  const std::shared_ptr<types::ISomething>& something() const override {
    return instance_->something();
  }

  std::shared_ptr<types::ISomething>& mutable_something() override {
    return instance_->mutable_something();
  }

  void set_something(
    std::shared_ptr<types::ISomething> value
  ) override {
    instance_->set_something(value);
  }

  const std::shared_ptr<E>& enhancement() const {
    return enhancement_;
  }

  std::shared_ptr<E>& mutable_enhancement() {
    return enhancement_;
  }

  void set_enhancement(
    std::shared_ptr<E> value
  ) {
    enhancement_ = std::move(value);
  }

  EnhancedContainer(
    std::shared_ptr<types::IContainer> instance,
    std::shared_ptr<E> enhancement
  ) :
    instance_(instance),
    enhancement_(enhancement) {
    // Intentionally empty.
  }

  virtual ~EnhancedContainer() = default;

 private:
  std::shared_ptr<types::IContainer> instance_;
  std::shared_ptr<E> enhancement_;
};

/**
 * Wrap \p that with an enhanced instance.
 *
 * \param that instance to be wrapped and enhanced
 * \param factory to produce an enhancement based on an instance
 * \return Enhanced instance, or `that` if no enhancement produced
 *
 * \tparam E type of the enhancement
 */
template<typename E>
std::shared_ptr<types::IBranch> WrapBranch(
  const std::shared_ptr<types::IBranch>& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  // We assume that we already checked whether `that` has been enhanced
  // in the caller.

  // No properties to be recursively enhanced.

  std::shared_ptr<E> enh(
    factory(that)
  );
  return (enh == nullptr)
    ? that
    : std::shared_ptr<types::IBranch>(
      new EnhancedBranch<E>(
        that,
        enh
      )
    );
}

/**
 * Wrap \p that with an enhanced instance.
 *
 * \param that instance to be wrapped and enhanced
 * \param factory to produce an enhancement based on an instance
 * \return Enhanced instance, or `that` if no enhancement produced
 *
 * \tparam E type of the enhancement
 */
template<typename E>
std::shared_ptr<types::ILeaf> WrapLeaf(
  const std::shared_ptr<types::ILeaf>& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  // We assume that we already checked whether `that` has been enhanced
  // in the caller.

  // No properties to be recursively enhanced.

  std::shared_ptr<E> enh(
    factory(that)
  );
  return (enh == nullptr)
    ? that
    : std::shared_ptr<types::ILeaf>(
      new EnhancedLeaf<E>(
        that,
        enh
      )
    );
}

/**
 * Wrap \p that with an enhanced instance.
 *
 * \param that instance to be wrapped and enhanced
 * \param factory to produce an enhancement based on an instance
 * \return Enhanced instance, or `that` if no enhancement produced
 *
 * \tparam E type of the enhancement
 */
template<typename E>
std::shared_ptr<types::IBlossom> WrapBlossom(
  const std::shared_ptr<types::IBlossom>& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  // We assume that we already checked whether `that` has been enhanced
  // in the caller.

  // No properties to be recursively enhanced.

  std::shared_ptr<E> enh(
    factory(that)
  );
  return (enh == nullptr)
    ? that
    : std::shared_ptr<types::IBlossom>(
      new EnhancedBlossom<E>(
        that,
        enh
      )
    );
}

/**
 * Wrap \p that with an enhanced instance.
 *
 * \param that instance to be wrapped and enhanced
 * \param factory to produce an enhancement based on an instance
 * \return Enhanced instance, or `that` if no enhancement produced
 *
 * \tparam E type of the enhancement
 */
template<typename E>
std::shared_ptr<types::ISomething> WrapSomething(
  const std::shared_ptr<types::ISomething>& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  // We assume that we already checked whether `that` has been enhanced
  // in the caller.

  that->set_some_choice(
    Wrap<E>(
      that->some_choice(),
      factory
    )
  );

  that->set_something_without_choice(
    Wrap<E>(
      that->something_without_choice(),
      factory
    )
  );

  std::shared_ptr<E> enh(
    factory(that)
  );
  return (enh == nullptr)
    ? that
    : std::shared_ptr<types::ISomething>(
      new EnhancedSomething<E>(
        that,
        enh
      )
    );
}

/**
 * Wrap \p that with an enhanced instance.
 *
 * \param that instance to be wrapped and enhanced
 * \param factory to produce an enhancement based on an instance
 * \return Enhanced instance, or `that` if no enhancement produced
 *
 * \tparam E type of the enhancement
 */
template<typename E>
std::shared_ptr<types::IContainer> WrapContainer(
  const std::shared_ptr<types::IContainer>& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  // We assume that we already checked whether `that` has been enhanced
  // in the caller.

  that->set_node(
    Wrap<E>(
      that->node(),
      factory
    )
  );

  that->set_something(
    Wrap<E>(
      that->something(),
      factory
    )
  );

  std::shared_ptr<E> enh(
    factory(that)
  );
  return (enh == nullptr)
    ? that
    : std::shared_ptr<types::IContainer>(
      new EnhancedContainer<E>(
        that,
        enh
      )
    );
}

/**
 * Assert that the \p that instance has not been already enhanced.
 *
 * \param that instance to be checked
 * \tparam E type of the enhancement
 * \tparam T interface type of \p that instance
 * \throw std::logic_error if \p that already enhanced
 */
template<
  typename E,
  typename T,
  typename std::enable_if<
    std::is_base_of<types::IClass, T>::value
  >::type* = nullptr
>
void AssertNotEnhanced(
  const std::shared_ptr<T>& that
) {
  std::shared_ptr<impl::IEnhanced<E> > enhanced(
    std::dynamic_pointer_cast<
      impl::IEnhanced<E>
    >(that)
  );
  if (enhanced != nullptr) {
    throw std::logic_error(
      common::Concat(
        "An instance of ",
        stringification::to_string(that->model_type()),
        " has been already wrapped."
      )
    );
  }
}

}  // namespace impl
/// \endcond

/**
 * Wrap \p that instance recursively with the enhancement produced by the \p factory.
 *
 * The factory decides itself whether it will produce an enhancement for
 * \p that instance, or not. Even if no enhancement has been produced for \p that
 * instance, we will still continue to enhance the instances referenced
 * by \p that instance recursively.
 *
 * \param that instance to wrap
 * \param factory to selectively produce an enhancement
 * \return enhanced \p that instance
 * \throw std::logic_error if \p that instance has been already wrapped.
 * \tparam E type of the enhancement
 */
///@{

template <typename E>
std::shared_ptr<
  types::IClass
> Wrap(
  const std::shared_ptr<
    types::IClass
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  impl::AssertNotEnhanced<
    E,
    types::IClass
  >(that);

  switch (that->model_type()) {
    case types::ModelType::kBranch:
      return impl::WrapBranch<E>(
        std::dynamic_pointer_cast<
          types::IBranch
        >(that),
        factory
      );
      break;
    case types::ModelType::kLeaf:
      return impl::WrapLeaf<E>(
        std::dynamic_pointer_cast<
          types::ILeaf
        >(that),
        factory
      );
      break;
    case types::ModelType::kBlossom:
      return impl::WrapBlossom<E>(
        std::dynamic_pointer_cast<
          types::IBlossom
        >(that),
        factory
      );
      break;
    case types::ModelType::kSomething:
      return impl::WrapSomething<E>(
        std::dynamic_pointer_cast<
          types::ISomething
        >(that),
        factory
      );
      break;
    case types::ModelType::kContainer:
      return impl::WrapContainer<E>(
        std::dynamic_pointer_cast<
          types::IContainer
        >(that),
        factory
      );
      break;
    default:
      throw std::invalid_argument(
        common::Concat(
          "Unexpected model type: ",
          std::to_string(
            static_cast<std::uint32_t>(
              that->model_type()
            )
          )
        )
      );
      break;
  }
}

template <typename E>
std::shared_ptr<
  types::INode
> Wrap(
  const std::shared_ptr<
    types::INode
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  impl::AssertNotEnhanced<
    E,
    types::INode
  >(that);

  switch (that->model_type()) {
    case types::ModelType::kBranch:
      return impl::WrapBranch<E>(
        std::dynamic_pointer_cast<
          types::IBranch
        >(that),
        factory
      );
      break;
    case types::ModelType::kLeaf:
      return impl::WrapLeaf<E>(
        std::dynamic_pointer_cast<
          types::ILeaf
        >(that),
        factory
      );
      break;
    case types::ModelType::kBlossom:
      return impl::WrapBlossom<E>(
        std::dynamic_pointer_cast<
          types::IBlossom
        >(that),
        factory
      );
      break;
    default:
      throw std::invalid_argument(
        common::Concat(
          "Unexpected model type: ",
          std::to_string(
            static_cast<std::uint32_t>(
              that->model_type()
            )
          )
        )
      );
      break;
  }
}

template <typename E>
std::shared_ptr<
  types::IBranch
> Wrap(
  const std::shared_ptr<
    types::IBranch
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  impl::AssertNotEnhanced<
    E,
    types::IBranch
  >(that);

  switch (that->model_type()) {
    case types::ModelType::kBranch:
      return impl::WrapBranch<E>(
        that,
        factory
      );
      break;
    case types::ModelType::kLeaf:
      return impl::WrapLeaf<E>(
        std::dynamic_pointer_cast<
          types::ILeaf
        >(that),
        factory
      );
      break;
    case types::ModelType::kBlossom:
      return impl::WrapBlossom<E>(
        std::dynamic_pointer_cast<
          types::IBlossom
        >(that),
        factory
      );
      break;
    default:
      throw std::invalid_argument(
        common::Concat(
          "Unexpected model type: ",
          std::to_string(
            static_cast<std::uint32_t>(
              that->model_type()
            )
          )
        )
      );
      break;
  }
}

template <typename E>
std::shared_ptr<
  types::ILeaf
> Wrap(
  const std::shared_ptr<
    types::ILeaf
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  impl::AssertNotEnhanced<
    E,
    types::ILeaf
  >(that);

  switch (that->model_type()) {
    case types::ModelType::kLeaf:
      return impl::WrapLeaf<E>(
        that,
        factory
      );
      break;
    case types::ModelType::kBlossom:
      return impl::WrapBlossom<E>(
        std::dynamic_pointer_cast<
          types::IBlossom
        >(that),
        factory
      );
      break;
    default:
      throw std::invalid_argument(
        common::Concat(
          "Unexpected model type: ",
          std::to_string(
            static_cast<std::uint32_t>(
              that->model_type()
            )
          )
        )
      );
      break;
  }
}

template <typename E>
std::shared_ptr<
  types::IBlossom
> Wrap(
  const std::shared_ptr<
    types::IBlossom
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  impl::AssertNotEnhanced<
    E,
    types::IBlossom
  >(that);

  switch (that->model_type()) {
    case types::ModelType::kBlossom:
      return impl::WrapBlossom<E>(
        that,
        factory
      );
      break;
    default:
      throw std::invalid_argument(
        common::Concat(
          "Unexpected model type: ",
          std::to_string(
            static_cast<std::uint32_t>(
              that->model_type()
            )
          )
        )
      );
      break;
  }
}

template <typename E>
std::shared_ptr<
  types::ISomething
> Wrap(
  const std::shared_ptr<
    types::ISomething
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  impl::AssertNotEnhanced<
    E,
    types::ISomething
  >(that);

  switch (that->model_type()) {
    case types::ModelType::kSomething:
      return impl::WrapSomething<E>(
        that,
        factory
      );
      break;
    default:
      throw std::invalid_argument(
        common::Concat(
          "Unexpected model type: ",
          std::to_string(
            static_cast<std::uint32_t>(
              that->model_type()
            )
          )
        )
      );
      break;
  }
}

template <typename E>
std::shared_ptr<
  types::IContainer
> Wrap(
  const std::shared_ptr<
    types::IContainer
  >& that,
  const std::function<
    std::shared_ptr<E>(
      const std::shared_ptr<types::IClass>&
    )
  >& factory
) {
  impl::AssertNotEnhanced<
    E,
    types::IContainer
  >(that);

  switch (that->model_type()) {
    case types::ModelType::kContainer:
      return impl::WrapContainer<E>(
        that,
        factory
      );
      break;
    default:
      throw std::invalid_argument(
        common::Concat(
          "Unexpected model type: ",
          std::to_string(
            static_cast<std::uint32_t>(
              that->model_type()
            )
          )
        )
      );
      break;
  }
}

///@}}

/**
 * Try to unwrap the enhancement from \p that instance.
 *
 * \param that instance possibly wrapped with an enhancement
 * \return the enhancement, or `nullptr` if \p that instance has not been wrapped
 * \tparam E type of the enhancement
 */
template <typename E>
std::shared_ptr<E> Unwrap(
  const std::shared_ptr<types::IClass>& that
) {
  const std::shared_ptr<impl::IEnhanced<E> >& maybe_enhanced(
    std::dynamic_pointer_cast<impl::IEnhanced<E> >(that)
  );

  if (!maybe_enhanced) {
    return nullptr;
  }

  return maybe_enhanced->enhancement();
}

/**
 * Unwrap the enhancement from \p that instance.
 *
 * \remark \p that instance must have been wrapped before.
 *
 * \param that instance expected to be wrapped with an enhancement
 * \return the enhancement
 * \throw std::invalid_argument if \p that instance has not been wrapped
 * \tparam E type of the enhancement
 */
template <typename E>
std::shared_ptr<E> MustUnwrap(
  const std::shared_ptr<types::IClass>& that
) {
  std::shared_ptr<E> enhancement(
    Unwrap<E>(that)
  );
  if (!enhancement) {
    throw std::invalid_argument(
      common::Concat(
        "Expected an instance of ",
        stringification::to_string(that->model_type()),
        " to have been already wrapped with an enhancement, "
        "but it has been not."
      )
    );
  }
  return enhancement;
}

}  // namespace enhancing
/**@}*/

}  // namespace dummy

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

#endif  // DUMMY_ENHANCING_GUARD_
