#ifndef DUMMY_TYPES_GUARD_
#define DUMMY_TYPES_GUARD_

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

#include "dummy/common.hpp"

#pragma warning(push, 0)
#include <cstddef>
#include <cstdint>
#include <memory>
#include <string>
#include <vector>
#pragma warning(pop)

namespace dummy {

/**
 * \defgroup types Define data structures corresponding to the meta-model.
 * @{
 */
namespace types {

// region Enumerations

/**
 * Enumerate the model types for faster type switches.
 *
 * For example, switch statements can be implemented as jump tables.
 */
enum class ModelType : std::uint32_t {
  kSomething = 0
};

// endregion Enumerations

// region Forward declaration of interfaces

// endregion Forward declaration of interfaces

class ISomething;

// region Class interfaces

/**
 * Model the most general instance of the model.
 */
class IClass {
 public:
  /**
   * Indicate the runtime model type.
   */
  virtual ModelType model_type() const = 0;
  virtual ~IClass() = default;
};

class ISomething
    : virtual public IClass {
 public:
  virtual const std::vector<bool>& some_bools() const = 0;

  virtual std::vector<bool>& mutable_some_bools() = 0;

  virtual void set_some_bools(
    std::vector<bool> value
  ) = 0;

  virtual const std::vector<int64_t>& some_ints() const = 0;

  virtual std::vector<int64_t>& mutable_some_ints() = 0;

  virtual void set_some_ints(
    std::vector<int64_t> value
  ) = 0;

  virtual const std::vector<double>& some_floats() const = 0;

  virtual std::vector<double>& mutable_some_floats() = 0;

  virtual void set_some_floats(
    std::vector<double> value
  ) = 0;

  virtual const std::vector<std::wstring>& some_strings() const = 0;

  virtual std::vector<std::wstring>& mutable_some_strings() = 0;

  virtual void set_some_strings(
    std::vector<std::wstring> value
  ) = 0;

  virtual const std::vector<
    std::vector<std::uint8_t>
  >& some_bytes() const = 0;

  virtual std::vector<
    std::vector<std::uint8_t>
  >& mutable_some_bytes() = 0;

  virtual void set_some_bytes(
    std::vector<
      std::vector<std::uint8_t>
    > value
  ) = 0;

  virtual ~ISomething() = default;
};

// endregion

// region Definitions of concrete classes

class Something
    : public ISomething {
 public:
  Something(
    std::vector<bool> some_bools,
    std::vector<int64_t> some_ints,
    std::vector<double> some_floats,
    std::vector<std::wstring> some_strings,
    std::vector<
      std::vector<std::uint8_t>
    > some_bytes
  );

  ModelType model_type() const override;

  // region Get and set some_bools_

  const std::vector<bool>& some_bools() const override;

  std::vector<bool>& mutable_some_bools() override;

  void set_some_bools(
    std::vector<bool> value
  ) override;

  // endregion

  // region Get and set some_ints_

  const std::vector<int64_t>& some_ints() const override;

  std::vector<int64_t>& mutable_some_ints() override;

  void set_some_ints(
    std::vector<int64_t> value
  ) override;

  // endregion

  // region Get and set some_floats_

  const std::vector<double>& some_floats() const override;

  std::vector<double>& mutable_some_floats() override;

  void set_some_floats(
    std::vector<double> value
  ) override;

  // endregion

  // region Get and set some_strings_

  const std::vector<std::wstring>& some_strings() const override;

  std::vector<std::wstring>& mutable_some_strings() override;

  void set_some_strings(
    std::vector<std::wstring> value
  ) override;

  // endregion

  // region Get and set some_bytes_

  const std::vector<
    std::vector<std::uint8_t>
  >& some_bytes() const override;

  std::vector<
    std::vector<std::uint8_t>
  >& mutable_some_bytes() override;

  void set_some_bytes(
    std::vector<
      std::vector<std::uint8_t>
    > value
  ) override;

  // endregion

  ~Something() override = default;

 private:
  std::vector<bool> some_bools_;

  std::vector<int64_t> some_ints_;

  std::vector<double> some_floats_;

  std::vector<std::wstring> some_strings_;

  std::vector<
    std::vector<std::uint8_t>
  > some_bytes_;
};

// endregion

// region Is-a functions

/**
 * \brief Check whether \p that instance is of runtime type
 * \ref ISomething.
 *
 * We use `IClass::model_type` to determine the runtime type, which is
 * a bit faster than native C++'s RTTI.
 *
 * \param that instance to check for runtime type
 * \return `true` if \p that instance is indeed
 * an instance of \ref ISomething
 */
bool IsSomething(
  const IClass& that
);

// endregion Is-a functions

}  // namespace types
/**@}*/

}  // namespace dummy

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

#endif  // DUMMY_TYPES_GUARD_
