#ifndef DUMMY_TYPES_GUARD_
#define DUMMY_TYPES_GUARD_

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

#include "dummy/common.hpp"

#pragma warning(push, 0)
#include <cstddef>
#include <cstdint>
#include <memory>
#include <string>
#include <vector>
#pragma warning(pop)

namespace dummy {

/**
 * \defgroup types Define data structures corresponding to the meta-model.
 * @{
 */
namespace types {

// region Enumerations

/**
 * Enumerate the model types for faster type switches.
 *
 * For example, switch statements can be implemented as jump tables.
 */
enum class ModelType : std::uint32_t {
  kSomeItem = 0,
  kAnotherItem = 1,
  kSomething = 2
};

enum class Result : std::uint32_t {
  kOk = 0,
  kNotOk = 1,
};  // enum class Result

// endregion Enumerations

// region Forward declaration of interfaces

// endregion Forward declaration of interfaces

class IAbstractItem;

class ISomeItem;

class IAnotherItem;

class ISomething;

// region Class interfaces

/**
 * Model the most general instance of the model.
 */
class IClass {
 public:
  /**
   * Indicate the runtime model type.
   */
  virtual ModelType model_type() const = 0;
  virtual ~IClass() = default;
};

class IAbstractItem
    : virtual public IClass {
 public:
  virtual ~IAbstractItem() = default;
};

class ISomeItem
    : virtual public IAbstractItem {
 public:
  virtual const std::wstring& name() const = 0;

  virtual std::wstring& mutable_name() = 0;

  virtual void set_name(
    std::wstring value
  ) = 0;

  virtual ~ISomeItem() = default;
};

class IAnotherItem
    : virtual public IAbstractItem {
 public:
  virtual int64_t serial_number() const = 0;

  virtual int64_t& mutable_serial_number() = 0;

  virtual void set_serial_number(
    int64_t value
  ) = 0;

  virtual ~IAnotherItem() = default;
};

class ISomething
    : virtual public IClass {
 public:
  virtual const std::tuple<
    std::wstring,
    int64_t
  >& pair() const = 0;

  virtual std::tuple<
    std::wstring,
    int64_t
  >& mutable_pair() = 0;

  virtual void set_pair(
    std::tuple<
      std::wstring,
      int64_t
    > value
  ) = 0;

  virtual const std::tuple<
    std::shared_ptr<IAbstractItem>,
    std::shared_ptr<IAbstractItem>
  >& items() const = 0;

  virtual std::tuple<
    std::shared_ptr<IAbstractItem>,
    std::shared_ptr<IAbstractItem>
  >& mutable_items() = 0;

  virtual void set_items(
    std::tuple<
      std::shared_ptr<IAbstractItem>,
      std::shared_ptr<IAbstractItem>
    > value
  ) = 0;

  virtual const std::tuple<
    int64_t,
    std::shared_ptr<ISomeItem>,
    std::shared_ptr<IAbstractItem>,
    std::shared_ptr<ISomeItem>,
    int64_t,
    Result
  >& tricky() const = 0;

  virtual std::tuple<
    int64_t,
    std::shared_ptr<ISomeItem>,
    std::shared_ptr<IAbstractItem>,
    std::shared_ptr<ISomeItem>,
    int64_t,
    Result
  >& mutable_tricky() = 0;

  virtual void set_tricky(
    std::tuple<
      int64_t,
      std::shared_ptr<ISomeItem>,
      std::shared_ptr<IAbstractItem>,
      std::shared_ptr<ISomeItem>,
      int64_t,
      Result
    > value
  ) = 0;

  virtual ~ISomething() = default;
};

// endregion

// region Definitions of concrete classes

class SomeItem
    : public ISomeItem {
 public:
  explicit SomeItem(
    std::wstring name
  );

  ModelType model_type() const override;

  // region Get and set name_

  const std::wstring& name() const override;

  std::wstring& mutable_name() override;

  void set_name(
    std::wstring value
  ) override;

  // endregion

  ~SomeItem() override = default;

 private:
  std::wstring name_;
};

class AnotherItem
    : public IAnotherItem {
 public:
  explicit AnotherItem(
    int64_t serial_number
  );

  ModelType model_type() const override;

  // region Get and set serial_number_

  int64_t serial_number() const override;

  int64_t& mutable_serial_number() override;

  void set_serial_number(
    int64_t value
  ) override;

  // endregion

  ~AnotherItem() override = default;

 private:
  int64_t serial_number_;
};

class Something
    : public ISomething {
 public:
  Something(
    std::tuple<
      std::wstring,
      int64_t
    > pair,
    std::tuple<
      std::shared_ptr<IAbstractItem>,
      std::shared_ptr<IAbstractItem>
    > items,
    std::tuple<
      int64_t,
      std::shared_ptr<ISomeItem>,
      std::shared_ptr<IAbstractItem>,
      std::shared_ptr<ISomeItem>,
      int64_t,
      Result
    > tricky
  );

  ModelType model_type() const override;

  // region Get and set pair_

  const std::tuple<
    std::wstring,
    int64_t
  >& pair() const override;

  std::tuple<
    std::wstring,
    int64_t
  >& mutable_pair() override;

  void set_pair(
    std::tuple<
      std::wstring,
      int64_t
    > value
  ) override;

  // endregion

  // region Get and set items_

  const std::tuple<
    std::shared_ptr<IAbstractItem>,
    std::shared_ptr<IAbstractItem>
  >& items() const override;

  std::tuple<
    std::shared_ptr<IAbstractItem>,
    std::shared_ptr<IAbstractItem>
  >& mutable_items() override;

  void set_items(
    std::tuple<
      std::shared_ptr<IAbstractItem>,
      std::shared_ptr<IAbstractItem>
    > value
  ) override;

  // endregion

  // region Get and set tricky_

  const std::tuple<
    int64_t,
    std::shared_ptr<ISomeItem>,
    std::shared_ptr<IAbstractItem>,
    std::shared_ptr<ISomeItem>,
    int64_t,
    Result
  >& tricky() const override;

  std::tuple<
    int64_t,
    std::shared_ptr<ISomeItem>,
    std::shared_ptr<IAbstractItem>,
    std::shared_ptr<ISomeItem>,
    int64_t,
    Result
  >& mutable_tricky() override;

  void set_tricky(
    std::tuple<
      int64_t,
      std::shared_ptr<ISomeItem>,
      std::shared_ptr<IAbstractItem>,
      std::shared_ptr<ISomeItem>,
      int64_t,
      Result
    > value
  ) override;

  // endregion

  ~Something() override = default;

 private:
  std::tuple<
    std::wstring,
    int64_t
  > pair_;

  std::tuple<
    std::shared_ptr<IAbstractItem>,
    std::shared_ptr<IAbstractItem>
  > items_;

  std::tuple<
    int64_t,
    std::shared_ptr<ISomeItem>,
    std::shared_ptr<IAbstractItem>,
    std::shared_ptr<ISomeItem>,
    int64_t,
    Result
  > tricky_;
};

// endregion

// region Is-a functions

/**
 * \brief Check whether \p that instance is of runtime type
 * \ref IAbstractItem.
 *
 * We use `IClass::model_type` to determine the runtime type, which is
 * a bit faster than native C++'s RTTI.
 *
 * \param that instance to check for runtime type
 * \return `true` if \p that instance is indeed
 * an instance of \ref IAbstractItem
 */
bool IsAbstractItem(
  const IClass& that
);

/**
 * \brief Check whether \p that instance is of runtime type
 * \ref ISomeItem.
 *
 * We use `IClass::model_type` to determine the runtime type, which is
 * a bit faster than native C++'s RTTI.
 *
 * \param that instance to check for runtime type
 * \return `true` if \p that instance is indeed
 * an instance of \ref ISomeItem
 */
bool IsSomeItem(
  const IClass& that
);

/**
 * \brief Check whether \p that instance is of runtime type
 * \ref IAnotherItem.
 *
 * We use `IClass::model_type` to determine the runtime type, which is
 * a bit faster than native C++'s RTTI.
 *
 * \param that instance to check for runtime type
 * \return `true` if \p that instance is indeed
 * an instance of \ref IAnotherItem
 */
bool IsAnotherItem(
  const IClass& that
);

/**
 * \brief Check whether \p that instance is of runtime type
 * \ref ISomething.
 *
 * We use `IClass::model_type` to determine the runtime type, which is
 * a bit faster than native C++'s RTTI.
 *
 * \param that instance to check for runtime type
 * \return `true` if \p that instance is indeed
 * an instance of \ref ISomething
 */
bool IsSomething(
  const IClass& that
);

// endregion Is-a functions

}  // namespace types
/**@}*/

}  // namespace dummy

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

#endif  // DUMMY_TYPES_GUARD_
