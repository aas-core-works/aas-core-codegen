// Package types provides the data structures corresponding to the meta-model.
package types

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

// Enumerate the model types for faster type switches.
//
// For example, you can use an array of function pointers to
// implement such a switch.
type ModelType int
const (
	ModelTypeBranch ModelType = iota
	ModelTypeLeaf
	ModelTypeBlossom
	ModelTypeSomething
	ModelTypeContainer
)

// Represent the most general interface of an AAS model.
type IClass interface {
	// Return the concrete model type at run-time.
	//
	// Use the model type if you want to switch on the concrete model type
	// in efficient manner, as the compiler will most probably implement
	// the switch in form of a jump table.
	ModelType() ModelType

	// Apply the action on the instances referenced from this instance.
	//
	// If any of the actions returns abort `true`, the descent is immediately
	// stopped,  and abort `true` is also returned. Otherwise, return abort `false`.
	//
	// We do not recurse into the referenced instances.
	//
	// The action is not applied on this instance.
	DescendOnce(action func(IClass) bool) (abort bool)

	// Apply the action recursively on the instances referenced from this instance.
	//
	// If any of the actions returns abort `true`, the descent is immediately
	// stopped,  and abort `true` is also returned. Otherwise, return abort `false`.
	//
	// The action is not applied on this instance.
	Descend(action func(IClass) bool) (abort bool)
}

type INode interface {
	IClass

	Identifier() string;

	SetIdentifier(
		value string,
	);
}

// Check whether the instance corresponds to [aastypes.INode]
// based on its run-time model type.
//
// The implementation uses a switch statements which is
// most probably compiled as an efficient jump table by the compiler.
func IsNode(
	that IClass,
) (ok bool) {
	switch that.ModelType() {
	case ModelTypeBranch:
		ok = true
	case ModelTypeLeaf:
		ok = true
	case ModelTypeBlossom:
		ok = true
	}
	return
}

type IBranch interface {
	INode

	Description() string;

	SetDescription(
		value string,
	);
}

// Check whether the instance corresponds to [aastypes.IBranch]
// based on its run-time model type.
//
// The implementation uses a switch statements which is
// most probably compiled as an efficient jump table by the compiler.
func IsBranch(
	that IClass,
) (ok bool) {
	switch that.ModelType() {
	case ModelTypeBranch:
		ok = true
	case ModelTypeLeaf:
		ok = true
	case ModelTypeBlossom:
		ok = true
	}
	return
}

// Implements IBranch.
type Branch struct {
	identifier string
	description string
}

func (b *Branch) Identifier(
) string {
	return b.identifier
}

func (b *Branch) SetIdentifier(
	value string,
) {
	b.identifier = value
}

func (b *Branch) Description(
) string {
	return b.description
}

func (b *Branch) SetDescription(
	value string,
) {
	b.description = value
}

func (b *Branch) ModelType(
) ModelType {
	return ModelTypeBranch
}

// Apply the action on the instances referenced from b.
//
// If any of the actions returns abort `true`, the descent is immediately
// stopped,  and abort `true` is also returned. Otherwise, return abort `false`.
//
// We do not recurse into the referenced instances.
//
// The action is not applied on b.
func (b *Branch) DescendOnce(
	action func(IClass) bool,
) (abort bool) {
	// No descendable properties

	return
}

// Apply the action recursively on the instances referenced from b.
//
// If any of the actions returns abort `true`, the descent is immediately
// stopped,  and abort `true` is also returned. Otherwise, return abort `false`.
//
// The action is not applied on b.
func (b *Branch) Descend(
	action func(IClass) bool,
) (abort bool) {
	// No descendable properties

	return
}

// Create a new instance of Branch with
// the given properties.
func NewBranch(
	identifier string,
	description string,
) *Branch {
	return &Branch{
		identifier: identifier,
		description: description,
	}
}

type ILeaf interface {
	IBranch

	Value() int64;

	SetValue(
		value int64,
	);
}

// Check whether the instance corresponds to [aastypes.ILeaf]
// based on its run-time model type.
//
// The implementation uses a switch statements which is
// most probably compiled as an efficient jump table by the compiler.
func IsLeaf(
	that IClass,
) (ok bool) {
	switch that.ModelType() {
	case ModelTypeLeaf:
		ok = true
	case ModelTypeBlossom:
		ok = true
	}
	return
}

// Implements ILeaf.
type Leaf struct {
	identifier string
	description string
	value int64
}

func (l *Leaf) Identifier(
) string {
	return l.identifier
}

func (l *Leaf) SetIdentifier(
	value string,
) {
	l.identifier = value
}

func (l *Leaf) Description(
) string {
	return l.description
}

func (l *Leaf) SetDescription(
	value string,
) {
	l.description = value
}

func (l *Leaf) Value(
) int64 {
	return l.value
}

func (l *Leaf) SetValue(
	value int64,
) {
	l.value = value
}

func (l *Leaf) ModelType(
) ModelType {
	return ModelTypeLeaf
}

// Apply the action on the instances referenced from l.
//
// If any of the actions returns abort `true`, the descent is immediately
// stopped,  and abort `true` is also returned. Otherwise, return abort `false`.
//
// We do not recurse into the referenced instances.
//
// The action is not applied on l.
func (l *Leaf) DescendOnce(
	action func(IClass) bool,
) (abort bool) {
	// No descendable properties

	return
}

// Apply the action recursively on the instances referenced from l.
//
// If any of the actions returns abort `true`, the descent is immediately
// stopped,  and abort `true` is also returned. Otherwise, return abort `false`.
//
// The action is not applied on l.
func (l *Leaf) Descend(
	action func(IClass) bool,
) (abort bool) {
	// No descendable properties

	return
}

// Create a new instance of Leaf with
// the given properties.
func NewLeaf(
	identifier string,
	description string,
	value int64,
) *Leaf {
	return &Leaf{
		identifier: identifier,
		description: description,
		value: value,
	}
}

type IBlossom interface {
	ILeaf

	Details() string;

	SetDetails(
		value string,
	);
}

// Check whether the instance corresponds to [aastypes.IBlossom]
// based on its run-time model type.
//
// The implementation uses a switch statements which is
// most probably compiled as an efficient jump table by the compiler.
func IsBlossom(
	that IClass,
) (ok bool) {
	ok = that.ModelType() == ModelTypeBlossom
	return
}

// Implements IBlossom.
type Blossom struct {
	identifier string
	description string
	value int64
	details string
}

func (b *Blossom) Identifier(
) string {
	return b.identifier
}

func (b *Blossom) SetIdentifier(
	value string,
) {
	b.identifier = value
}

func (b *Blossom) Description(
) string {
	return b.description
}

func (b *Blossom) SetDescription(
	value string,
) {
	b.description = value
}

func (b *Blossom) Value(
) int64 {
	return b.value
}

func (b *Blossom) SetValue(
	value int64,
) {
	b.value = value
}

func (b *Blossom) Details(
) string {
	return b.details
}

func (b *Blossom) SetDetails(
	value string,
) {
	b.details = value
}

func (b *Blossom) ModelType(
) ModelType {
	return ModelTypeBlossom
}

// Apply the action on the instances referenced from b.
//
// If any of the actions returns abort `true`, the descent is immediately
// stopped,  and abort `true` is also returned. Otherwise, return abort `false`.
//
// We do not recurse into the referenced instances.
//
// The action is not applied on b.
func (b *Blossom) DescendOnce(
	action func(IClass) bool,
) (abort bool) {
	// No descendable properties

	return
}

// Apply the action recursively on the instances referenced from b.
//
// If any of the actions returns abort `true`, the descent is immediately
// stopped,  and abort `true` is also returned. Otherwise, return abort `false`.
//
// The action is not applied on b.
func (b *Blossom) Descend(
	action func(IClass) bool,
) (abort bool) {
	// No descendable properties

	return
}

// Create a new instance of Blossom with
// the given properties.
func NewBlossom(
	identifier string,
	description string,
	value int64,
	details string,
) *Blossom {
	return &Blossom{
		identifier: identifier,
		description: description,
		value: value,
		details: details,
	}
}

type ISomething interface {
	IClass

	SomeChoice() INode;

	SetSomeChoice(
		value INode,
	);

	SomethingWithoutChoice() IBranch;

	SetSomethingWithoutChoice(
		value IBranch,
	);
}

// Check whether the instance corresponds to [aastypes.ISomething]
// based on its run-time model type.
//
// The implementation uses a switch statements which is
// most probably compiled as an efficient jump table by the compiler.
func IsSomething(
	that IClass,
) (ok bool) {
	ok = that.ModelType() == ModelTypeSomething
	return
}

// Implements ISomething.
type Something struct {
	someChoice INode
	somethingWithoutChoice IBranch
}

func (s *Something) SomeChoice(
) INode {
	return s.someChoice
}

func (s *Something) SetSomeChoice(
	value INode,
) {
	s.someChoice = value
}

func (s *Something) SomethingWithoutChoice(
) IBranch {
	return s.somethingWithoutChoice
}

func (s *Something) SetSomethingWithoutChoice(
	value IBranch,
) {
	s.somethingWithoutChoice = value
}

func (s *Something) ModelType(
) ModelType {
	return ModelTypeSomething
}

// Apply the action on the instances referenced from s.
//
// If any of the actions returns abort `true`, the descent is immediately
// stopped,  and abort `true` is also returned. Otherwise, return abort `false`.
//
// We do not recurse into the referenced instances.
//
// The action is not applied on s.
func (s *Something) DescendOnce(
	action func(IClass) bool,
) (abort bool) {
	abort = action(
		s.someChoice,
	)
	if abort {
		return
	}

	abort = action(
		s.somethingWithoutChoice,
	)
	if abort {
		return
	}

	return
}

// Apply the action recursively on the instances referenced from s.
//
// If any of the actions returns abort `true`, the descent is immediately
// stopped,  and abort `true` is also returned. Otherwise, return abort `false`.
//
// The action is not applied on s.
func (s *Something) Descend(
	action func(IClass) bool,
) (abort bool) {
	abort = action(
		s.someChoice,
	)
	if abort {
		return
	}
	abort = s.someChoice.Descend(
		action,
	)
	if abort {
		return
	}

	abort = action(
		s.somethingWithoutChoice,
	)
	if abort {
		return
	}
	abort = s.somethingWithoutChoice.Descend(
		action,
	)
	if abort {
		return
	}

	return
}

// Create a new instance of Something with
// the given properties.
func NewSomething(
	someChoice INode,
	somethingWithoutChoice IBranch,
) *Something {
	return &Something{
		someChoice: someChoice,
		somethingWithoutChoice: somethingWithoutChoice,
	}
}

type IContainer interface {
	IClass

	Node() INode;

	SetNode(
		value INode,
	);

	Something() ISomething;

	SetSomething(
		value ISomething,
	);
}

// Check whether the instance corresponds to [aastypes.IContainer]
// based on its run-time model type.
//
// The implementation uses a switch statements which is
// most probably compiled as an efficient jump table by the compiler.
func IsContainer(
	that IClass,
) (ok bool) {
	ok = that.ModelType() == ModelTypeContainer
	return
}

// Implements IContainer.
type Container struct {
	node INode
	something ISomething
}

func (c *Container) Node(
) INode {
	return c.node
}

func (c *Container) SetNode(
	value INode,
) {
	c.node = value
}

func (c *Container) Something(
) ISomething {
	return c.something
}

func (c *Container) SetSomething(
	value ISomething,
) {
	c.something = value
}

func (c *Container) ModelType(
) ModelType {
	return ModelTypeContainer
}

// Apply the action on the instances referenced from c.
//
// If any of the actions returns abort `true`, the descent is immediately
// stopped,  and abort `true` is also returned. Otherwise, return abort `false`.
//
// We do not recurse into the referenced instances.
//
// The action is not applied on c.
func (c *Container) DescendOnce(
	action func(IClass) bool,
) (abort bool) {
	abort = action(
		c.node,
	)
	if abort {
		return
	}

	abort = action(
		c.something,
	)
	if abort {
		return
	}

	return
}

// Apply the action recursively on the instances referenced from c.
//
// If any of the actions returns abort `true`, the descent is immediately
// stopped,  and abort `true` is also returned. Otherwise, return abort `false`.
//
// The action is not applied on c.
func (c *Container) Descend(
	action func(IClass) bool,
) (abort bool) {
	abort = action(
		c.node,
	)
	if abort {
		return
	}
	abort = c.node.Descend(
		action,
	)
	if abort {
		return
	}

	abort = action(
		c.something,
	)
	if abort {
		return
	}
	abort = c.something.Descend(
		action,
	)
	if abort {
		return
	}

	return
}

// Create a new instance of Container with
// the given properties.
func NewContainer(
	node INode,
	something ISomething,
) *Container {
	return &Container{
		node: node,
		something: something,
	}
}

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.
