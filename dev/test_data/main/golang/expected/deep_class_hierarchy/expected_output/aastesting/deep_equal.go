package aastesting

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

import (
	"bytes"
	"fmt"
	aastypes "github.com/dummy-works/dummy/types"
)

// Perform a comparison for deep equality between `that` and `other` instance.
//
// The deep equality means that all the properties are checked for equality recursively.
func deepEqualBranch(
	that aastypes.IBranch,
	other aastypes.IBranch,
) bool {
	thatIdentifier := that.Identifier()
	otherIdentifier := other.Identifier()
	if thatIdentifier != otherIdentifier {
		return false
	}

	thatDescription := that.Description()
	otherDescription := other.Description()
	if thatDescription != otherDescription {
		return false
	}

	return true
}

// Perform a comparison for deep equality between `that` and `other` instance.
//
// The deep equality means that all the properties are checked for equality recursively.
func deepEqualLeaf(
	that aastypes.ILeaf,
	other aastypes.ILeaf,
) bool {
	thatIdentifier := that.Identifier()
	otherIdentifier := other.Identifier()
	if thatIdentifier != otherIdentifier {
		return false
	}

	thatDescription := that.Description()
	otherDescription := other.Description()
	if thatDescription != otherDescription {
		return false
	}

	thatValue := that.Value()
	otherValue := other.Value()
	if thatValue != otherValue {
		return false
	}

	return true
}

// Perform a comparison for deep equality between `that` and `other` instance.
//
// The deep equality means that all the properties are checked for equality recursively.
func deepEqualBlossom(
	that aastypes.IBlossom,
	other aastypes.IBlossom,
) bool {
	thatIdentifier := that.Identifier()
	otherIdentifier := other.Identifier()
	if thatIdentifier != otherIdentifier {
		return false
	}

	thatDescription := that.Description()
	otherDescription := other.Description()
	if thatDescription != otherDescription {
		return false
	}

	thatValue := that.Value()
	otherValue := other.Value()
	if thatValue != otherValue {
		return false
	}

	thatDetails := that.Details()
	otherDetails := other.Details()
	if thatDetails != otherDetails {
		return false
	}

	return true
}

// Perform a comparison for deep equality between `that` and `other` instance.
//
// The deep equality means that all the properties are checked for equality recursively.
func deepEqualSomething(
	that aastypes.ISomething,
	other aastypes.ISomething,
) bool {
	thatSomeChoice := that.SomeChoice()
	otherSomeChoice := other.SomeChoice()
	if !DeepEqual(
		thatSomeChoice,
		otherSomeChoice,
	) {
		return false
	}

	thatSomethingWithoutChoice := that.SomethingWithoutChoice()
	otherSomethingWithoutChoice := other.SomethingWithoutChoice()
	if !DeepEqual(
		thatSomethingWithoutChoice,
		otherSomethingWithoutChoice,
	) {
		return false
	}

	return true
}

// Perform a comparison for deep equality between `that` and `other` instance.
//
// The deep equality means that all the properties are checked for equality recursively.
func deepEqualContainer(
	that aastypes.IContainer,
	other aastypes.IContainer,
) bool {
	thatNode := that.Node()
	otherNode := other.Node()
	if !DeepEqual(
		thatNode,
		otherNode,
	) {
		return false
	}

	thatSomething := that.Something()
	otherSomething := other.Something()
	if !DeepEqual(
		thatSomething,
		otherSomething,
	) {
		return false
	}

	return true
}

func DeepEqual(
	that aastypes.IClass,
	other aastypes.IClass,
) bool {
	if that.ModelType() != other.ModelType() {
		return false
	}

	switch that.ModelType() {
	case aastypes.ModelTypeBranch:
		return deepEqualBranch(
			that.(aastypes.IBranch),
			other.(aastypes.IBranch),
		)
	case aastypes.ModelTypeLeaf:
		return deepEqualLeaf(
			that.(aastypes.ILeaf),
			other.(aastypes.ILeaf),
		)
	case aastypes.ModelTypeBlossom:
		return deepEqualBlossom(
			that.(aastypes.IBlossom),
			other.(aastypes.IBlossom),
		)
	case aastypes.ModelTypeSomething:
		return deepEqualSomething(
			that.(aastypes.ISomething),
			other.(aastypes.ISomething),
		)
	case aastypes.ModelTypeContainer:
		return deepEqualContainer(
			that.(aastypes.IContainer),
			other.(aastypes.IContainer),
		)
	}

	panic(fmt.Sprintf("Unexpected model type: %d", that.ModelType()))
}

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.
