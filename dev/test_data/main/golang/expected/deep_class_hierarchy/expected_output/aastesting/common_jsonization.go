package aastesting

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

import (
	"fmt"
	"path"
	aasjsonization "github.com/dummy-works/dummy/jsonization"
	aastypes "github.com/dummy-works/dummy/types"
)

// Load a maximal example of [aastypes.IBranch] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMaximalBranch(
) (result aastypes.IBranch) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"Branch",
		"maximal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.BranchFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IBranch " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IBranch)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IBranch at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a minimal example of [aastypes.IBranch] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMinimalBranch(
) (result aastypes.IBranch) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"Branch",
		"minimal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.BranchFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IBranch " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IBranch)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IBranch at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a maximal example of [aastypes.ILeaf] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMaximalLeaf(
) (result aastypes.ILeaf) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"Leaf",
		"maximal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.LeafFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of ILeaf " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.ILeaf)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of ILeaf at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a minimal example of [aastypes.ILeaf] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMinimalLeaf(
) (result aastypes.ILeaf) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"Leaf",
		"minimal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.LeafFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of ILeaf " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.ILeaf)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of ILeaf at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a maximal example of [aastypes.IBlossom] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMaximalBlossom(
) (result aastypes.IBlossom) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"Blossom",
		"maximal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.BlossomFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IBlossom " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IBlossom)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IBlossom at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a minimal example of [aastypes.IBlossom] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMinimalBlossom(
) (result aastypes.IBlossom) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"Blossom",
		"minimal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.BlossomFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IBlossom " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IBlossom)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IBlossom at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a maximal example of [aastypes.ISomething] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMaximalSomething(
) (result aastypes.ISomething) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"Something",
		"maximal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.SomethingFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of ISomething " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.ISomething)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of ISomething at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a minimal example of [aastypes.ISomething] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMinimalSomething(
) (result aastypes.ISomething) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"Something",
		"minimal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.SomethingFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of ISomething " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.ISomething)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of ISomething at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a maximal example of [aastypes.IContainer] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMaximalContainer(
) (result aastypes.IContainer) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"Container",
		"maximal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.ContainerFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IContainer " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IContainer)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IContainer at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// Load a minimal example of [aastypes.IContainer] from
// the test data directory.
//
// If there is any error, panic.
func MustLoadMinimalContainer(
) (result aastypes.IContainer) {
	pth := path.Join(
		TestDataDir,
		"Json",
		"Expected",
		"Container",
		"minimal.json",
	)

	jsonable := MustReadJsonable(pth)

	instance, err := aasjsonization.ContainerFromJsonable(
		jsonable,
	)
	if err != nil {
		panic(
			fmt.Sprintf(
				"Failed to de-serialize an instance of IContainer " +
				"from %s: %s",
				pth, err.Error(),
			),
		)
	}
	var ok bool
	result, ok = instance.(aastypes.IContainer)
	if !ok {
		panic(
			fmt.Sprintf(
				"Expected to find an instance of IContainer at %s, " +
				"but got an instance of %T: %v",
				pth, instance, instance,
			),
		)
	}
	return
}

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.
