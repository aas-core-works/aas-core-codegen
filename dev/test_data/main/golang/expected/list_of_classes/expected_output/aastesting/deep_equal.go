package aastesting

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

import (
	"bytes"
	"fmt"
	aastypes "github.com/dummy-works/dummy/types"
)

// Perform a comparison for deep equality between `that` and `other` instance.
//
// The deep equality means that all the properties are checked for equality recursively.
func deepEqualSomeItem(
	that aastypes.ISomeItem,
	other aastypes.ISomeItem,
) bool {
	thatName := that.Name()
	otherName := other.Name()
	if thatName != otherName {
		return false
	}

	return true
}

// Perform a comparison for deep equality between `that` and `other` instance.
//
// The deep equality means that all the properties are checked for equality recursively.
func deepEqualAnotherItem(
	that aastypes.IAnotherItem,
	other aastypes.IAnotherItem,
) bool {
	thatSerialNumber := that.SerialNumber()
	otherSerialNumber := other.SerialNumber()
	if thatSerialNumber != otherSerialNumber {
		return false
	}

	return true
}

// Perform a comparison for deep equality between `that` and `other` instance.
//
// The deep equality means that all the properties are checked for equality recursively.
func deepEqualSimple(
	that aastypes.ISimple,
	other aastypes.ISimple,
) bool {
	thatName := that.Name()
	otherName := other.Name()
	if thatName != otherName {
		return false
	}

	return true
}

// Perform a comparison for deep equality between `that` and `other` instance.
//
// The deep equality means that all the properties are checked for equality recursively.
func deepEqualSomething(
	that aastypes.ISomething,
	other aastypes.ISomething,
) bool {
	thatSomeItems := that.SomeItems()
	otherSomeItems := other.SomeItems()
	if 
		len(thatSomeItems) !=
		len(otherSomeItems) {
		return false
	}
	for i := range thatSomeItems {
		if !DeepEqual(
			thatSomeItems[i],
			otherSomeItems[i],
		) {
			return false
		}
	}

	thatSomeSimples := that.SomeSimples()
	otherSomeSimples := other.SomeSimples()
	if 
		len(thatSomeSimples) !=
		len(otherSomeSimples) {
		return false
	}
	for i := range thatSomeSimples {
		if !DeepEqual(
			thatSomeSimples[i],
			otherSomeSimples[i],
		) {
			return false
		}
	}

	return true
}

func DeepEqual(
	that aastypes.IClass,
	other aastypes.IClass,
) bool {
	if that.ModelType() != other.ModelType() {
		return false
	}

	switch that.ModelType() {
	case aastypes.ModelTypeSomeItem:
		return deepEqualSomeItem(
			that.(aastypes.ISomeItem),
			other.(aastypes.ISomeItem),
		)
	case aastypes.ModelTypeAnotherItem:
		return deepEqualAnotherItem(
			that.(aastypes.IAnotherItem),
			other.(aastypes.IAnotherItem),
		)
	case aastypes.ModelTypeSimple:
		return deepEqualSimple(
			that.(aastypes.ISimple),
			other.(aastypes.ISimple),
		)
	case aastypes.ModelTypeSomething:
		return deepEqualSomething(
			that.(aastypes.ISomething),
			other.(aastypes.ISomething),
		)
	}

	panic(fmt.Sprintf("Unexpected model type: %d", that.ModelType()))
}

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.
