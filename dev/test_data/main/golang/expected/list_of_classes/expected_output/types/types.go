// Package types provides the data structures corresponding to the meta-model.
package types

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

// Enumerate the model types for faster type switches.
//
// For example, you can use an array of function pointers to
// implement such a switch.
type ModelType int
const (
	ModelTypeSomeItem ModelType = iota
	ModelTypeAnotherItem
	ModelTypeSimple
	ModelTypeSomething
)

// Represent the most general interface of an AAS model.
type IClass interface {
	// Return the concrete model type at run-time.
	//
	// Use the model type if you want to switch on the concrete model type
	// in efficient manner, as the compiler will most probably implement
	// the switch in form of a jump table.
	ModelType() ModelType

	// Apply the action on the instances referenced from this instance.
	//
	// If any of the actions returns abort `true`, the descent is immediately
	// stopped,  and abort `true` is also returned. Otherwise, return abort `false`.
	//
	// We do not recurse into the referenced instances.
	//
	// The action is not applied on this instance.
	DescendOnce(action func(IClass) bool) (abort bool)

	// Apply the action recursively on the instances referenced from this instance.
	//
	// If any of the actions returns abort `true`, the descent is immediately
	// stopped,  and abort `true` is also returned. Otherwise, return abort `false`.
	//
	// The action is not applied on this instance.
	Descend(action func(IClass) bool) (abort bool)
}

type IAbstractItem interface {
	IClass
}

// Check whether the instance corresponds to [aastypes.IAbstractItem]
// based on its run-time model type.
//
// The implementation uses a switch statements which is
// most probably compiled as an efficient jump table by the compiler.
func IsAbstractItem(
	that IClass,
) (ok bool) {
	switch that.ModelType() {
	case ModelTypeSomeItem:
		ok = true
	case ModelTypeAnotherItem:
		ok = true
	}
	return
}

type ISomeItem interface {
	IAbstractItem

	Name() string;

	SetName(
		value string,
	);
}

// Check whether the instance corresponds to [aastypes.ISomeItem]
// based on its run-time model type.
//
// The implementation uses a switch statements which is
// most probably compiled as an efficient jump table by the compiler.
func IsSomeItem(
	that IClass,
) (ok bool) {
	ok = that.ModelType() == ModelTypeSomeItem
	return
}

// Implements ISomeItem.
type SomeItem struct {
	name string
}

func (si *SomeItem) Name(
) string {
	return si.name
}

func (si *SomeItem) SetName(
	value string,
) {
	si.name = value
}

func (si *SomeItem) ModelType(
) ModelType {
	return ModelTypeSomeItem
}

// Apply the action on the instances referenced from si.
//
// If any of the actions returns abort `true`, the descent is immediately
// stopped,  and abort `true` is also returned. Otherwise, return abort `false`.
//
// We do not recurse into the referenced instances.
//
// The action is not applied on si.
func (si *SomeItem) DescendOnce(
	action func(IClass) bool,
) (abort bool) {
	// No descendable properties

	return
}

// Apply the action recursively on the instances referenced from si.
//
// If any of the actions returns abort `true`, the descent is immediately
// stopped,  and abort `true` is also returned. Otherwise, return abort `false`.
//
// The action is not applied on si.
func (si *SomeItem) Descend(
	action func(IClass) bool,
) (abort bool) {
	// No descendable properties

	return
}

// Create a new instance of SomeItem with
// the given properties.
func NewSomeItem(
	name string,
) *SomeItem {
	return &SomeItem{
		name: name,
	}
}

type IAnotherItem interface {
	IAbstractItem

	SerialNumber() int64;

	SetSerialNumber(
		value int64,
	);
}

// Check whether the instance corresponds to [aastypes.IAnotherItem]
// based on its run-time model type.
//
// The implementation uses a switch statements which is
// most probably compiled as an efficient jump table by the compiler.
func IsAnotherItem(
	that IClass,
) (ok bool) {
	ok = that.ModelType() == ModelTypeAnotherItem
	return
}

// Implements IAnotherItem.
type AnotherItem struct {
	serialNumber int64
}

func (ai *AnotherItem) SerialNumber(
) int64 {
	return ai.serialNumber
}

func (ai *AnotherItem) SetSerialNumber(
	value int64,
) {
	ai.serialNumber = value
}

func (ai *AnotherItem) ModelType(
) ModelType {
	return ModelTypeAnotherItem
}

// Apply the action on the instances referenced from ai.
//
// If any of the actions returns abort `true`, the descent is immediately
// stopped,  and abort `true` is also returned. Otherwise, return abort `false`.
//
// We do not recurse into the referenced instances.
//
// The action is not applied on ai.
func (ai *AnotherItem) DescendOnce(
	action func(IClass) bool,
) (abort bool) {
	// No descendable properties

	return
}

// Apply the action recursively on the instances referenced from ai.
//
// If any of the actions returns abort `true`, the descent is immediately
// stopped,  and abort `true` is also returned. Otherwise, return abort `false`.
//
// The action is not applied on ai.
func (ai *AnotherItem) Descend(
	action func(IClass) bool,
) (abort bool) {
	// No descendable properties

	return
}

// Create a new instance of AnotherItem with
// the given properties.
func NewAnotherItem(
	serialNumber int64,
) *AnotherItem {
	return &AnotherItem{
		serialNumber: serialNumber,
	}
}

type ISimple interface {
	IClass

	Name() string;

	SetName(
		value string,
	);
}

// Check whether the instance corresponds to [aastypes.ISimple]
// based on its run-time model type.
//
// The implementation uses a switch statements which is
// most probably compiled as an efficient jump table by the compiler.
func IsSimple(
	that IClass,
) (ok bool) {
	ok = that.ModelType() == ModelTypeSimple
	return
}

// Implements ISimple.
type Simple struct {
	name string
}

func (s *Simple) Name(
) string {
	return s.name
}

func (s *Simple) SetName(
	value string,
) {
	s.name = value
}

func (s *Simple) ModelType(
) ModelType {
	return ModelTypeSimple
}

// Apply the action on the instances referenced from s.
//
// If any of the actions returns abort `true`, the descent is immediately
// stopped,  and abort `true` is also returned. Otherwise, return abort `false`.
//
// We do not recurse into the referenced instances.
//
// The action is not applied on s.
func (s *Simple) DescendOnce(
	action func(IClass) bool,
) (abort bool) {
	// No descendable properties

	return
}

// Apply the action recursively on the instances referenced from s.
//
// If any of the actions returns abort `true`, the descent is immediately
// stopped,  and abort `true` is also returned. Otherwise, return abort `false`.
//
// The action is not applied on s.
func (s *Simple) Descend(
	action func(IClass) bool,
) (abort bool) {
	// No descendable properties

	return
}

// Create a new instance of Simple with
// the given properties.
func NewSimple(
	name string,
) *Simple {
	return &Simple{
		name: name,
	}
}

type ISomething interface {
	IClass

	SomeItems() []IAbstractItem;

	SetSomeItems(
		value []IAbstractItem,
	);

	SomeSimples() []ISimple;

	SetSomeSimples(
		value []ISimple,
	);
}

// Check whether the instance corresponds to [aastypes.ISomething]
// based on its run-time model type.
//
// The implementation uses a switch statements which is
// most probably compiled as an efficient jump table by the compiler.
func IsSomething(
	that IClass,
) (ok bool) {
	ok = that.ModelType() == ModelTypeSomething
	return
}

// Implements ISomething.
type Something struct {
	someItems []IAbstractItem
	someSimples []ISimple
}

func (s *Something) SomeItems(
) []IAbstractItem {
	return s.someItems
}

func (s *Something) SetSomeItems(
	value []IAbstractItem,
) {
	s.someItems = value
}

func (s *Something) SomeSimples(
) []ISimple {
	return s.someSimples
}

func (s *Something) SetSomeSimples(
	value []ISimple,
) {
	s.someSimples = value
}

func (s *Something) ModelType(
) ModelType {
	return ModelTypeSomething
}

// Apply the action on the instances referenced from s.
//
// If any of the actions returns abort `true`, the descent is immediately
// stopped,  and abort `true` is also returned. Otherwise, return abort `false`.
//
// We do not recurse into the referenced instances.
//
// The action is not applied on s.
func (s *Something) DescendOnce(
	action func(IClass) bool,
) (abort bool) {
	for _, v := range s.someItems {
		abort = action(v);
		if abort {
			return
		}
	}

	for _, v1 := range s.someSimples {
		abort = action(v1);
		if abort {
			return
		}
	}

	return
}

// Apply the action recursively on the instances referenced from s.
//
// If any of the actions returns abort `true`, the descent is immediately
// stopped,  and abort `true` is also returned. Otherwise, return abort `false`.
//
// The action is not applied on s.
func (s *Something) Descend(
	action func(IClass) bool,
) (abort bool) {
	for _, v := range s.someItems {
		abort = action(v);
		if abort {
			return
		}

		abort = v.Descend(
			action,
		);
		if abort {
			return
		}
	}

	for _, v1 := range s.someSimples {
		abort = action(v1);
		if abort {
			return
		}

		abort = v1.Descend(
			action,
		);
		if abort {
			return
		}
	}

	return
}

// Create a new instance of Something with
// the given properties.
func NewSomething(
	someItems []IAbstractItem,
	someSimples []ISimple,
) *Something {
	return &Something{
		someItems: someItems,
		someSimples: someSimples,
	}
}

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.
