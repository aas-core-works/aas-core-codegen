/*
 * This code has been automatically generated by aas-core-codegen.
 * Do NOT edit or append.
 */

package dummy.tests;

import static org.junit.jupiter.api.Assertions.assertTrue;

import dummy.copying.Copying;
import dummy.types.impl.*;
import dummy.types.model.*;
import dummy.types.model.IClass;
import dummy.visitation.AbstractTransformerWithContext;
import java.io.IOException;
import java.util.*;
import java.util.stream.Stream;
import java.util.stream.StreamSupport;
import org.junit.jupiter.api.Test;

public class TestCopying {
  private static class _DeepEqualiser extends AbstractTransformerWithContext<IClass, Boolean> {
    @Override
    public Boolean transformBranch(IBranch that, IClass other) {
      if (!(other instanceof Branch)) {
        return false;
      }

      Branch casted = (Branch) that;

      return (
        that.getIdentifier().equals(casted.getIdentifier())
        && that.getDescription().equals(casted.getDescription()));
    }

    @Override
    public Boolean transformLeaf(ILeaf that, IClass other) {
      if (!(other instanceof Leaf)) {
        return false;
      }

      Leaf casted = (Leaf) that;

      return (
        that.getIdentifier().equals(casted.getIdentifier())
        && that.getDescription().equals(casted.getDescription())
        && that.getValue().equals(casted.getValue()));
    }

    @Override
    public Boolean transformBlossom(IBlossom that, IClass other) {
      if (!(other instanceof Blossom)) {
        return false;
      }

      Blossom casted = (Blossom) that;

      return (
        that.getIdentifier().equals(casted.getIdentifier())
        && that.getDescription().equals(casted.getDescription())
        && that.getValue().equals(casted.getValue())
        && that.getDetails().equals(casted.getDetails()));
    }

    @Override
    public Boolean transformSomething(ISomething that, IClass other) {
      if (!(other instanceof Something)) {
        return false;
      }

      Something casted = (Something) that;

      return (
        transform(
          that.getSomeChoice(),
          casted.getSomeChoice())
        && transform(
          that.getSomethingWithoutChoice(),
          casted.getSomethingWithoutChoice()));
    }

    @Override
    public Boolean transformContainer(IContainer that, IClass other) {
      if (!(other instanceof Container)) {
        return false;
      }

      Container casted = (Container) that;

      return (
        transform(
          that.getNode(),
          casted.getNode())
        && transform(
          that.getSomething(),
          casted.getSomething()));
    }
  } // class _DeepEqualiser

  private static final _DeepEqualiser DeepEqualiserInstance = new _DeepEqualiser();

  /**
   * Compare two byte spans for equal content.
   */
  private static Boolean byteSpansEqual(byte[] that, byte[] other) {
    return that.equals(other);
  }

  private static class _Pair<A, B> {
    private final A first;
    private final B second;
    
    public _Pair(A first, B second) {
      this.first = first;
      this.second = second;
    }
    
    public A getFirst() {
      return first;
    }
    
    public B getSecond() {
      return second;
    }
  }

  // Java 8 doesn't provide a zip operation out of the box, so we have to ship our own.
  // Adapted from: https://stackoverflow.com/a/23529010
  private static <A, B> Stream<_Pair<A, B>> zip(
    Stream<? extends A> a,
    Stream<? extends B> b) {
    Spliterator<? extends A> aSplit = Objects.requireNonNull(a).spliterator();
    Spliterator<? extends B> bSplit = Objects.requireNonNull(b).spliterator();
    
    int characteristics = aSplit.characteristics() & bSplit.characteristics() &
      ~(Spliterator.DISTINCT | Spliterator.SORTED);
    
    long zipSize = ((characteristics & Spliterator.SIZED) != 0)
      ? Math.min(aSplit.getExactSizeIfKnown(), bSplit.getExactSizeIfKnown())
      : -1;
    
    Iterator<A> aIter = Spliterators.iterator(aSplit);
    Iterator<B> bIter = Spliterators.iterator(bSplit);
    Iterator<_Pair<A, B>> cIter = new Iterator<_Pair<A, B>>() {
      @Override
      public boolean hasNext() {
        return aIter.hasNext() && bIter.hasNext();
      }
      
      @Override
      public _Pair<A, B> next() {
        return new _Pair<>(aIter.next(), bIter.next());
      }
    };
    
    Spliterator<_Pair<A, B>> split = Spliterators.spliterator(cIter, zipSize, characteristics);
    return StreamSupport.stream(split, false);
  }

  private static Boolean BranchShallowEquals(
    Branch that,
    Branch other) {
    return (
      that.getIdentifier().equals(other.getIdentifier())
      && that.getDescription().equals(other.getDescription()));
  }

  private static Boolean LeafShallowEquals(
    Leaf that,
    Leaf other) {
    return (
      that.getIdentifier().equals(other.getIdentifier())
      && that.getDescription().equals(other.getDescription())
      && that.getValue().equals(other.getValue()));
  }

  private static Boolean BlossomShallowEquals(
    Blossom that,
    Blossom other) {
    return (
      that.getIdentifier().equals(other.getIdentifier())
      && that.getDescription().equals(other.getDescription())
      && that.getValue().equals(other.getValue())
      && that.getDetails().equals(other.getDetails()));
  }

  private static Boolean SomethingShallowEquals(
    Something that,
    Something other) {
    return (
      that.getSomeChoice().equals(other.getSomeChoice())
      && that.getSomethingWithoutChoice().equals(other.getSomethingWithoutChoice()));
  }

  private static Boolean ContainerShallowEquals(
    Container that,
    Container other) {
    return (
      that.getNode().equals(other.getNode())
      && that.getSomething().equals(other.getSomething()));
  }

  private static Boolean BranchDeepEquals(Branch that, Branch other) {
    return DeepEqualiserInstance.transform(that, other);
  }

  private static Boolean LeafDeepEquals(Leaf that, Leaf other) {
    return DeepEqualiserInstance.transform(that, other);
  }

  private static Boolean BlossomDeepEquals(Blossom that, Blossom other) {
    return DeepEqualiserInstance.transform(that, other);
  }

  private static Boolean SomethingDeepEquals(Something that, Something other) {
    return DeepEqualiserInstance.transform(that, other);
  }

  private static Boolean ContainerDeepEquals(Container that, Container other) {
    return DeepEqualiserInstance.transform(that, other);
  }

  @Test
  public void testBranchShallowCopy() throws IOException {
    final Branch instance = CommonJsonization.loadMaximalBranch();
    final Branch instanceCopy = Copying.shallow(instance);

    assertTrue(
      BranchShallowEquals(instance, instanceCopy),
      "Branch");
  } // public void testBranchShallowCopy

  @Test
  public void testBranchDeepCopy() throws IOException {
    final Branch instance = CommonJsonization.loadMaximalBranch();
    final Branch instanceCopy = Copying.deep(instance);

    assertTrue(
      BranchDeepEquals(instance, instanceCopy),
      "Branch");
  } // public void testBranchDeepCopy

  @Test
  public void testLeafShallowCopy() throws IOException {
    final Leaf instance = CommonJsonization.loadMaximalLeaf();
    final Leaf instanceCopy = Copying.shallow(instance);

    assertTrue(
      LeafShallowEquals(instance, instanceCopy),
      "Leaf");
  } // public void testLeafShallowCopy

  @Test
  public void testLeafDeepCopy() throws IOException {
    final Leaf instance = CommonJsonization.loadMaximalLeaf();
    final Leaf instanceCopy = Copying.deep(instance);

    assertTrue(
      LeafDeepEquals(instance, instanceCopy),
      "Leaf");
  } // public void testLeafDeepCopy

  @Test
  public void testBlossomShallowCopy() throws IOException {
    final Blossom instance = CommonJsonization.loadMaximalBlossom();
    final Blossom instanceCopy = Copying.shallow(instance);

    assertTrue(
      BlossomShallowEquals(instance, instanceCopy),
      "Blossom");
  } // public void testBlossomShallowCopy

  @Test
  public void testBlossomDeepCopy() throws IOException {
    final Blossom instance = CommonJsonization.loadMaximalBlossom();
    final Blossom instanceCopy = Copying.deep(instance);

    assertTrue(
      BlossomDeepEquals(instance, instanceCopy),
      "Blossom");
  } // public void testBlossomDeepCopy

  @Test
  public void testSomethingShallowCopy() throws IOException {
    final Something instance = CommonJsonization.loadMaximalSomething();
    final Something instanceCopy = Copying.shallow(instance);

    assertTrue(
      SomethingShallowEquals(instance, instanceCopy),
      "Something");
  } // public void testSomethingShallowCopy

  @Test
  public void testSomethingDeepCopy() throws IOException {
    final Something instance = CommonJsonization.loadMaximalSomething();
    final Something instanceCopy = Copying.deep(instance);

    assertTrue(
      SomethingDeepEquals(instance, instanceCopy),
      "Something");
  } // public void testSomethingDeepCopy

  @Test
  public void testContainerShallowCopy() throws IOException {
    final Container instance = CommonJsonization.loadMaximalContainer();
    final Container instanceCopy = Copying.shallow(instance);

    assertTrue(
      ContainerShallowEquals(instance, instanceCopy),
      "Container");
  } // public void testContainerShallowCopy

  @Test
  public void testContainerDeepCopy() throws IOException {
    final Container instance = CommonJsonization.loadMaximalContainer();
    final Container instanceCopy = Copying.deep(instance);

    assertTrue(
      ContainerDeepEquals(instance, instanceCopy),
      "Container");
  } // public void testContainerDeepCopy
} // class TestCopying

// package dummy.tests

/*
 * This code has been automatically generated by aas-core-codegen.
 * Do NOT edit or append.
 */
