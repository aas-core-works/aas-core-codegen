/*
 * This code has been automatically generated by aas-core-codegen.
 * Do NOT edit or append.
 */

package dummy.stringification;

import java.util.Arrays;
import java.util.Collections;
import java.util.HashMap;
import java.util.Map;
import java.util.Optional;
import dummy.types.enums.*;

public class Stringification {
  private static final Map<Result, String> resultToString;
  static {
    final Map<Result, String> temp = new HashMap<>();

    temp.put(Result.OK, "ok");
    temp.put(Result.FAIL, "fail");

    if (!temp.keySet().containsAll(Arrays.asList(Result.values()))) {
      throw new IllegalStateException("Unmapped Result");
    }

    resultToString = Collections.unmodifiableMap(temp);
  }

  /**
   * Retrieve the string representation of {@code that}.
   *
   * <p>If {@code that} is not a valid literal, return {@code Optional#empty()}.
   */
  public static Optional<String> toString(Result that)
  {
    return Optional.ofNullable(that).map(resultToString::get);
  }

  /**
   * Retrieve the string representation of {@code that}.
   *
   * @throws IllegalArgumentException if {@code that} is not a valid literal
   */
  public static String mustToString(Result that)
  {
    final Optional<String> text = toString(that);
    if (!text.isPresent()) {
      throw new IllegalArgumentException("Invalid literal of Result: " + that);
    }
    return text.get();
  }

  private static final Map<String, Result> resultFromString;
  static {
    final Map<String, Result> temp = new HashMap<>();

    temp.put("ok", Result.OK);
    temp.put("fail", Result.FAIL);

    if (!temp.values().containsAll(Arrays.asList(Result.values()))) {
      throw new IllegalStateException("Unmapped Result");
    }

    resultFromString = Collections.unmodifiableMap(temp);
  }

  /**
   * Parse the string representation of {@link Result}.
   *
   * <p>If {@code text} is not a valid string representation
   * of a literal of {@link Result},
   * return {@code Optional#empty()}.
   */
  public static Optional<Result> resultFromString(String text)
  {
    Result value = resultFromString.get(text);
    if (value == null) {
      return Optional.empty();
    } else {
      return Optional.of(value);
    }
  }
}

/*
 * This code has been automatically generated by aas-core-codegen.
 * Do NOT edit or append.
 */
