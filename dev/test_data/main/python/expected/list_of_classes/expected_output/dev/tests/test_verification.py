"""Test the verification of instances."""


# This code has been automatically generated by aas-core-codegen.
# Do NOT edit or append.


# pylint: disable=missing-docstring


import json
import unittest


import dummy.jsonization as aas_jsonization
import dummy.verification as aas_verification


import tests.common


class TestVerificationOfValidInstances(unittest.TestCase):
    def test_some_item(self) -> None:
        for path in sorted(
            (
                tests.common.TEST_DATA_DIR
                / "Json"
                / "Expected"
                / 'SomeItem'
            ).glob("**/*.json")
        ):
            with path.open("rt") as fid:
                jsonable = json.load(fid)

            instance = aas_jsonization.some_item_from_jsonable(jsonable)

            errors = list(aas_verification.verify(instance))

            if len(errors) > 0:
                self.fail(
                    f"Expected no errors when verifying the instance de-serialized "
                    f"from {path}, but got {len(errors)} error(s):\n"
                    + "\n".join(
                        f"{error.path}: {error.cause}" for error in errors
                    )
                )

    def test_another_item(self) -> None:
        for path in sorted(
            (
                tests.common.TEST_DATA_DIR
                / "Json"
                / "Expected"
                / 'AnotherItem'
            ).glob("**/*.json")
        ):
            with path.open("rt") as fid:
                jsonable = json.load(fid)

            instance = aas_jsonization.another_item_from_jsonable(jsonable)

            errors = list(aas_verification.verify(instance))

            if len(errors) > 0:
                self.fail(
                    f"Expected no errors when verifying the instance de-serialized "
                    f"from {path}, but got {len(errors)} error(s):\n"
                    + "\n".join(
                        f"{error.path}: {error.cause}" for error in errors
                    )
                )

    def test_simple(self) -> None:
        for path in sorted(
            (
                tests.common.TEST_DATA_DIR
                / "Json"
                / "Expected"
                / 'Simple'
            ).glob("**/*.json")
        ):
            with path.open("rt") as fid:
                jsonable = json.load(fid)

            instance = aas_jsonization.simple_from_jsonable(jsonable)

            errors = list(aas_verification.verify(instance))

            if len(errors) > 0:
                self.fail(
                    f"Expected no errors when verifying the instance de-serialized "
                    f"from {path}, but got {len(errors)} error(s):\n"
                    + "\n".join(
                        f"{error.path}: {error.cause}" for error in errors
                    )
                )

    def test_something(self) -> None:
        for path in sorted(
            (
                tests.common.TEST_DATA_DIR
                / "Json"
                / "Expected"
                / 'Something'
            ).glob("**/*.json")
        ):
            with path.open("rt") as fid:
                jsonable = json.load(fid)

            instance = aas_jsonization.something_from_jsonable(jsonable)

            errors = list(aas_verification.verify(instance))

            if len(errors) > 0:
                self.fail(
                    f"Expected no errors when verifying the instance de-serialized "
                    f"from {path}, but got {len(errors)} error(s):\n"
                    + "\n".join(
                        f"{error.path}: {error.cause}" for error in errors
                    )
                )


class TestVerificationOfInvalidInstances(unittest.TestCase):
    def test_some_item(self) -> None:
        for cause_dir in sorted(
            (
                tests.common.TEST_DATA_DIR
                / "Json"
                / "Unexpected"
                / "Invalid"
            ).iterdir()
        ):
            for path in sorted(
                (cause_dir / 'SomeItem').glob("**/*.json")
            ):
                rel_path = path.relative_to(tests.common.TEST_DATA_DIR)

                expected_path = (
                    tests.common.TEST_DATA_DIR
                    / "VerificationError"
                    / rel_path.parent
                    / f"{rel_path.name}.errors"
                )

                with path.open("rt") as fid:
                    jsonable = json.load(fid)

                instance = aas_jsonization.some_item_from_jsonable(jsonable)

                errors = list(aas_verification.verify(instance))

                if len(errors) == 0:
                    self.fail(
                        f"Expected at least one verification error "
                        f"when verifying the instance de-serialized "
                        f"from {path}, but got none"
                    )

                got = "\n".join(
                    f"{error.path}: {error.cause}" for error in errors
                ) + "\n"

                tests.common.record_or_check(expected_path, got)

    def test_another_item(self) -> None:
        for cause_dir in sorted(
            (
                tests.common.TEST_DATA_DIR
                / "Json"
                / "Unexpected"
                / "Invalid"
            ).iterdir()
        ):
            for path in sorted(
                (cause_dir / 'AnotherItem').glob("**/*.json")
            ):
                rel_path = path.relative_to(tests.common.TEST_DATA_DIR)

                expected_path = (
                    tests.common.TEST_DATA_DIR
                    / "VerificationError"
                    / rel_path.parent
                    / f"{rel_path.name}.errors"
                )

                with path.open("rt") as fid:
                    jsonable = json.load(fid)

                instance = aas_jsonization.another_item_from_jsonable(jsonable)

                errors = list(aas_verification.verify(instance))

                if len(errors) == 0:
                    self.fail(
                        f"Expected at least one verification error "
                        f"when verifying the instance de-serialized "
                        f"from {path}, but got none"
                    )

                got = "\n".join(
                    f"{error.path}: {error.cause}" for error in errors
                ) + "\n"

                tests.common.record_or_check(expected_path, got)

    def test_simple(self) -> None:
        for cause_dir in sorted(
            (
                tests.common.TEST_DATA_DIR
                / "Json"
                / "Unexpected"
                / "Invalid"
            ).iterdir()
        ):
            for path in sorted(
                (cause_dir / 'Simple').glob("**/*.json")
            ):
                rel_path = path.relative_to(tests.common.TEST_DATA_DIR)

                expected_path = (
                    tests.common.TEST_DATA_DIR
                    / "VerificationError"
                    / rel_path.parent
                    / f"{rel_path.name}.errors"
                )

                with path.open("rt") as fid:
                    jsonable = json.load(fid)

                instance = aas_jsonization.simple_from_jsonable(jsonable)

                errors = list(aas_verification.verify(instance))

                if len(errors) == 0:
                    self.fail(
                        f"Expected at least one verification error "
                        f"when verifying the instance de-serialized "
                        f"from {path}, but got none"
                    )

                got = "\n".join(
                    f"{error.path}: {error.cause}" for error in errors
                ) + "\n"

                tests.common.record_or_check(expected_path, got)

    def test_something(self) -> None:
        for cause_dir in sorted(
            (
                tests.common.TEST_DATA_DIR
                / "Json"
                / "Unexpected"
                / "Invalid"
            ).iterdir()
        ):
            for path in sorted(
                (cause_dir / 'Something').glob("**/*.json")
            ):
                rel_path = path.relative_to(tests.common.TEST_DATA_DIR)

                expected_path = (
                    tests.common.TEST_DATA_DIR
                    / "VerificationError"
                    / rel_path.parent
                    / f"{rel_path.name}.errors"
                )

                with path.open("rt") as fid:
                    jsonable = json.load(fid)

                instance = aas_jsonization.something_from_jsonable(jsonable)

                errors = list(aas_verification.verify(instance))

                if len(errors) == 0:
                    self.fail(
                        f"Expected at least one verification error "
                        f"when verifying the instance de-serialized "
                        f"from {path}, but got none"
                    )

                got = "\n".join(
                    f"{error.path}: {error.cause}" for error in errors
                ) + "\n"

                tests.common.record_or_check(expected_path, got)


if __name__ == "__main__":
    unittest.main()


# This code has been automatically generated by aas-core-codegen.
# Do NOT edit or append.
