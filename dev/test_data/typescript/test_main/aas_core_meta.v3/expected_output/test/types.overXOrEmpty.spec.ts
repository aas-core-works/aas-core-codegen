/**
 * Test `over*OrEmpty` methods.
 */

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

import * as TestCommonJsonization from "./commonJsonization";

test("Extension.overSupplementalSemanticIdsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalExtension();

  let count = 0;
  for (const _ of instance.overSupplementalSemanticIdsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.supplementalSemanticIds?.length ?? 0,
  );
});

test("Extension.overSupplementalSemanticIdsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalExtension();

  let count = 0;
  for (const _ of instance.overSupplementalSemanticIdsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.supplementalSemanticIds?.length ?? 0,
  );
});

test("Extension.overRefersToOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalExtension();

  let count = 0;
  for (const _ of instance.overRefersToOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.refersTo?.length ?? 0,
  );
});

test("Extension.overRefersToOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalExtension();

  let count = 0;
  for (const _ of instance.overRefersToOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.refersTo?.length ?? 0,
  );
});

test("AdministrativeInformation.overEmbeddedDataSpecificationsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalAdministrativeInformation();

  let count = 0;
  for (const _ of instance.overEmbeddedDataSpecificationsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.embeddedDataSpecifications?.length ?? 0,
  );
});

test("AdministrativeInformation.overEmbeddedDataSpecificationsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalAdministrativeInformation();

  let count = 0;
  for (const _ of instance.overEmbeddedDataSpecificationsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.embeddedDataSpecifications?.length ?? 0,
  );
});

test("Qualifier.overSupplementalSemanticIdsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalQualifier();

  let count = 0;
  for (const _ of instance.overSupplementalSemanticIdsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.supplementalSemanticIds?.length ?? 0,
  );
});

test("Qualifier.overSupplementalSemanticIdsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalQualifier();

  let count = 0;
  for (const _ of instance.overSupplementalSemanticIdsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.supplementalSemanticIds?.length ?? 0,
  );
});

test("AssetAdministrationShell.overExtensionsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalAssetAdministrationShell();

  let count = 0;
  for (const _ of instance.overExtensionsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.extensions?.length ?? 0,
  );
});

test("AssetAdministrationShell.overExtensionsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalAssetAdministrationShell();

  let count = 0;
  for (const _ of instance.overExtensionsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.extensions?.length ?? 0,
  );
});

test("AssetAdministrationShell.overDisplayNameOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalAssetAdministrationShell();

  let count = 0;
  for (const _ of instance.overDisplayNameOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.displayName?.length ?? 0,
  );
});

test("AssetAdministrationShell.overDisplayNameOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalAssetAdministrationShell();

  let count = 0;
  for (const _ of instance.overDisplayNameOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.displayName?.length ?? 0,
  );
});

test("AssetAdministrationShell.overDescriptionOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalAssetAdministrationShell();

  let count = 0;
  for (const _ of instance.overDescriptionOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.description?.length ?? 0,
  );
});

test("AssetAdministrationShell.overDescriptionOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalAssetAdministrationShell();

  let count = 0;
  for (const _ of instance.overDescriptionOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.description?.length ?? 0,
  );
});

test("AssetAdministrationShell.overEmbeddedDataSpecificationsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalAssetAdministrationShell();

  let count = 0;
  for (const _ of instance.overEmbeddedDataSpecificationsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.embeddedDataSpecifications?.length ?? 0,
  );
});

test("AssetAdministrationShell.overEmbeddedDataSpecificationsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalAssetAdministrationShell();

  let count = 0;
  for (const _ of instance.overEmbeddedDataSpecificationsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.embeddedDataSpecifications?.length ?? 0,
  );
});

test("AssetAdministrationShell.overSubmodelsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalAssetAdministrationShell();

  let count = 0;
  for (const _ of instance.overSubmodelsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.submodels?.length ?? 0,
  );
});

test("AssetAdministrationShell.overSubmodelsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalAssetAdministrationShell();

  let count = 0;
  for (const _ of instance.overSubmodelsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.submodels?.length ?? 0,
  );
});

test("AssetInformation.overSpecificAssetIdsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalAssetInformation();

  let count = 0;
  for (const _ of instance.overSpecificAssetIdsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.specificAssetIds?.length ?? 0,
  );
});

test("AssetInformation.overSpecificAssetIdsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalAssetInformation();

  let count = 0;
  for (const _ of instance.overSpecificAssetIdsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.specificAssetIds?.length ?? 0,
  );
});

test("SpecificAssetId.overSupplementalSemanticIdsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalSpecificAssetId();

  let count = 0;
  for (const _ of instance.overSupplementalSemanticIdsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.supplementalSemanticIds?.length ?? 0,
  );
});

test("SpecificAssetId.overSupplementalSemanticIdsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalSpecificAssetId();

  let count = 0;
  for (const _ of instance.overSupplementalSemanticIdsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.supplementalSemanticIds?.length ?? 0,
  );
});

test("Submodel.overExtensionsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalSubmodel();

  let count = 0;
  for (const _ of instance.overExtensionsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.extensions?.length ?? 0,
  );
});

test("Submodel.overExtensionsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalSubmodel();

  let count = 0;
  for (const _ of instance.overExtensionsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.extensions?.length ?? 0,
  );
});

test("Submodel.overDisplayNameOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalSubmodel();

  let count = 0;
  for (const _ of instance.overDisplayNameOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.displayName?.length ?? 0,
  );
});

test("Submodel.overDisplayNameOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalSubmodel();

  let count = 0;
  for (const _ of instance.overDisplayNameOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.displayName?.length ?? 0,
  );
});

test("Submodel.overDescriptionOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalSubmodel();

  let count = 0;
  for (const _ of instance.overDescriptionOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.description?.length ?? 0,
  );
});

test("Submodel.overDescriptionOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalSubmodel();

  let count = 0;
  for (const _ of instance.overDescriptionOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.description?.length ?? 0,
  );
});

test("Submodel.overSupplementalSemanticIdsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalSubmodel();

  let count = 0;
  for (const _ of instance.overSupplementalSemanticIdsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.supplementalSemanticIds?.length ?? 0,
  );
});

test("Submodel.overSupplementalSemanticIdsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalSubmodel();

  let count = 0;
  for (const _ of instance.overSupplementalSemanticIdsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.supplementalSemanticIds?.length ?? 0,
  );
});

test("Submodel.overQualifiersOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalSubmodel();

  let count = 0;
  for (const _ of instance.overQualifiersOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.qualifiers?.length ?? 0,
  );
});

test("Submodel.overQualifiersOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalSubmodel();

  let count = 0;
  for (const _ of instance.overQualifiersOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.qualifiers?.length ?? 0,
  );
});

test("Submodel.overEmbeddedDataSpecificationsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalSubmodel();

  let count = 0;
  for (const _ of instance.overEmbeddedDataSpecificationsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.embeddedDataSpecifications?.length ?? 0,
  );
});

test("Submodel.overEmbeddedDataSpecificationsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalSubmodel();

  let count = 0;
  for (const _ of instance.overEmbeddedDataSpecificationsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.embeddedDataSpecifications?.length ?? 0,
  );
});

test("Submodel.overSubmodelElementsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalSubmodel();

  let count = 0;
  for (const _ of instance.overSubmodelElementsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.submodelElements?.length ?? 0,
  );
});

test("Submodel.overSubmodelElementsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalSubmodel();

  let count = 0;
  for (const _ of instance.overSubmodelElementsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.submodelElements?.length ?? 0,
  );
});

test("RelationshipElement.overExtensionsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalRelationshipElement();

  let count = 0;
  for (const _ of instance.overExtensionsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.extensions?.length ?? 0,
  );
});

test("RelationshipElement.overExtensionsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalRelationshipElement();

  let count = 0;
  for (const _ of instance.overExtensionsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.extensions?.length ?? 0,
  );
});

test("RelationshipElement.overDisplayNameOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalRelationshipElement();

  let count = 0;
  for (const _ of instance.overDisplayNameOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.displayName?.length ?? 0,
  );
});

test("RelationshipElement.overDisplayNameOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalRelationshipElement();

  let count = 0;
  for (const _ of instance.overDisplayNameOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.displayName?.length ?? 0,
  );
});

test("RelationshipElement.overDescriptionOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalRelationshipElement();

  let count = 0;
  for (const _ of instance.overDescriptionOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.description?.length ?? 0,
  );
});

test("RelationshipElement.overDescriptionOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalRelationshipElement();

  let count = 0;
  for (const _ of instance.overDescriptionOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.description?.length ?? 0,
  );
});

test("RelationshipElement.overSupplementalSemanticIdsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalRelationshipElement();

  let count = 0;
  for (const _ of instance.overSupplementalSemanticIdsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.supplementalSemanticIds?.length ?? 0,
  );
});

test("RelationshipElement.overSupplementalSemanticIdsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalRelationshipElement();

  let count = 0;
  for (const _ of instance.overSupplementalSemanticIdsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.supplementalSemanticIds?.length ?? 0,
  );
});

test("RelationshipElement.overQualifiersOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalRelationshipElement();

  let count = 0;
  for (const _ of instance.overQualifiersOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.qualifiers?.length ?? 0,
  );
});

test("RelationshipElement.overQualifiersOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalRelationshipElement();

  let count = 0;
  for (const _ of instance.overQualifiersOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.qualifiers?.length ?? 0,
  );
});

test("RelationshipElement.overEmbeddedDataSpecificationsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalRelationshipElement();

  let count = 0;
  for (const _ of instance.overEmbeddedDataSpecificationsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.embeddedDataSpecifications?.length ?? 0,
  );
});

test("RelationshipElement.overEmbeddedDataSpecificationsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalRelationshipElement();

  let count = 0;
  for (const _ of instance.overEmbeddedDataSpecificationsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.embeddedDataSpecifications?.length ?? 0,
  );
});

test("SubmodelElementList.overExtensionsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalSubmodelElementList();

  let count = 0;
  for (const _ of instance.overExtensionsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.extensions?.length ?? 0,
  );
});

test("SubmodelElementList.overExtensionsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalSubmodelElementList();

  let count = 0;
  for (const _ of instance.overExtensionsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.extensions?.length ?? 0,
  );
});

test("SubmodelElementList.overDisplayNameOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalSubmodelElementList();

  let count = 0;
  for (const _ of instance.overDisplayNameOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.displayName?.length ?? 0,
  );
});

test("SubmodelElementList.overDisplayNameOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalSubmodelElementList();

  let count = 0;
  for (const _ of instance.overDisplayNameOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.displayName?.length ?? 0,
  );
});

test("SubmodelElementList.overDescriptionOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalSubmodelElementList();

  let count = 0;
  for (const _ of instance.overDescriptionOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.description?.length ?? 0,
  );
});

test("SubmodelElementList.overDescriptionOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalSubmodelElementList();

  let count = 0;
  for (const _ of instance.overDescriptionOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.description?.length ?? 0,
  );
});

test("SubmodelElementList.overSupplementalSemanticIdsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalSubmodelElementList();

  let count = 0;
  for (const _ of instance.overSupplementalSemanticIdsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.supplementalSemanticIds?.length ?? 0,
  );
});

test("SubmodelElementList.overSupplementalSemanticIdsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalSubmodelElementList();

  let count = 0;
  for (const _ of instance.overSupplementalSemanticIdsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.supplementalSemanticIds?.length ?? 0,
  );
});

test("SubmodelElementList.overQualifiersOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalSubmodelElementList();

  let count = 0;
  for (const _ of instance.overQualifiersOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.qualifiers?.length ?? 0,
  );
});

test("SubmodelElementList.overQualifiersOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalSubmodelElementList();

  let count = 0;
  for (const _ of instance.overQualifiersOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.qualifiers?.length ?? 0,
  );
});

test("SubmodelElementList.overEmbeddedDataSpecificationsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalSubmodelElementList();

  let count = 0;
  for (const _ of instance.overEmbeddedDataSpecificationsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.embeddedDataSpecifications?.length ?? 0,
  );
});

test("SubmodelElementList.overEmbeddedDataSpecificationsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalSubmodelElementList();

  let count = 0;
  for (const _ of instance.overEmbeddedDataSpecificationsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.embeddedDataSpecifications?.length ?? 0,
  );
});

test("SubmodelElementList.overValueOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalSubmodelElementList();

  let count = 0;
  for (const _ of instance.overValueOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.value?.length ?? 0,
  );
});

test("SubmodelElementList.overValueOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalSubmodelElementList();

  let count = 0;
  for (const _ of instance.overValueOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.value?.length ?? 0,
  );
});

test("SubmodelElementCollection.overExtensionsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalSubmodelElementCollection();

  let count = 0;
  for (const _ of instance.overExtensionsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.extensions?.length ?? 0,
  );
});

test("SubmodelElementCollection.overExtensionsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalSubmodelElementCollection();

  let count = 0;
  for (const _ of instance.overExtensionsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.extensions?.length ?? 0,
  );
});

test("SubmodelElementCollection.overDisplayNameOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalSubmodelElementCollection();

  let count = 0;
  for (const _ of instance.overDisplayNameOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.displayName?.length ?? 0,
  );
});

test("SubmodelElementCollection.overDisplayNameOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalSubmodelElementCollection();

  let count = 0;
  for (const _ of instance.overDisplayNameOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.displayName?.length ?? 0,
  );
});

test("SubmodelElementCollection.overDescriptionOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalSubmodelElementCollection();

  let count = 0;
  for (const _ of instance.overDescriptionOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.description?.length ?? 0,
  );
});

test("SubmodelElementCollection.overDescriptionOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalSubmodelElementCollection();

  let count = 0;
  for (const _ of instance.overDescriptionOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.description?.length ?? 0,
  );
});

test("SubmodelElementCollection.overSupplementalSemanticIdsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalSubmodelElementCollection();

  let count = 0;
  for (const _ of instance.overSupplementalSemanticIdsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.supplementalSemanticIds?.length ?? 0,
  );
});

test("SubmodelElementCollection.overSupplementalSemanticIdsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalSubmodelElementCollection();

  let count = 0;
  for (const _ of instance.overSupplementalSemanticIdsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.supplementalSemanticIds?.length ?? 0,
  );
});

test("SubmodelElementCollection.overQualifiersOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalSubmodelElementCollection();

  let count = 0;
  for (const _ of instance.overQualifiersOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.qualifiers?.length ?? 0,
  );
});

test("SubmodelElementCollection.overQualifiersOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalSubmodelElementCollection();

  let count = 0;
  for (const _ of instance.overQualifiersOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.qualifiers?.length ?? 0,
  );
});

test("SubmodelElementCollection.overEmbeddedDataSpecificationsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalSubmodelElementCollection();

  let count = 0;
  for (const _ of instance.overEmbeddedDataSpecificationsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.embeddedDataSpecifications?.length ?? 0,
  );
});

test("SubmodelElementCollection.overEmbeddedDataSpecificationsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalSubmodelElementCollection();

  let count = 0;
  for (const _ of instance.overEmbeddedDataSpecificationsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.embeddedDataSpecifications?.length ?? 0,
  );
});

test("SubmodelElementCollection.overValueOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalSubmodelElementCollection();

  let count = 0;
  for (const _ of instance.overValueOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.value?.length ?? 0,
  );
});

test("SubmodelElementCollection.overValueOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalSubmodelElementCollection();

  let count = 0;
  for (const _ of instance.overValueOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.value?.length ?? 0,
  );
});

test("Property.overExtensionsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalProperty();

  let count = 0;
  for (const _ of instance.overExtensionsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.extensions?.length ?? 0,
  );
});

test("Property.overExtensionsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalProperty();

  let count = 0;
  for (const _ of instance.overExtensionsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.extensions?.length ?? 0,
  );
});

test("Property.overDisplayNameOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalProperty();

  let count = 0;
  for (const _ of instance.overDisplayNameOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.displayName?.length ?? 0,
  );
});

test("Property.overDisplayNameOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalProperty();

  let count = 0;
  for (const _ of instance.overDisplayNameOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.displayName?.length ?? 0,
  );
});

test("Property.overDescriptionOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalProperty();

  let count = 0;
  for (const _ of instance.overDescriptionOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.description?.length ?? 0,
  );
});

test("Property.overDescriptionOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalProperty();

  let count = 0;
  for (const _ of instance.overDescriptionOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.description?.length ?? 0,
  );
});

test("Property.overSupplementalSemanticIdsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalProperty();

  let count = 0;
  for (const _ of instance.overSupplementalSemanticIdsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.supplementalSemanticIds?.length ?? 0,
  );
});

test("Property.overSupplementalSemanticIdsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalProperty();

  let count = 0;
  for (const _ of instance.overSupplementalSemanticIdsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.supplementalSemanticIds?.length ?? 0,
  );
});

test("Property.overQualifiersOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalProperty();

  let count = 0;
  for (const _ of instance.overQualifiersOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.qualifiers?.length ?? 0,
  );
});

test("Property.overQualifiersOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalProperty();

  let count = 0;
  for (const _ of instance.overQualifiersOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.qualifiers?.length ?? 0,
  );
});

test("Property.overEmbeddedDataSpecificationsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalProperty();

  let count = 0;
  for (const _ of instance.overEmbeddedDataSpecificationsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.embeddedDataSpecifications?.length ?? 0,
  );
});

test("Property.overEmbeddedDataSpecificationsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalProperty();

  let count = 0;
  for (const _ of instance.overEmbeddedDataSpecificationsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.embeddedDataSpecifications?.length ?? 0,
  );
});

test("MultiLanguageProperty.overExtensionsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalMultiLanguageProperty();

  let count = 0;
  for (const _ of instance.overExtensionsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.extensions?.length ?? 0,
  );
});

test("MultiLanguageProperty.overExtensionsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalMultiLanguageProperty();

  let count = 0;
  for (const _ of instance.overExtensionsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.extensions?.length ?? 0,
  );
});

test("MultiLanguageProperty.overDisplayNameOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalMultiLanguageProperty();

  let count = 0;
  for (const _ of instance.overDisplayNameOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.displayName?.length ?? 0,
  );
});

test("MultiLanguageProperty.overDisplayNameOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalMultiLanguageProperty();

  let count = 0;
  for (const _ of instance.overDisplayNameOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.displayName?.length ?? 0,
  );
});

test("MultiLanguageProperty.overDescriptionOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalMultiLanguageProperty();

  let count = 0;
  for (const _ of instance.overDescriptionOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.description?.length ?? 0,
  );
});

test("MultiLanguageProperty.overDescriptionOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalMultiLanguageProperty();

  let count = 0;
  for (const _ of instance.overDescriptionOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.description?.length ?? 0,
  );
});

test("MultiLanguageProperty.overSupplementalSemanticIdsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalMultiLanguageProperty();

  let count = 0;
  for (const _ of instance.overSupplementalSemanticIdsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.supplementalSemanticIds?.length ?? 0,
  );
});

test("MultiLanguageProperty.overSupplementalSemanticIdsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalMultiLanguageProperty();

  let count = 0;
  for (const _ of instance.overSupplementalSemanticIdsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.supplementalSemanticIds?.length ?? 0,
  );
});

test("MultiLanguageProperty.overQualifiersOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalMultiLanguageProperty();

  let count = 0;
  for (const _ of instance.overQualifiersOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.qualifiers?.length ?? 0,
  );
});

test("MultiLanguageProperty.overQualifiersOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalMultiLanguageProperty();

  let count = 0;
  for (const _ of instance.overQualifiersOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.qualifiers?.length ?? 0,
  );
});

test("MultiLanguageProperty.overEmbeddedDataSpecificationsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalMultiLanguageProperty();

  let count = 0;
  for (const _ of instance.overEmbeddedDataSpecificationsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.embeddedDataSpecifications?.length ?? 0,
  );
});

test("MultiLanguageProperty.overEmbeddedDataSpecificationsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalMultiLanguageProperty();

  let count = 0;
  for (const _ of instance.overEmbeddedDataSpecificationsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.embeddedDataSpecifications?.length ?? 0,
  );
});

test("MultiLanguageProperty.overValueOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalMultiLanguageProperty();

  let count = 0;
  for (const _ of instance.overValueOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.value?.length ?? 0,
  );
});

test("MultiLanguageProperty.overValueOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalMultiLanguageProperty();

  let count = 0;
  for (const _ of instance.overValueOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.value?.length ?? 0,
  );
});

test("Range.overExtensionsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalRange();

  let count = 0;
  for (const _ of instance.overExtensionsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.extensions?.length ?? 0,
  );
});

test("Range.overExtensionsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalRange();

  let count = 0;
  for (const _ of instance.overExtensionsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.extensions?.length ?? 0,
  );
});

test("Range.overDisplayNameOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalRange();

  let count = 0;
  for (const _ of instance.overDisplayNameOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.displayName?.length ?? 0,
  );
});

test("Range.overDisplayNameOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalRange();

  let count = 0;
  for (const _ of instance.overDisplayNameOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.displayName?.length ?? 0,
  );
});

test("Range.overDescriptionOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalRange();

  let count = 0;
  for (const _ of instance.overDescriptionOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.description?.length ?? 0,
  );
});

test("Range.overDescriptionOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalRange();

  let count = 0;
  for (const _ of instance.overDescriptionOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.description?.length ?? 0,
  );
});

test("Range.overSupplementalSemanticIdsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalRange();

  let count = 0;
  for (const _ of instance.overSupplementalSemanticIdsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.supplementalSemanticIds?.length ?? 0,
  );
});

test("Range.overSupplementalSemanticIdsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalRange();

  let count = 0;
  for (const _ of instance.overSupplementalSemanticIdsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.supplementalSemanticIds?.length ?? 0,
  );
});

test("Range.overQualifiersOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalRange();

  let count = 0;
  for (const _ of instance.overQualifiersOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.qualifiers?.length ?? 0,
  );
});

test("Range.overQualifiersOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalRange();

  let count = 0;
  for (const _ of instance.overQualifiersOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.qualifiers?.length ?? 0,
  );
});

test("Range.overEmbeddedDataSpecificationsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalRange();

  let count = 0;
  for (const _ of instance.overEmbeddedDataSpecificationsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.embeddedDataSpecifications?.length ?? 0,
  );
});

test("Range.overEmbeddedDataSpecificationsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalRange();

  let count = 0;
  for (const _ of instance.overEmbeddedDataSpecificationsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.embeddedDataSpecifications?.length ?? 0,
  );
});

test("ReferenceElement.overExtensionsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalReferenceElement();

  let count = 0;
  for (const _ of instance.overExtensionsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.extensions?.length ?? 0,
  );
});

test("ReferenceElement.overExtensionsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalReferenceElement();

  let count = 0;
  for (const _ of instance.overExtensionsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.extensions?.length ?? 0,
  );
});

test("ReferenceElement.overDisplayNameOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalReferenceElement();

  let count = 0;
  for (const _ of instance.overDisplayNameOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.displayName?.length ?? 0,
  );
});

test("ReferenceElement.overDisplayNameOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalReferenceElement();

  let count = 0;
  for (const _ of instance.overDisplayNameOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.displayName?.length ?? 0,
  );
});

test("ReferenceElement.overDescriptionOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalReferenceElement();

  let count = 0;
  for (const _ of instance.overDescriptionOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.description?.length ?? 0,
  );
});

test("ReferenceElement.overDescriptionOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalReferenceElement();

  let count = 0;
  for (const _ of instance.overDescriptionOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.description?.length ?? 0,
  );
});

test("ReferenceElement.overSupplementalSemanticIdsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalReferenceElement();

  let count = 0;
  for (const _ of instance.overSupplementalSemanticIdsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.supplementalSemanticIds?.length ?? 0,
  );
});

test("ReferenceElement.overSupplementalSemanticIdsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalReferenceElement();

  let count = 0;
  for (const _ of instance.overSupplementalSemanticIdsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.supplementalSemanticIds?.length ?? 0,
  );
});

test("ReferenceElement.overQualifiersOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalReferenceElement();

  let count = 0;
  for (const _ of instance.overQualifiersOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.qualifiers?.length ?? 0,
  );
});

test("ReferenceElement.overQualifiersOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalReferenceElement();

  let count = 0;
  for (const _ of instance.overQualifiersOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.qualifiers?.length ?? 0,
  );
});

test("ReferenceElement.overEmbeddedDataSpecificationsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalReferenceElement();

  let count = 0;
  for (const _ of instance.overEmbeddedDataSpecificationsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.embeddedDataSpecifications?.length ?? 0,
  );
});

test("ReferenceElement.overEmbeddedDataSpecificationsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalReferenceElement();

  let count = 0;
  for (const _ of instance.overEmbeddedDataSpecificationsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.embeddedDataSpecifications?.length ?? 0,
  );
});

test("Blob.overExtensionsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalBlob();

  let count = 0;
  for (const _ of instance.overExtensionsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.extensions?.length ?? 0,
  );
});

test("Blob.overExtensionsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalBlob();

  let count = 0;
  for (const _ of instance.overExtensionsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.extensions?.length ?? 0,
  );
});

test("Blob.overDisplayNameOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalBlob();

  let count = 0;
  for (const _ of instance.overDisplayNameOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.displayName?.length ?? 0,
  );
});

test("Blob.overDisplayNameOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalBlob();

  let count = 0;
  for (const _ of instance.overDisplayNameOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.displayName?.length ?? 0,
  );
});

test("Blob.overDescriptionOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalBlob();

  let count = 0;
  for (const _ of instance.overDescriptionOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.description?.length ?? 0,
  );
});

test("Blob.overDescriptionOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalBlob();

  let count = 0;
  for (const _ of instance.overDescriptionOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.description?.length ?? 0,
  );
});

test("Blob.overSupplementalSemanticIdsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalBlob();

  let count = 0;
  for (const _ of instance.overSupplementalSemanticIdsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.supplementalSemanticIds?.length ?? 0,
  );
});

test("Blob.overSupplementalSemanticIdsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalBlob();

  let count = 0;
  for (const _ of instance.overSupplementalSemanticIdsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.supplementalSemanticIds?.length ?? 0,
  );
});

test("Blob.overQualifiersOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalBlob();

  let count = 0;
  for (const _ of instance.overQualifiersOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.qualifiers?.length ?? 0,
  );
});

test("Blob.overQualifiersOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalBlob();

  let count = 0;
  for (const _ of instance.overQualifiersOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.qualifiers?.length ?? 0,
  );
});

test("Blob.overEmbeddedDataSpecificationsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalBlob();

  let count = 0;
  for (const _ of instance.overEmbeddedDataSpecificationsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.embeddedDataSpecifications?.length ?? 0,
  );
});

test("Blob.overEmbeddedDataSpecificationsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalBlob();

  let count = 0;
  for (const _ of instance.overEmbeddedDataSpecificationsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.embeddedDataSpecifications?.length ?? 0,
  );
});

test("File.overExtensionsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalFile();

  let count = 0;
  for (const _ of instance.overExtensionsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.extensions?.length ?? 0,
  );
});

test("File.overExtensionsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalFile();

  let count = 0;
  for (const _ of instance.overExtensionsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.extensions?.length ?? 0,
  );
});

test("File.overDisplayNameOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalFile();

  let count = 0;
  for (const _ of instance.overDisplayNameOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.displayName?.length ?? 0,
  );
});

test("File.overDisplayNameOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalFile();

  let count = 0;
  for (const _ of instance.overDisplayNameOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.displayName?.length ?? 0,
  );
});

test("File.overDescriptionOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalFile();

  let count = 0;
  for (const _ of instance.overDescriptionOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.description?.length ?? 0,
  );
});

test("File.overDescriptionOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalFile();

  let count = 0;
  for (const _ of instance.overDescriptionOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.description?.length ?? 0,
  );
});

test("File.overSupplementalSemanticIdsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalFile();

  let count = 0;
  for (const _ of instance.overSupplementalSemanticIdsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.supplementalSemanticIds?.length ?? 0,
  );
});

test("File.overSupplementalSemanticIdsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalFile();

  let count = 0;
  for (const _ of instance.overSupplementalSemanticIdsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.supplementalSemanticIds?.length ?? 0,
  );
});

test("File.overQualifiersOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalFile();

  let count = 0;
  for (const _ of instance.overQualifiersOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.qualifiers?.length ?? 0,
  );
});

test("File.overQualifiersOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalFile();

  let count = 0;
  for (const _ of instance.overQualifiersOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.qualifiers?.length ?? 0,
  );
});

test("File.overEmbeddedDataSpecificationsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalFile();

  let count = 0;
  for (const _ of instance.overEmbeddedDataSpecificationsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.embeddedDataSpecifications?.length ?? 0,
  );
});

test("File.overEmbeddedDataSpecificationsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalFile();

  let count = 0;
  for (const _ of instance.overEmbeddedDataSpecificationsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.embeddedDataSpecifications?.length ?? 0,
  );
});

test("AnnotatedRelationshipElement.overExtensionsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalAnnotatedRelationshipElement();

  let count = 0;
  for (const _ of instance.overExtensionsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.extensions?.length ?? 0,
  );
});

test("AnnotatedRelationshipElement.overExtensionsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalAnnotatedRelationshipElement();

  let count = 0;
  for (const _ of instance.overExtensionsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.extensions?.length ?? 0,
  );
});

test("AnnotatedRelationshipElement.overDisplayNameOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalAnnotatedRelationshipElement();

  let count = 0;
  for (const _ of instance.overDisplayNameOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.displayName?.length ?? 0,
  );
});

test("AnnotatedRelationshipElement.overDisplayNameOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalAnnotatedRelationshipElement();

  let count = 0;
  for (const _ of instance.overDisplayNameOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.displayName?.length ?? 0,
  );
});

test("AnnotatedRelationshipElement.overDescriptionOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalAnnotatedRelationshipElement();

  let count = 0;
  for (const _ of instance.overDescriptionOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.description?.length ?? 0,
  );
});

test("AnnotatedRelationshipElement.overDescriptionOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalAnnotatedRelationshipElement();

  let count = 0;
  for (const _ of instance.overDescriptionOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.description?.length ?? 0,
  );
});

test("AnnotatedRelationshipElement.overSupplementalSemanticIdsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalAnnotatedRelationshipElement();

  let count = 0;
  for (const _ of instance.overSupplementalSemanticIdsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.supplementalSemanticIds?.length ?? 0,
  );
});

test("AnnotatedRelationshipElement.overSupplementalSemanticIdsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalAnnotatedRelationshipElement();

  let count = 0;
  for (const _ of instance.overSupplementalSemanticIdsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.supplementalSemanticIds?.length ?? 0,
  );
});

test("AnnotatedRelationshipElement.overQualifiersOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalAnnotatedRelationshipElement();

  let count = 0;
  for (const _ of instance.overQualifiersOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.qualifiers?.length ?? 0,
  );
});

test("AnnotatedRelationshipElement.overQualifiersOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalAnnotatedRelationshipElement();

  let count = 0;
  for (const _ of instance.overQualifiersOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.qualifiers?.length ?? 0,
  );
});

test("AnnotatedRelationshipElement.overEmbeddedDataSpecificationsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalAnnotatedRelationshipElement();

  let count = 0;
  for (const _ of instance.overEmbeddedDataSpecificationsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.embeddedDataSpecifications?.length ?? 0,
  );
});

test("AnnotatedRelationshipElement.overEmbeddedDataSpecificationsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalAnnotatedRelationshipElement();

  let count = 0;
  for (const _ of instance.overEmbeddedDataSpecificationsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.embeddedDataSpecifications?.length ?? 0,
  );
});

test("AnnotatedRelationshipElement.overAnnotationsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalAnnotatedRelationshipElement();

  let count = 0;
  for (const _ of instance.overAnnotationsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.annotations?.length ?? 0,
  );
});

test("AnnotatedRelationshipElement.overAnnotationsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalAnnotatedRelationshipElement();

  let count = 0;
  for (const _ of instance.overAnnotationsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.annotations?.length ?? 0,
  );
});

test("Entity.overExtensionsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalEntity();

  let count = 0;
  for (const _ of instance.overExtensionsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.extensions?.length ?? 0,
  );
});

test("Entity.overExtensionsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalEntity();

  let count = 0;
  for (const _ of instance.overExtensionsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.extensions?.length ?? 0,
  );
});

test("Entity.overDisplayNameOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalEntity();

  let count = 0;
  for (const _ of instance.overDisplayNameOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.displayName?.length ?? 0,
  );
});

test("Entity.overDisplayNameOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalEntity();

  let count = 0;
  for (const _ of instance.overDisplayNameOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.displayName?.length ?? 0,
  );
});

test("Entity.overDescriptionOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalEntity();

  let count = 0;
  for (const _ of instance.overDescriptionOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.description?.length ?? 0,
  );
});

test("Entity.overDescriptionOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalEntity();

  let count = 0;
  for (const _ of instance.overDescriptionOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.description?.length ?? 0,
  );
});

test("Entity.overSupplementalSemanticIdsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalEntity();

  let count = 0;
  for (const _ of instance.overSupplementalSemanticIdsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.supplementalSemanticIds?.length ?? 0,
  );
});

test("Entity.overSupplementalSemanticIdsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalEntity();

  let count = 0;
  for (const _ of instance.overSupplementalSemanticIdsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.supplementalSemanticIds?.length ?? 0,
  );
});

test("Entity.overQualifiersOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalEntity();

  let count = 0;
  for (const _ of instance.overQualifiersOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.qualifiers?.length ?? 0,
  );
});

test("Entity.overQualifiersOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalEntity();

  let count = 0;
  for (const _ of instance.overQualifiersOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.qualifiers?.length ?? 0,
  );
});

test("Entity.overEmbeddedDataSpecificationsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalEntity();

  let count = 0;
  for (const _ of instance.overEmbeddedDataSpecificationsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.embeddedDataSpecifications?.length ?? 0,
  );
});

test("Entity.overEmbeddedDataSpecificationsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalEntity();

  let count = 0;
  for (const _ of instance.overEmbeddedDataSpecificationsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.embeddedDataSpecifications?.length ?? 0,
  );
});

test("Entity.overStatementsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalEntity();

  let count = 0;
  for (const _ of instance.overStatementsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.statements?.length ?? 0,
  );
});

test("Entity.overStatementsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalEntity();

  let count = 0;
  for (const _ of instance.overStatementsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.statements?.length ?? 0,
  );
});

test("Entity.overSpecificAssetIdsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalEntity();

  let count = 0;
  for (const _ of instance.overSpecificAssetIdsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.specificAssetIds?.length ?? 0,
  );
});

test("Entity.overSpecificAssetIdsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalEntity();

  let count = 0;
  for (const _ of instance.overSpecificAssetIdsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.specificAssetIds?.length ?? 0,
  );
});

test("BasicEventElement.overExtensionsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalBasicEventElement();

  let count = 0;
  for (const _ of instance.overExtensionsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.extensions?.length ?? 0,
  );
});

test("BasicEventElement.overExtensionsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalBasicEventElement();

  let count = 0;
  for (const _ of instance.overExtensionsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.extensions?.length ?? 0,
  );
});

test("BasicEventElement.overDisplayNameOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalBasicEventElement();

  let count = 0;
  for (const _ of instance.overDisplayNameOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.displayName?.length ?? 0,
  );
});

test("BasicEventElement.overDisplayNameOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalBasicEventElement();

  let count = 0;
  for (const _ of instance.overDisplayNameOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.displayName?.length ?? 0,
  );
});

test("BasicEventElement.overDescriptionOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalBasicEventElement();

  let count = 0;
  for (const _ of instance.overDescriptionOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.description?.length ?? 0,
  );
});

test("BasicEventElement.overDescriptionOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalBasicEventElement();

  let count = 0;
  for (const _ of instance.overDescriptionOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.description?.length ?? 0,
  );
});

test("BasicEventElement.overSupplementalSemanticIdsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalBasicEventElement();

  let count = 0;
  for (const _ of instance.overSupplementalSemanticIdsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.supplementalSemanticIds?.length ?? 0,
  );
});

test("BasicEventElement.overSupplementalSemanticIdsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalBasicEventElement();

  let count = 0;
  for (const _ of instance.overSupplementalSemanticIdsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.supplementalSemanticIds?.length ?? 0,
  );
});

test("BasicEventElement.overQualifiersOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalBasicEventElement();

  let count = 0;
  for (const _ of instance.overQualifiersOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.qualifiers?.length ?? 0,
  );
});

test("BasicEventElement.overQualifiersOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalBasicEventElement();

  let count = 0;
  for (const _ of instance.overQualifiersOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.qualifiers?.length ?? 0,
  );
});

test("BasicEventElement.overEmbeddedDataSpecificationsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalBasicEventElement();

  let count = 0;
  for (const _ of instance.overEmbeddedDataSpecificationsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.embeddedDataSpecifications?.length ?? 0,
  );
});

test("BasicEventElement.overEmbeddedDataSpecificationsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalBasicEventElement();

  let count = 0;
  for (const _ of instance.overEmbeddedDataSpecificationsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.embeddedDataSpecifications?.length ?? 0,
  );
});

test("Operation.overExtensionsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalOperation();

  let count = 0;
  for (const _ of instance.overExtensionsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.extensions?.length ?? 0,
  );
});

test("Operation.overExtensionsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalOperation();

  let count = 0;
  for (const _ of instance.overExtensionsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.extensions?.length ?? 0,
  );
});

test("Operation.overDisplayNameOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalOperation();

  let count = 0;
  for (const _ of instance.overDisplayNameOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.displayName?.length ?? 0,
  );
});

test("Operation.overDisplayNameOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalOperation();

  let count = 0;
  for (const _ of instance.overDisplayNameOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.displayName?.length ?? 0,
  );
});

test("Operation.overDescriptionOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalOperation();

  let count = 0;
  for (const _ of instance.overDescriptionOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.description?.length ?? 0,
  );
});

test("Operation.overDescriptionOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalOperation();

  let count = 0;
  for (const _ of instance.overDescriptionOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.description?.length ?? 0,
  );
});

test("Operation.overSupplementalSemanticIdsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalOperation();

  let count = 0;
  for (const _ of instance.overSupplementalSemanticIdsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.supplementalSemanticIds?.length ?? 0,
  );
});

test("Operation.overSupplementalSemanticIdsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalOperation();

  let count = 0;
  for (const _ of instance.overSupplementalSemanticIdsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.supplementalSemanticIds?.length ?? 0,
  );
});

test("Operation.overQualifiersOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalOperation();

  let count = 0;
  for (const _ of instance.overQualifiersOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.qualifiers?.length ?? 0,
  );
});

test("Operation.overQualifiersOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalOperation();

  let count = 0;
  for (const _ of instance.overQualifiersOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.qualifiers?.length ?? 0,
  );
});

test("Operation.overEmbeddedDataSpecificationsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalOperation();

  let count = 0;
  for (const _ of instance.overEmbeddedDataSpecificationsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.embeddedDataSpecifications?.length ?? 0,
  );
});

test("Operation.overEmbeddedDataSpecificationsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalOperation();

  let count = 0;
  for (const _ of instance.overEmbeddedDataSpecificationsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.embeddedDataSpecifications?.length ?? 0,
  );
});

test("Operation.overInputVariablesOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalOperation();

  let count = 0;
  for (const _ of instance.overInputVariablesOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.inputVariables?.length ?? 0,
  );
});

test("Operation.overInputVariablesOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalOperation();

  let count = 0;
  for (const _ of instance.overInputVariablesOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.inputVariables?.length ?? 0,
  );
});

test("Operation.overOutputVariablesOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalOperation();

  let count = 0;
  for (const _ of instance.overOutputVariablesOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.outputVariables?.length ?? 0,
  );
});

test("Operation.overOutputVariablesOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalOperation();

  let count = 0;
  for (const _ of instance.overOutputVariablesOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.outputVariables?.length ?? 0,
  );
});

test("Operation.overInoutputVariablesOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalOperation();

  let count = 0;
  for (const _ of instance.overInoutputVariablesOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.inoutputVariables?.length ?? 0,
  );
});

test("Operation.overInoutputVariablesOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalOperation();

  let count = 0;
  for (const _ of instance.overInoutputVariablesOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.inoutputVariables?.length ?? 0,
  );
});

test("Capability.overExtensionsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalCapability();

  let count = 0;
  for (const _ of instance.overExtensionsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.extensions?.length ?? 0,
  );
});

test("Capability.overExtensionsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalCapability();

  let count = 0;
  for (const _ of instance.overExtensionsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.extensions?.length ?? 0,
  );
});

test("Capability.overDisplayNameOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalCapability();

  let count = 0;
  for (const _ of instance.overDisplayNameOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.displayName?.length ?? 0,
  );
});

test("Capability.overDisplayNameOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalCapability();

  let count = 0;
  for (const _ of instance.overDisplayNameOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.displayName?.length ?? 0,
  );
});

test("Capability.overDescriptionOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalCapability();

  let count = 0;
  for (const _ of instance.overDescriptionOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.description?.length ?? 0,
  );
});

test("Capability.overDescriptionOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalCapability();

  let count = 0;
  for (const _ of instance.overDescriptionOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.description?.length ?? 0,
  );
});

test("Capability.overSupplementalSemanticIdsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalCapability();

  let count = 0;
  for (const _ of instance.overSupplementalSemanticIdsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.supplementalSemanticIds?.length ?? 0,
  );
});

test("Capability.overSupplementalSemanticIdsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalCapability();

  let count = 0;
  for (const _ of instance.overSupplementalSemanticIdsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.supplementalSemanticIds?.length ?? 0,
  );
});

test("Capability.overQualifiersOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalCapability();

  let count = 0;
  for (const _ of instance.overQualifiersOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.qualifiers?.length ?? 0,
  );
});

test("Capability.overQualifiersOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalCapability();

  let count = 0;
  for (const _ of instance.overQualifiersOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.qualifiers?.length ?? 0,
  );
});

test("Capability.overEmbeddedDataSpecificationsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalCapability();

  let count = 0;
  for (const _ of instance.overEmbeddedDataSpecificationsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.embeddedDataSpecifications?.length ?? 0,
  );
});

test("Capability.overEmbeddedDataSpecificationsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalCapability();

  let count = 0;
  for (const _ of instance.overEmbeddedDataSpecificationsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.embeddedDataSpecifications?.length ?? 0,
  );
});

test("ConceptDescription.overExtensionsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalConceptDescription();

  let count = 0;
  for (const _ of instance.overExtensionsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.extensions?.length ?? 0,
  );
});

test("ConceptDescription.overExtensionsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalConceptDescription();

  let count = 0;
  for (const _ of instance.overExtensionsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.extensions?.length ?? 0,
  );
});

test("ConceptDescription.overDisplayNameOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalConceptDescription();

  let count = 0;
  for (const _ of instance.overDisplayNameOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.displayName?.length ?? 0,
  );
});

test("ConceptDescription.overDisplayNameOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalConceptDescription();

  let count = 0;
  for (const _ of instance.overDisplayNameOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.displayName?.length ?? 0,
  );
});

test("ConceptDescription.overDescriptionOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalConceptDescription();

  let count = 0;
  for (const _ of instance.overDescriptionOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.description?.length ?? 0,
  );
});

test("ConceptDescription.overDescriptionOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalConceptDescription();

  let count = 0;
  for (const _ of instance.overDescriptionOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.description?.length ?? 0,
  );
});

test("ConceptDescription.overEmbeddedDataSpecificationsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalConceptDescription();

  let count = 0;
  for (const _ of instance.overEmbeddedDataSpecificationsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.embeddedDataSpecifications?.length ?? 0,
  );
});

test("ConceptDescription.overEmbeddedDataSpecificationsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalConceptDescription();

  let count = 0;
  for (const _ of instance.overEmbeddedDataSpecificationsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.embeddedDataSpecifications?.length ?? 0,
  );
});

test("ConceptDescription.overIsCaseOfOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalConceptDescription();

  let count = 0;
  for (const _ of instance.overIsCaseOfOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.isCaseOf?.length ?? 0,
  );
});

test("ConceptDescription.overIsCaseOfOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalConceptDescription();

  let count = 0;
  for (const _ of instance.overIsCaseOfOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.isCaseOf?.length ?? 0,
  );
});

test("Environment.overAssetAdministrationShellsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalEnvironment();

  let count = 0;
  for (const _ of instance.overAssetAdministrationShellsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.assetAdministrationShells?.length ?? 0,
  );
});

test("Environment.overAssetAdministrationShellsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalEnvironment();

  let count = 0;
  for (const _ of instance.overAssetAdministrationShellsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.assetAdministrationShells?.length ?? 0,
  );
});

test("Environment.overSubmodelsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalEnvironment();

  let count = 0;
  for (const _ of instance.overSubmodelsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.submodels?.length ?? 0,
  );
});

test("Environment.overSubmodelsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalEnvironment();

  let count = 0;
  for (const _ of instance.overSubmodelsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.submodels?.length ?? 0,
  );
});

test("Environment.overConceptDescriptionsOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalEnvironment();

  let count = 0;
  for (const _ of instance.overConceptDescriptionsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.conceptDescriptions?.length ?? 0,
  );
});

test("Environment.overConceptDescriptionsOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalEnvironment();

  let count = 0;
  for (const _ of instance.overConceptDescriptionsOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.conceptDescriptions?.length ?? 0,
  );
});

test("DataSpecificationIec61360.overShortNameOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalDataSpecificationIec61360();

  let count = 0;
  for (const _ of instance.overShortNameOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.shortName?.length ?? 0,
  );
});

test("DataSpecificationIec61360.overShortNameOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalDataSpecificationIec61360();

  let count = 0;
  for (const _ of instance.overShortNameOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.shortName?.length ?? 0,
  );
});

test("DataSpecificationIec61360.overDefinitionOrEmpty on maximal", () => {
  const instance = TestCommonJsonization.loadMaximalDataSpecificationIec61360();

  let count = 0;
  for (const _ of instance.overDefinitionOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.definition?.length ?? 0,
  );
});

test("DataSpecificationIec61360.overDefinitionOrEmpty on minimal", () => {
  const instance = TestCommonJsonization.loadMinimalDataSpecificationIec61360();

  let count = 0;
  for (const _ of instance.overDefinitionOrEmpty()) {
    count++;
  }

  expect(count).toStrictEqual(
    instance.definition?.length ?? 0,
  );
});

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.
